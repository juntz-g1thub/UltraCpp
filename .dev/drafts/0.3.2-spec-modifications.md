# UltraCPP 0.3.2 — Spec Modifications (草案)

**Date**: 2026-08-12
**Status**: draft · 待 spec 维护者审
**Owner**: orch-plan-writer
**基础 spec**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.0-spec-zh-CN.md` (2214 行, 2026-08-12)
**0.3.1 基础 spec**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.1-spec-zh-CN.md` (0.3.1 修订后, 待创建)
**关联 commit**: (待 0.3.2 milestone commit)
**关联 plan 文档**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.2-implementation-plan.md` (实施计划 — 给 implementer 用)
**关联 0.3.1 文档**:
- `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.1-spec-amendment-s2-lvalue.md` (S2 实施计划)
- `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.1-spec-text-changes.md` (S2 spec 文本变更)
**关联优先级文档**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.0-m0-priority.md` §9.x (S4, S5 rows)
**Estimated effort**: S (spec 维护者 1.5-2h)

---

## 1. 元信息 (Metadata)

| 字段 | 值 |
|------|-----|
| 文档标题 | UltraCPP Spec 0.3.2 — Modifications |
| 文档路径 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.2-spec-modifications.md` |
| 基础 spec | `docs/UltraCPP-v0.3.1-spec-zh-CN.md` (0.3.1 修订后, 待创建) |
| 0.3.2 范围 | §4.8 + §6.1 + §6.2 修订 + §4.8.1 / §6.2.1 / §11.0 新增 |
| 0.3.2 触发 | m0_42 deref-assign bug 完全修复 (S2 部分 + S5 完整) + m0_41 abs_int 链接 (S5 builtin sig) |
| 状态 | draft, 待 spec 维护者审 |
| 关联 commit | (待 0.3.2 milestone commit) |
| 关联 plan | `0.3.2-implementation-plan.md` (实施计划) |
| Output spec 文件 | (待创建) `docs/UltraCPP-v0.3.2-spec-zh-CN.md` |

### 1.1 0.3.1 → 0.3.2 升级影响 (摘要)

| 维度 | 0.3.1 → 0.3.2 | 备注 |
|------|---------------|------|
| 范围 | bug fix + 概念补全 | 不是 breaking change |
| 新语法 | C-style cast `(T)expr` | 0.3.1 spec 完全无,0.3.2 加入 |
| 函数返回类型规则 | 隐式 → 显式 (§6.2.1) | 0.3.1 spec 缺失,0.3.2 加入 |
| builtin 签名 | 散落各处 → §11.0 总表 | 0.3.1 spec 无,0.3.2 加入 |
| codegen 行为 | UC_EXPR_CALL 硬编码 i32 → 从签名表取 | Bug C2 修复 |
| codegen 行为 | UC_EXPR_CALL 不设 last_expr_type → 正确设置 | Bug C1 修复 |
| deref-assign LHS | S2 部分修 (UC_EXPR_ASSIGN 走 lvalue) | S2 0.3.1 已落地 |

### 1.2 关联 doc 引用

- **0.3.1 spec-text-changes**: `.dev/drafts/0.3.1-spec-text-changes.md` (S2 = §4.13 lvalue / rvalue + §4.1 / §4.6 / §7.8 修订)
- **0.3.1 spec-amendment-s2-lvalue**: `.dev/drafts/0.3.1-spec-amendment-s2-lvalue.md` (S2 实施计划)
- **0.3.2 implementation-plan**: `.dev/drafts/0.3.2-implementation-plan.md` (本文档配套)

---

## 2. 概述 (Overview)

### 2.1 0.3.2 修订范围

0.3.2 (minor) 修 3 个代码 bug + 补 2 个 spec 概念缺失。**核心特征**:

| ID | 类型 | 章节 | 性质 |
|----|------|------|------|
| **S4** | spec 缺失 | §4.8 C-style cast `(T)expr` | 新增 §4.8.1 完整子节, 修订 §4.8 简介 |
| **S5** | spec 缺失 | §6.2 函数返回类型 infer | 新增 §6.2.1 完整子节, 修订 §6.1 + §6.2 |
| **§11.0** | spec 缺失 | builtin 签名总表 | 新增 §11.0 (放在 §11 之前), §11.x 引用 |
| **Bug C1** | code 缺陷 | `UC_EXPR_CALL` 不设 `last_expr_type` | 1-2 行 codegen 改 |
| **Bug C2** | code 缺陷 | `UC_EXPR_CALL` 返回类型硬编码 i32 | 由 S5 §11.0 修 |
| **Bug C3** | code+spec 双层 | m0_42 deref-assign LHS | S2 (0.3.1) 部分修 + 0.3.2 完整修 |

合计:**3 个新章节 (§4.8.1 / §6.2.1 / §11.0) + 4 个章节修订 (§4.8 / §6.1 / §6.2 / §11) + 0 个语法破坏性改动**。

### 2.2 触发原因

1. **m0_42 deref-assign 编译失败** — `*view = payload` 现状:
   - S2 (0.3.1) 部分修:`UC_EXPR_ASSIGN` LHS 走 lvalue 路径 (§4.13.4)
   - S5 (0.3.2) 完整修:`view` 类型从 malloc 返回的指针,cast 到 `int*` 需要 S4 提供的 C-style cast
   - 完整链路:`*((int*)malloc(8)) = payload;` 需要 S4 cast + S5 返回类型 infer
2. **m0_41 abs_int 链接失败** — 当前 `abs_int` 是 `extern "C"` 函数声明但无 body;无 builtin signature → codegen 不知道返回类型 → 硬编码 i32 错位
   - S5 修:§11.0 builtin 签名表收录 `abs_int(int) → int`,codegen 查表得返回类型
3. **m0_19 / m0_29 / m0_31 受益** — S5 builtin 签名表给 `factorial` / pointer 操作 提供正确返回类型;预期仍 PASS,但类型更准确

### 2.3 0.3.1 → 0.3.2 不破向后兼容

| 维度 | 兼容性 |
|------|--------|
| 语法 | 0.3.2 加 `(T)expr`,**0.3.1 写法 `T(expr)` 仍合法** (等价) |
| 函数定义 | 0.3.2 强化返回类型编译期检查,**0.3.1 无声明返回的函数仍按 §6.2.1 infer** (向下兼容) |
| 函数调用 | 0.3.2 codegen 从签名表取返回类型,**功能等价** (只是类型更准确) |
| API | 无变化 |
| ABI | 无变化 (LLVM IR 等价) |

---

## 3. 新增章节 (New Chapters)

### 3.1 新增 §4.8.1 C-style cast (完整子节)

**插入位置**: `docs/UltraCPP-v0.3.1-spec-zh-CN.md` §4.8 (行 563 末)、§4.9 (行 569) 之前。

> **注意**: §4.8 当前只有 4 行简介 (括号表达式);0.3.2 加 §4.8.1 作为 C-style cast 的完整子节。

#### 3.1.1 Before (0.3.1 现状 — §4.8)

```markdown
### 4.8 带括号表达式

```cpp
(expr)  // 括号中的任意表达式
```
```

#### 3.1.2 After (0.3.2 新增内容 — 在 §4.8 简介后, §4.9 前插入 §4.8.1)

```markdown
### 4.8 带括号表达式 *(0.3.2 修订)*

```cpp
(expr)  // 括号中的任意表达式
```

> **[0.3.2]** 当 `(T)` 中 `T` 是类型名而非表达式时, 表示 **C-style 显式类型转换**
> (cast), 见 §4.8.1。

#### 4.8.1 C-style 显式类型转换 `(`*type*`)`*expr* *(0.3.2 新增)*

C-style cast 是 UltraCPP 中最常用的显式类型转换形式, 语法与 C/C++ 一致:

```
'(' type ')' unary_expression
```

其中 `type` 见 §3 (类型系统), `unary_expression` 见 §4.1 (运算符优先级)。

##### 4.8.1.1 语义 *(0.3.2 新增)*

`(T)expr` 将表达式 `expr` 的值转换为类型 `T`。转换规则:

| 源类型 → 目标类型 | 行为 | codegen 层 |
|-------------------|------|-----------|
| `T*` → `T*` (同类型) | ✅ no-op (仅编译期确认) | 直接传递指针值 |
| `T*` → `void*` | ✅ 隐式允许 | 直接传递指针值 |
| `void*` → `T*` | ✅ 显式 cast (程序员担保类型正确) | bitcast (`bitcast T*` `...` to `T*`) |
| `void*` → `int` (i64) | ✅ 显式 cast | ptrtoint (`ptrtoint T*` `...` to `i64`) |
| `int` (i32/i64) → `T*` | ✅ 显式 cast | inttoptr (`inttoptr i64` `...` to `T*`) |
| `int` (i32) → `int` (i64) | ✅ 符号扩展 | sext (`sext i32` `...` to `i64`) |
| `int` (i64) → `int` (i32) | ✅ 截断 | trunc (`trunc i64` `...` to `i32`) |
| `int` → `float` / `double` | ✅ (若支持) | sitofp (`sitofp i32` `...` to `float`) |
| 不相关类型 (e.g. `int` → `Point`) | ❌ 编译期错误 `InvalidCastError` | — |

##### 4.8.1.2 与函数式 cast `T(expr)` 的关系 *(0.3.2 新增)*

UltraCPP 同时支持两种 cast 形式:

| 形式 | 名称 | 例子 |
|------|------|------|
| `T(expr)` | 函数式 cast (functional cast) | `int(3.14)` → 3 |
| `(T)expr` | C-style cast | `(int)3.14` → 3 |

两种形式**完全等价**。程序员可任选其一。UltraCPP 推荐使用 C-style cast
(与 C/C++ 生态对齐, FFI 互操作更直观)。

> **历史**: 0.3.0 / 0.3.1 spec 只显式支持 `T(expr)`, `(T)expr` 缺失, 导致 m0_42 中
> `*((int*)malloc(8))` 必须用函数式 `*((int*)malloc(8))` 但语义不清晰。0.3.2 加入
> C-style cast 后两种形式等价。

##### 4.8.1.3 示例 *(0.3.2 新增)*

```cpp
// === 整数 ↔ 指针互转 (FFI / 系统调用常用) ===
int* p = alloc(int);          // p: int*
void* vp = (void*)p;          // T* → void*: 隐式允许
int addr = (int)vp;           // void* → int: ptrtoint
int* p2 = (int*)addr;         // int → int*: inttoptr

// === 整数宽度转换 ===
int big = (int)1000000L;      // i64 → i32: trunc (语义: 取低 32 位)
long huge = (long)42;         // i32 → i64: sext

// === 类型断言 (程序员担保类型正确) ===
void* raw = malloc(8);
int* typed = (int*)raw;       // void* → int*: bitcast, 程序员担保 raw 实际是 int*

// === FFI 场景 ===
extern "C" {
    void* malloc(int size);
}
int* arr = (int*)malloc(8);   // 必须 cast void* → int*
*arr = 42;                     // 通过 typed 指针写
```

##### 4.8.1.4 编译期检查 *(0.3.2 新增)*

C-style cast 的编译期检查包括:

1. **`type` 必须是已知类型** (§3 类型系统中定义的类型, 包括 built-in + 用户定义)
2. **转换必须合法** (上表 8 行允许 + 1 行不相关类型拒绝)
3. **指针 ↔ 整数转换需显式 cast** (不能隐式 — 由 §3.5 决定, 0.3.2 不变)

错误类型: `InvalidCastError` (转换不合法) / `UnknownTypeError` (type 不识别)。

##### 4.8.1.5 与其他章节的交叉引用 *(0.3.2 新增)*

| 章节 | 关系 |
|------|------|
| §3 类型系统 | cast 目标 `T` 必须是 §3 定义的类型 |
| §4.1 优先级 | C-style cast 是 §4.1 第 2.5 级一元 op (与 `*` / `&` 同级, 右到左) |
| §4.8 (本节) | C-style cast 是带括号表达式的特殊形式 |
| §6.2 函数调用 | 调用结果可作 cast 源: `(int)factorial(5)` |
| §7.4 `malloc` 返回 | `void*` 返回值通常需要 cast 到具体类型 |
| §10 FFI | FFI 函数返回 `void*` 必 cast |
| §11.0 builtin 签名表 | builtin 函数返回类型已知 (无需 cast) |
| §12.1 EBNF | `cast_expression` 产生式见 §12.1 (0.3.2 新增) |

```

---

### 3.2 新增 §6.2.1 函数返回类型 (完整子节)

**插入位置**: `docs/UltraCPP-v0.3.1-spec-zh-CN.md` §6.2 (行 768 末)、§6.3 (行 825) 之前。

#### 3.2.1 Before (0.3.1 现状 — §6.2)

```markdown
### 6.2 函数调用

```cpp
int result = add(10, 20);
greet("Hello");
int fact = factorial(5);
```
```

#### 3.2.2 After (0.3.2 新增内容 — 在 §6.2 后, §6.3 前插入 §6.2.1)

```markdown
### 6.2 函数调用 *(0.3.2 修订)*

```cpp
int result = add(10, 20);
greet("Hello");
int fact = factorial(5);
```

> **[0.3.2]** 函数调用的**返回类型**由调用目标的**签名**决定。详见 §6.2.1。

#### 6.2.1 函数返回类型 *(0.3.2 新增)*

UltraCPP 中, 函数调用的返回类型由以下优先级决定:

##### 6.2.1.1 返回类型确定规则 *(0.3.2 新增)*

```
1. 若被调用函数是 builtin (见 §11.0 builtin 签名表)
   → 返回类型 = §11.0 表中签名
   例: factorial(5) 的返回类型 = int (来自 §11.0 表)
       abs_int(-5) 的返回类型 = int (来自 §11.0 表)

2. 若被调用函数是用户定义函数 (§6.1)
   → 返回类型 = 函数定义中声明的返回类型
   例: int add(int a, int b) → 返回类型 = int
       void greet(const char* name) → 返回类型 = void

3. 若被调用函数是 extern "C" (§10 FFI)
   → 返回类型 = 声明中的返回类型 (程序员担保)
   例: extern "C" int abs_int(int x); → 返回类型 = int
```

##### 6.2.1.2 返回类型传播 *(0.3.2 新增)*

调用表达式的**结果**有以下用途:

| 上下文 | 行为 |
|--------|------|
| 赋值 RHS | `int x = factorial(5);` — `factorial(5)` 的返回类型 (`int`) 必须可赋给 `x` |
| 表达式语句 | `factorial(5);` — 丢弃返回值, 合法 (类似 C) |
| 子表达式 | `int y = factorial(5) + 1;` — 返回值作算术运算输入 |
| 函数实参 | `print_num(factorial(5));` — 返回值传给 `print_num` |
| 条件 | `if (factorial(5) > 100)` — 返回值作 bool 上下文 |
| cast 源 | `(long)factorial(5)` — 见 §4.8.1 C-style cast |
| return 值 | `return factorial(5);` — 必须匹配函数声明返回类型 |

##### 6.2.1.3 void 函数调用 *(0.3.2 新增)*

返回类型为 `void` 的函数 (如 `print`, `print_num`):

- 调用表达式的"值"是 `void`, 没有具体类型
- 不可作赋值 RHS, 不可作子表达式, 不可作 return 值 (除非外层函数也返回 void)
- 可单独作语句: `print("hello");`

错误类型:
- `VoidUsedAsValueError` — void 结果被用作值
- `ReturnTypeMismatchError` — return 值与函数声明返回类型不匹配

##### 6.2.1.4 codegen 集成 *(0.3.2 新增)*

函数调用的 codegen 实现 (在 `src-c/src/codegen.c` 的 `UC_EXPR_CALL` case):

```
1. 求值所有实参 (走 rvalue 路径, §4.13.4)
2. emit call 指令: ret_val = call i32 @factorial(i32 5)
3. 设置 g->last_expr_type 为函数签名返回类型 (从 §11.0 表或函数定义取)
4. 返回 ret_val (后续表达式可继续使用)
```

例:

```c
int x = factorial(5);
// codegen:
//   %1 = call i32 @factorial(i32 5)   ; §11.0 表查得 factorial 返回 int (i32)
//   store i32 %1, i32* %x
//   g->last_expr_type = "i32"          ; 由 §11.0 提供, Bug C2 修复
```

##### 6.2.1.5 与其他章节的交叉引用 *(0.3.2 新增)*

| 章节 | 关系 |
|------|------|
| §6.1 函数定义 | 函数定义时声明的返回类型是 §6.2.1 优先级 2 的依据 |
| §10 FFI | extern 函数声明的返回类型是 §6.2.1 优先级 3 的依据 |
| §11.0 builtin 签名表 | builtin 函数返回类型是 §6.2.1 优先级 1 的依据 |
| §4.13.4 codegen 路径 | 调用表达式是 rvalue, 返回值类型由 §11.0 提供 |
| §7.4 malloc 返回 | `malloc` 返回 `void*`, 调用方需 cast 到具体类型 (§4.8.1) |
| §12.1 EBNF | 函数调用产生式见 §12.1 |

```

---

### 3.3 新增 §11.0 builtin 签名总表 (完整列表)

**插入位置**: `docs/UltraCPP-v0.3.1-spec-zh-CN.md` §11 (行 1565 之前, 作为 §11 节的引言)。

> **重要**: 当前 §11 直接从 §11.1 开始, 没有 §11.0。0.3.2 加 §11.0 作为 builtin 签名总表,
> §11.1 - §11.6 改为详细章节 (引用 §11.0)。

#### 3.3.1 Before (0.3.1 现状 — §11 直接进入 §11.1)

```markdown
---

## 11. 标准库约定

### 11.1 基本 I/O 函数
...
```

#### 3.3.2 After (0.3.2 新增内容 — 在 §11 标题后, §11.1 前插入 §11.0)

```markdown
---

## 11. 标准库约定

> **[0.3.2 新增]** 本章描述 UltraCPP 标准库函数的**签名约定**和**语义约定**。
> **builtin 签名总表**见 §11.0 (0.3.2 新增)。编译器在 codegen 阶段查询 §11.0
> 表以确定函数调用的返回类型, 详见 §6.2.1。

### 11.0 builtin 签名总表 *(0.3.2 新增)*

UltraCPP 提供以下 **builtin 函数**。builtin 函数由编译器**内置识别**:
- 不需要 `#include` 任何头文件
- 不需要在用户代码中声明
- codegen 阶段直接 emit 对应的 LLVM IR 调用

> **历史**: 0.3.1 spec 没有 builtin 签名总表, 散落在 §11.1-§11.6 中。0.3.2
> 集中为 §11.0, 便于 codegen 查询和 spec 维护。

#### 11.0.1 完整 builtin 签名表 *(0.3.2 新增)*

| 函数名 | 签名 | 返回类型 | 分类 | 详细说明 |
|--------|------|----------|------|----------|
| `print` | `void print(const char* s)` | `void` | I/O | §11.1 |
| `print_num` | `void print_num(int n)` | `void` | I/O | §11.1 |
| `print_float` | `void print_float(double f)` | `void` | I/O | §11.1 |
| `strlen` | `int strlen(const char* s)` | `int` | 字符串 | §11.2 |
| `strcpy` | `char* strcpy(char* dest, const char* src)` | `char*` | 字符串 | §11.2 |
| `strcmp` | `int strcmp(const char* a, const char* b)` | `int` | 字符串 | §11.2 |
| `memcpy` | `void* memcpy(void* dest, const void* src, int n)` | `void*` | 内存 | §11.3 |
| `memmove` | `void* memmove(void* dest, const void* src, int n)` | `void*` | 内存 | §11.3 |
| `memset` | `void* memset(void* s, int c, int n)` | `void*` | 内存 | §11.3 |
| `malloc` | `void* malloc(int size)` | `void*` | 内存 | §11.3, §7.4 |
| `free` | `void free(void* p)` | `void` | 内存 | §11.3, §7.4 |
| `sizeof_impl` | `int sizeof_impl()` | `int` | 工具 | §11.4 |
| `alignof_impl` | `int alignof_impl()` | `int` | 工具 | §11.4 |
| `is_null` | `bool is_null(int* ptr)` | `bool` | 工具 | §11.4 |
| `clone_impl` | `int* clone_impl(int* ptr)` | `int*` | 工具 | §11.4 |
| `move` | `T* move(T* p)` *(类型参数化)* | `T*` | 内存 | §7.5, §11.3 |
| `alloc` | `T* alloc(T)` *(类型参数化, 由 `alloc(int)` 等实例化)* | `T*` | 内存 | §7.4, §11.3 |
| `abs_int` | `int abs_int(int x)` | `int` | 数学 | 0.3.2 收录 (m0_41 触发) |

**类型映射到 LLVM IR**:

| UltraCPP 类型 | LLVM IR 类型 |
|---------------|--------------|
| `void` | `void` |
| `bool` | `i1` |
| `int` | `i32` |
| `long` / `int64` | `i64` |
| `float` | `float` |
| `double` | `double` |
| `char` | `i8` |
| `T*` | `T*` (e.g. `i32*`, `i8*`) |
| `void*` | `i8*` |
| `const char*` | `i8*` (无 LLVM const 概念) |

#### 11.0.2 codegen 集成 *(0.3.2 新增)*

`src-c/src/codegen.c` 中, builtin 签名表用 `static const struct` 数组维护:

```c
// 伪代码 (实际实现见 src-c/src/codegen.c)
typedef struct {
    const char* name;
    const char* ret_type;  // LLVM IR 类型字符串
    int is_void;
} builtin_sig_t;

static const builtin_sig_t builtin_sigs[] = {
    {"print",       "void", 1},
    {"print_num",   "void", 1},
    {"print_float", "void", 1},
    {"strlen",      "i32",  0},
    {"strcpy",      "i8*",  0},
    {"strcmp",      "i32",  0},
    {"memcpy",      "i8*",  0},
    {"memmove",     "i8*",  0},
    {"memset",      "i8*",  0},
    {"malloc",      "i8*",  0},
    {"free",        "void", 1},
    {"sizeof_impl", "i32",  0},
    {"alignof_impl","i32",  0},
    {"is_null",     "i1",   0},
    {"clone_impl",  "i32*", 0},
    {"abs_int",     "i32",  0},
    // 类型参数化的 alloc/move 单独处理
    {NULL, NULL, 0}  // sentinel
};
```

`UC_EXPR_CALL` 处理:

```c
case UC_EXPR_CALL:
    // ... (emit args, emit call)
    const char* fn_name = call_expr->as.call.callee;
    const builtin_sig_t* sig = lookup_builtin(fn_name);
    if (sig) {
        // builtin: 从签名表取返回类型
        g->last_expr_type = cgen_strdup(sig->ret_type);
        if (sig->is_void) {
            // void 函数无返回值, 不 emit ret_val
        }
    } else {
        // 用户函数: 从函数定义取返回类型
        // ... (查符号表)
    }
    break;
```

#### 11.0.3 添加新 builtin 的流程 *(0.3.2 新增)*

未来要加新 builtin (例如 `sqrt`, `pow`):

1. 在 §11.x 子节加详细说明 (语义、示例)
2. 在 §11.0 表加一行签名
3. 在 `src-c/src/codegen.c` 的 `builtin_sigs[]` 加一项
4. (可选) 在 `src-c/src/stdlib/` 加 stub 实现 (若 builtin 需要 runtime 支持)

#### 11.0.4 与其他章节的交叉引用 *(0.3.2 新增)*

| 章节 | 关系 |
|------|------|
| §6.2.1 函数返回类型 | builtin 签名是优先级 1 的依据 |
| §7.4 malloc/free | `malloc` / `free` 在表中 |
| §7.5 move | `move` 是 builtin (类型参数化) |
| §11.1 - §11.6 | 各子节详细说明引用 §11.0 |
| §12.1 EBNF | 函数调用产生式见 §12.1 |

```

---

## 4. 修改章节 (Modified Chapters)

### 4.1 修改 §4.8 带括号表达式 (简介)

**位置**: `docs/UltraCPP-v0.3.1-spec-zh-CN.md` §4.8 (行 563 ~ 566)。

#### 4.1.1 Before (0.3.1 现状)

```markdown
### 4.8 带括号表达式

```cpp
(expr)  // 括号中的任意表达式
```
```

#### 4.1.2 After (0.3.2 修订)

```markdown
### 4.8 带括号表达式 *(0.3.2 修订)*

```cpp
(expr)  // 括号中的任意表达式
```

> **[0.3.2]** 当 `(T)` 中 `T` 是**类型名**而非**表达式**时, 表示 **C-style 显式类型转换**
> (cast), 见 §4.8.1。C-style cast 与函数式 cast `T(expr)` 完全等价 (见 §4.8.1.2)。
```

#### 4.1.3 Rationale

- 0.3.1 §4.8 只说"括号中的任意表达式", 没说 `(T)` 中 `T` 是类型时是 cast
- 0.3.2 加 C-style cast 提示, 引导读者看 §4.8.1

---

### 4.2 修改 §6.1 函数定义 (加返回类型编译期检查)

**位置**: `docs/UltraCPP-v0.3.1-spec-zh-CN.md` §6.1 (行 749 ~ 766)。

#### 4.2.1 Before (0.3.1 现状)

```markdown
### 6.1 函数定义

```cpp
int add(int a, int b) {
    return a + b;
}

void greet(const char* name) {
    print(name);
}

int factorial(int n) {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}
```
```

#### 4.2.2 After (0.3.2 修订)

```markdown
### 6.1 函数定义 *(0.3.2 修订)*

```cpp
int add(int a, int b) {
    return a + b;
}

void greet(const char* name) {
    print(name);
}

int factorial(int n) {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}
```

> **[0.3.2]** 函数定义时**必须声明返回类型** (`int` / `void` / 用户类型 / `T*` 等)。
> 返回类型决定函数调用表达式的结果类型, 见 §6.2.1。
>
> **编译期检查**:
>
> 1. 函数体所有 `return` 语句的返回值必须与声明的返回类型**匹配** (允许隐式
>    数值拓宽, 如 `int` → `long`; 不允许窄化或类型无关)
> 2. 若声明为非 `void` 返回类型, 函数体**必须**至少有一个 `return` 语句 (或
>    在控制流上保证到达末尾时已返回, 编译器可放宽此检查)
> 3. 若声明为 `void` 返回类型, 函数体可有 `return;` (无值) 或省略 `return`
>
> 错误类型: `ReturnTypeMismatchError` (return 类型不匹配)。
```

#### 4.2.3 Rationale

- 0.3.1 §6.1 只给示例, 没显式说"返回类型必须声明"
- 0.3.2 加显式说明 + 编译期检查列表

---

### 4.3 修改 §6.2 函数调用 (加"返回类型从签名表取"说明)

**位置**: `docs/UltraCPP-v0.3.1-spec-zh-CN.md` §6.2 (行 768 ~ 773)。

#### 4.3.1 Before (0.3.1 现状)

```markdown
### 6.2 函数调用

```cpp
int result = add(10, 20);
greet("Hello");
int fact = factorial(5);
```
```

#### 4.3.2 After (0.3.2 修订)

```markdown
### 6.2 函数调用 *(0.3.2 修订)*

```cpp
int result = add(10, 20);
greet("Hello");
int fact = factorial(5);
```

> **[0.3.2]** 函数调用的**返回类型**由调用目标的**签名**决定。详见 §6.2.1
> 函数返回类型规则。简言之:
>
> - builtin 函数 (e.g. `print`, `factorial`, `malloc`): 返回类型查 §11.0 builtin 签名总表
> - 用户函数 (e.g. `add`): 返回类型 = 函数定义中声明的返回类型 (§6.1)
> - extern "C" 函数 (e.g. `abs_int`): 返回类型 = extern 声明中的返回类型 (§10)
>
> 返回类型传播规则见 §6.2.1.2 (赋值 RHS / 子表达式 / 函数实参 / cast 源 / return 值)。
```

#### 4.3.3 Rationale

- 0.3.1 §6.2 只给示例, 没说返回类型从哪里来
- 0.3.2 加显式规则, 引导读者看 §6.2.1 + §11.0

---

### 4.4 修改 §11 标准库约定 (加 §11.0 引用)

**位置**: `docs/UltraCPP-v0.3.1-spec-zh-CN.md` §11 标题 (行 1565)。

#### 4.4.1 Before (0.3.1 现状)

```markdown
---

## 11. 标准库约定

### 11.1 基本 I/O 函数
...
```

#### 4.4.2 After (0.3.2 修订)

```markdown
---

## 11. 标准库约定 *(0.3.2 修订)*

> **[0.3.2]** 本章描述 UltraCPP 标准库函数的**签名约定**和**语义约定**。
> **builtin 签名总表**见 §11.0 (0.3.2 新增)。编译器在 codegen 阶段查询 §11.0
> 表以确定函数调用的返回类型, 详见 §6.2.1。

### 11.0 builtin 签名总表

(详见 §3.3.2 完整 §11.0 内容)

### 11.1 基本 I/O 函数
...
```

#### 4.4.3 Rationale

- 0.3.1 §11 直接进入 §11.1, 缺总览
- 0.3.2 加 §11 标题修订注 + §11.0 总表引用

---

### 4.5 修改 §11.1 - §11.6 (各子节加 §11.0 引用)

**位置**: §11.1 (行 1565) / §11.2 (行 1573) / §11.3 (行 1581) / §11.4 (行 1589) /
§11.5 (行 1598) / §11.6 (行 1670) — 6 个子节统一加注。

#### 4.5.1 After (统一加注模式)

每个 §11.x 子节**头部**加一行:

```markdown
> **[0.3.2]** 本节详细说明以下 builtin: ... 完整签名表见 §11.0。
```

例 (§11.1):

```markdown
### 11.1 基本 I/O 函数

> **[0.3.2]** 本节详细说明以下 builtin: `print`, `print_num`, `print_float`。
> 完整签名表见 §11.0。

```cpp
void print_num(int n);        // 打印整数
void print(const char* s);    // 打印字符串
void print_float(double f);   // 打印浮点数
```
```

#### 4.5.2 Rationale

- 各 §11.x 子节是详细说明, §11.0 是签名总表
- 0.3.2 加双向引用, 维护一致性

---

## 5. 跨章节引用更新 (Cross-Reference Updates)

下表列出 spec 全文涉及 "C-style cast" / "函数返回类型" / "builtin" / "签名" 的位置, 以及 0.3.2 修订建议。

| 章节 | 行号 (0.3.1) | 当前引用 | 0.3.2 修订 |
|------|---------------|----------|------------|
| §3 类型系统 | 100+ | 类型 ↔ LLVM IR 映射 | **加** §11.0 类型映射表引用 |
| §3.5 整数 ↔ 指针 | 280+ | 整数 ↔ 指针转换 | **加** §4.8.1 cast 引用 (含 ptrtoint / inttoptr) |
| §3.7 数组 | 380+ | 数组类型 | 不变 |
| §4.1 优先级表 | 477 | (0.3.1 已加 2.5 级) | **加** C-style cast 在 2.5 级 (与 `*` / `&` 同级) |
| §4.2 - §4.5 | 500+ | 二元运算符 | 不变 |
| §4.7 三元 `?:` | 557+ | 三元表达式 | 不变 |
| §4.8 带括号 | 563+ | 括号表达式 | **改** 加 §4.8.1 cast 引用 (见 §4.1) |
| §4.8.1 (新) | (待插入) | — | §4.8.1 完整 (见 §3.1) |
| §4.9 mod | 569+ | mod 操作数 | 不变 |
| §4.10 unmod | 610+ | unmod 操作数 | 不变 |
| §4.13 (0.3.1 新增) | (0.3.1 §4.13) | lvalue / rvalue | **加** §4.8.1 cast 上下文引用 (cast 源是 rvalue, cast 目标是 lvalue) |
| §6.1 函数定义 | 749+ | 函数定义示例 | **改** 加返回类型编译期检查 (见 §4.2) |
| §6.2 函数调用 | 768+ | 函数调用示例 | **改** 加返回类型规则 (见 §4.3) |
| §6.2.1 (新) | (待插入) | — | §6.2.1 完整 (见 §3.2) |
| §6.3 参数传递 | 825+ | 参数传递 | **加** 参数类型匹配 §6.2.1 引用 |
| §6.4 返回值 | (6.4 行号) | 返回值 | **加** §6.2.1 引用 |
| §7.4 malloc | 920+ | `malloc` 返回 `void*` | **加** §4.8.1 cast 引用 + §11.0 签名引用 |
| §7.5 move | 950+ | `move` 函数 | **加** §11.0 签名引用 |
| §10 FFI | 1300+ | extern 函数 | **加** §6.2.1 优先级 3 引用 + §11.0 收录 |
| §11 标题 | 1565+ | 标准库约定 | **改** 加 §11.0 引用 (见 §4.4) |
| §11.0 (新) | (待插入) | — | §11.0 完整 (见 §3.3) |
| §11.1 - §11.6 | 1565-1680 | 各子节 | **改** 加 §11.0 双向引用 (见 §4.5) |
| §12.1 EBNF | 1685+ | 完整 EBNF | **改** 加 `cast_expression` 产生式 (见 §6) |
| §12.3 附录优先级表 | 1924+ | 优先级表附录 | **改** 加 C-style cast 在 2.5 级 |

**注**: 跨章节引用改动 ~15 处。spec 维护者按本表逐项应用。

---

## 6. EBNF 语法更新 (§12.1)

**位置**: `docs/UltraCPP-v0.3.1-spec-zh-CN.md` §12.1 完整 EBNF 语法 (行 1685 起)。

### 6.1 Before (0.3.1 EBNF 现状 — 片段)

```ebnf
// === Expressions ===
expression        ::= assignment_expression

assignment_expression
                   ::= lvalue '=' assignment_expression             // [0.3.1] LHS constraint
                    | lvalue compound_assignment assignment_expression

unary_expression   ::= postfix_expression
                    | ('+' | '-' | '!' | '~' | '*' | '&') unary_expression
                    | 'sizeof' '(' type ')'

primary_expression ::= identifier
                    | literal
                    | '(' expression ')'
                    | 'null'
```

### 6.2 After (0.3.2 EBNF 修订 — 加 cast_expression)

```ebnf
// === Expressions ===
expression        ::= assignment_expression

// [0.3.1] Assignment LHS must be an lvalue (see §4.13.2).
assignment_expression
                   ::= lvalue '=' assignment_expression
                    | lvalue compound_assignment assignment_expression

// [0.3.2] C-style cast (T)expr — see §4.8.1.
cast_expression    ::= '(' type ')' unary_expression                // [0.3.2]
                    | unary_expression

unary_expression   ::= postfix_expression
                    | ('+' | '-' | '!' | '~' | '*' | '&') unary_expression
                    | 'sizeof' '(' type ')'
                    | 'cast' '(' type ',' expression ')'           // [0.3.2] function-style cast (alternative form)

postfix_expression ::= primary_expression
                    | postfix_expression '[' expression ']'
                    | postfix_expression '(' argument_list? ')'
                    | postfix_expression '.' identifier
                    | postfix_expression '->' identifier
                    | postfix_expression '++'
                    | postfix_expression '--'

primary_expression ::= identifier
                    | literal
                    | '(' expression ')'
                    | 'null'
```

### 6.3 同步: §12.3 附录优先级表

§12.3 (行 1924 起) 的运算符优先级表与 §4.1 同步: 显式标注 C-style cast 在 2.5 级。

**Before (0.3.1 现状)**:

```markdown
| 级别 | 运算符 | 描述 |
|------|--------|------|
| 1 | `::` | 作用域解析 |
| 2 | `()` `[]` `.` `->` `++` `--` | 后缀 |
| 2.5 | `*` `&` `+` `-` `!` `~` `mod` `unmod` `++` `--` (前缀) | 一元 *(0.3.1 补)* |
| 3 | `*` `/` `%` | 乘法 |
| ... |
```

**After (0.3.2 修订)**:

```markdown
| 级别 | 运算符 | 描述 |
|------|--------|------|
| 1 | `::` | 作用域解析 |
| 2 | `()` `[]` `.` `->` `++` `--` | 后缀 |
| 2.5 | `*` `&` `+` `-` `!` `~` `mod` `unmod` `++` `--` (前缀) | 一元 *(0.3.1 补)* |
| 2.5 | `(`*type*`)` | **C-style cast** *(0.3.2 补)* |
| 3 | `*` `/` `%` | 乘法 |
| ... |
```

### 6.4 Rationale

- EBNF 当前**无** C-style cast 产生式
- 0.3.2 加 `cast_expression` 规则 (作为 `unary_expression` 的替代路径)
- §12.3 附录表与 §4.1 主表保持一致

---

## 7. Before / After Diff 完整对比

### 7.1 §4.8 带括号 (修订)

| 维度 | 0.3.1 | 0.3.2 |
|------|-------|-------|
| 章节存在 | ✅ §4.8 (简介) | ✅ §4.8 (修订) + §4.8.1 (新增子节) |
| C-style cast 说明 | ❌ 无 | ✅ §4.8 简介加提示 + §4.8.1 完整子节 |
| `(T)expr` 语法 | ❌ 未列出 | ✅ §4.8.1 完整语法 + 语义 + 示例 |
| 整数 ↔ 指针转换 | ❌ | ✅ §4.8.1.1 表 (8 行允许) |
| 函数式 cast `T(expr)` | ❌ | ✅ §4.8.1.2 说明与 C-style 等价 |

### 7.2 §6.1 函数定义 (修订)

| 维度 | 0.3.1 | 0.3.2 |
|------|-------|-------|
| 示例数 | 3 个 | 3 个 (不变) |
| 返回类型说明 | ❌ 仅示例 | ✅ 加返回类型编译期检查 (3 条) |
| return 类型匹配 | ❌ 未说 | ✅ 显式约束 (`ReturnTypeMismatchError`) |
| void 函数 | 示例有 | ✅ 加显式约束 (允许无值 return) |

### 7.3 §6.2 函数调用 (修订)

| 维度 | 0.3.1 | 0.3.2 |
|------|-------|-------|
| 示例数 | 3 个 | 3 个 (不变) |
| 返回类型规则 | ❌ | ✅ 加 3 优先级 (builtin / 用户 / extern) |
| §6.2.1 引用 | ❌ | ✅ 加 (新章节) |
| §11.0 引用 | ❌ | ✅ 加 (新章节) |

### 7.4 §6.2.1 函数返回类型 (新增)

| 维度 | 0.3.1 | 0.3.2 |
|------|-------|-------|
| 章节存在 | ❌ 不存在 | ✅ §6.2.1 全章 |
| 优先级规则 | ❌ | ✅ 3 优先级 (builtin / 用户 / extern) |
| 返回类型传播 | ❌ | ✅ 7 上下文表 (赋值 / 子表达式 / 实参 / 条件 / cast / return) |
| void 函数 | ❌ 未说 | ✅ 显式约束 + 错误类型 |
| codegen 集成 | ❌ | ✅ §6.2.1.4 伪代码 |

### 7.5 §11 标准库约定 (修订)

| 维度 | 0.3.1 | 0.3.2 |
|------|-------|-------|
| §11 标题注 | ❌ | ✅ 加 §11.0 引用 |
| §11.0 总表 | ❌ | ✅ §11.0 (完整 18 个 builtin) |
| §11.1 - §11.6 | 各自独立 | ✅ 加 §11.0 双向引用 |

### 7.6 §11.0 builtin 签名总表 (新增)

| 维度 | 0.3.1 | 0.3.2 |
|------|-------|-------|
| 章节存在 | ❌ 不存在 | ✅ §11.0 全章 |
| builtin 数量 | 0 (无总表) | 18 个 builtin (I/O / 字符串 / 内存 / 工具 / 数学) |
| LLVM IR 类型映射 | ❌ | ✅ §11.0.1 表 (12 类型) |
| codegen 集成 | ❌ | ✅ §11.0.2 伪代码 (`builtin_sigs[]` 数组) |
| 添加新 builtin 流程 | ❌ | ✅ §11.0.3 (4 步) |

### 7.7 §12.1 EBNF (修订)

| 维度 | 0.3.1 | 0.3.2 |
|------|-------|-------|
| `cast_expression` 产生式 | ❌ 无 | ✅ 新增 (2 产生式) |
| `unary_expression` cast 形式 | ❌ 无 | ✅ 加 `'cast' '(' type ',' expression ')'` |
| §12.3 附录表 | 2.5 级一元 op | 2.5 级加 C-style cast 行 |

---

## 8. 0.3.1 → 0.3.2 升级影响分析

### 8.1 测试影响

- **现有 PASS 测试**: 27 baseline PASS 测试预期全保留 (S5 强化类型, 不破坏功能)
- **现有 FAIL 测试**:
  - **m0_42 deref-assign** — `*((int*)malloc(8)) = payload;` 预期从 compile_failed → PASS (S4 + S5 + C1 共同修)
  - **m0_41 extern_c** — `abs_int` 加 §11.0 builtin 后预期从 link_failed → PASS (S5 修)
  - 其他 19 FAIL 保持 (与 0.3.2 无关)
- **单元测试**: 505/505 预期保留 (codegen 改动局部)
- **e2e sample**: 7 sample (`m0_01`/`04`/`09`/`13`/`19`/`22`/`47`) 预期保留

**预期 baseline**: 27 → **29/48 PASS** (m0_42 + m0_41 翻 PASS)。

### 8.2 API 兼容

- ✅ **无变化** (0.3.2 是内部 codegen + spec 概念补全, 不暴露公共 API)

### 8.3 ABI 兼容

- ✅ **无变化** (0.3.1 生成的 LLVM IR 与 0.3.2 生成的 IR 在功能上等价; m0_42 + m0_41 修复只是 bug, 不是 ABI 变化)

### 8.4 运行时行为

- ✅ **无 breaking change** (只是 bug 修复 + 概念术语加入 + 1 个新语法 `(T)expr`)
- ✅ 现有合法 UltraCPP 程序预期**全部**保持编译/运行行为不变
- ✅ 新语法 `(T)expr` 是**新增**, 不影响现有 `T(expr)` 写法

### 8.5 编译器实现

- **codegen.c**: ~40-50 LOC (UC_EXPR_CALL 用 §11.0 查表 + UC_EXPR_CAST case 新增)
- **parser.c**: ~15-20 LOC (新增 `cast_expression` 解析 + `(T)expr` 分支)
- **ast.c / ast.h**: ~5 LOC (UC_EXPR_CAST 字段扩展, 若需要)
- **测试基础设施**: 无变化

### 8.6 spec 本身

- **新章节**: 3 个 (§4.8.1 + §6.2.1 + §11.0)
- **修订章节**: 5 个 (§4.8 + §6.1 + §6.2 + §11 + §12.1 + §12.3 = 实际 6 个)
- **总改动**: ~250 行 spec 文本新增/修订

---

## 9. Spec 0.3.2 Release Notes (正式版)

> 目标文件: `docs/UltraCPP-v0.3.2-release-notes.md` (0.3.2 发布时新建)

```markdown
# UltraCPP 0.3.2 Release Notes

**Release date**: 2026-08-XX (待定)
**Source spec**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md`
**Base spec**: `docs/UltraCPP-v0.3.1-spec-zh-CN.md`

## Spec 修订

### 修复 (bug fixes)

- **Bug C1**: `UC_EXPR_CALL` 不设 `last_expr_type` — codegen 改 1-2 行, 调用表达式结果类型正确传播
- **Bug C2**: `UC_EXPR_CALL` 返回类型硬编码 i32 — 由 §11.0 builtin 签名表修, codegen 查表得正确返回类型
- **Bug C3**: m0_42 deref-assign LHS — S2 (0.3.1) 部分修 + S4 (0.3.2) C-style cast 完整修

### 新增章节

- **§4.8.1 C-style 显式类型转换 `(T)expr`** (5 节, 完整语法 + 语义 + 类型映射表 + 函数式 cast 等价 + 示例 + 编译期检查 + 交叉引用)
  - §4.8.1.1 语义 (8 行类型映射表)
  - §4.8.1.2 与 `T(expr)` 函数式 cast 等价
  - §4.8.1.3 示例 (整数 ↔ 指针 / 宽度转换 / 类型断言 / FFI)
  - §4.8.1.4 编译期检查
  - §4.8.1.5 交叉引用
- **§6.2.1 函数返回类型** (5 节, 完整优先级规则 + 传播 + void 函数 + codegen + 交叉引用)
  - §6.2.1.1 返回类型确定规则 (3 优先级: builtin / 用户 / extern)
  - §6.2.1.2 返回类型传播 (7 上下文表)
  - §6.2.1.3 void 函数调用
  - §6.2.1.4 codegen 集成
  - §6.2.1.5 交叉引用
- **§11.0 builtin 签名总表** (4 节, 18 个 builtin + LLVM IR 类型映射 + codegen 集成 + 添加流程)
  - §11.0.1 完整 builtin 签名表 (18 行)
  - §11.0.2 codegen 集成 (`builtin_sigs[]` 数组)
  - §11.0.3 添加新 builtin 流程
  - §11.0.4 交叉引用

### 修改章节

- §4.8 带括号表达式 — 加 §4.8.1 C-style cast 提示
- §6.1 函数定义 — 加返回类型编译期检查 (3 条)
- §6.2 函数调用 — 加 §6.2.1 返回类型规则 + §11.0 引用
- §11 标准库约定 — 加 §11.0 总表引用 + §11.0 整体新章节
- §11.1 - §11.6 各子节 — 加 §11.0 双向引用
- §12.1 完整 EBNF — 加 `cast_expression` 产生式
- §12.3 附录优先级表 — 加 C-style cast 在 2.5 级

### 跨章节引用更新

~15 处 (见本文档 §5 表): §3 类型映射 / §3.5 整数↔指针 / §4.1 优先级 / §4.13 lvalue/rvalue / §6.3 参数 / §6.4 返回值 / §7.4 malloc / §7.5 move / §10 FFI / §11.x 子节 / §12.1 EBNF

### 不变

- 所有 27 个 P0/P1 baseline PASS 测试预期保留
- 单元测试 505/505 预期保留
- m0_19 / m0_29 / m0_31 (指针 / 递归) 受益, 仍 PASS (类型更准确)
- m0_42 (`*view = payload`) 从 compile_failed → PASS (S4 + S5 + C1 共同修)
- m0_41 (extern "C" abs_int) 从 link_failed → PASS (S5 收录 builtin)
- API 兼容、ABI 不变
- 现有 `T(expr)` 函数式 cast 仍合法 (与 `(T)expr` 等价)
```

---

## 10. Spec 维护 Checklist (供 spec maintainer 用)

> 以下是 spec 维护者按本草案应用 spec 修订时的 checklist。**plan 阶段不执行**, 留给 spec maintainer。

- [ ] **Step 1**: 复制 0.3.1 spec 文件到 0.3.2 working copy
  - `cp docs/UltraCPP-v0.3.1-spec-zh-CN.md docs/UltraCPP-v0.3.2-spec-zh-CN.md`
- [ ] **Step 2**: 在 §4.8 之后、§4.9 之前插入 §4.8.1 完整内容 (见本文档 §3.1.2)
  - [ ] §4.8.1.1 语义 (8 行类型映射表)
  - [ ] §4.8.1.2 函数式 cast 等价说明
  - [ ] §4.8.1.3 示例 (4 类)
  - [ ] §4.8.1.4 编译期检查
  - [ ] §4.8.1.5 交叉引用
- [ ] **Step 3**: 修改 §4.8 简介 (见本文档 §4.1.2) — 加 C-style cast 提示
- [ ] **Step 4**: 在 §6.2 之后、§6.3 之前插入 §6.2.1 完整内容 (见本文档 §3.2.2)
  - [ ] §6.2.1.1 返回类型确定规则 (3 优先级)
  - [ ] §6.2.1.2 返回类型传播 (7 上下文)
  - [ ] §6.2.1.3 void 函数调用
  - [ ] §6.2.1.4 codegen 集成
  - [ ] §6.2.1.5 交叉引用
- [ ] **Step 5**: 修改 §6.1 函数定义 (见本文档 §4.2.2) — 加返回类型编译期检查
- [ ] **Step 6**: 修改 §6.2 函数调用 (见本文档 §4.3.2) — 加 §6.2.1 引用
- [ ] **Step 7**: 在 §11 标题后、§11.1 前插入 §11.0 完整内容 (见本文档 §3.3.2)
  - [ ] §11.0.1 builtin 签名表 (18 行)
  - [ ] §11.0.2 codegen 集成
  - [ ] §11.0.3 添加新 builtin 流程
  - [ ] §11.0.4 交叉引用
- [ ] **Step 8**: 修改 §11 标题 (见本文档 §4.4.2) — 加 §11.0 引用
- [ ] **Step 9**: 修改 §11.1 - §11.6 (见本文档 §4.5.1) — 各子节加 §11.0 引用
- [ ] **Step 10**: 修改 §12.1 EBNF (见本文档 §6.2) — 加 `cast_expression` 产生式
- [ ] **Step 11**: 修改 §12.3 附录优先级表 (见本文档 §6.3) — 加 C-style cast
- [ ] **Step 12**: 跨章节引用更新 (见本文档 §5) — §3 / §3.5 / §4.1 / §4.13 / §6.3 / §6.4 / §7.4 / §7.5 / §10 加新引用
- [ ] **Step 13**: §0 / §版本历史 加 0.3.2 changelog 条目
- [ ] **Step 14**: 全文 search "C-style cast" / "(T)expr" / "cast_expression" 核对引用完整
- [ ] **Step 15**: 全文 search "返回类型" / "builtin" / "签名" 核对引用一致
- [ ] **Step 16**: markdownlint / 拼写检查
- [ ] **Step 17**: 章节编号核对 (§4.8.1 / §6.2.1 / §11.0 不与现有编号冲突)
- [ ] **Step 18**: 内部 review (spec 维护者 + implementer 互审)
- [ ] **Step 19**: commit 到 spec 0.3.2 release

---

## 11. Spec 0.3.1 → 0.3.2 兼容矩阵

| spec 0.3.1 写法 | 0.3.2 行为 | 兼容性 | 备注 |
|----------------|-----------|--------|------|
| `T(expr)` 函数式 cast | 同 0.3.1 (合法) | ✅ | §4.8.1.2 与 C-style 等价 |
| `(T)expr` C-style cast | 0.3.1 无, 0.3.2 新增合法 | ✅ 新 | §4.8.1 |
| `(int)malloc(8)` | 0.3.2 新增合法 | ✅ 新 | §4.8.1.3 示例 (m0_42 关键) |
| `*p = v` (p 是 T*) | 同 0.3.1 (S2 已修) | ✅ | S2 0.3.1 |
| `*((int*)malloc(8)) = payload;` | 0.3.2 从 compile_failed → PASS | ✅ 新 | m0_42 修复 |
| `int x = factorial(5);` | 同 0.3.1 (合法, 类型更准) | ✅ | §6.2.1 + §11.0 |
| `abs_int(-5)` (extern) | 0.3.2 从 link_failed → PASS | ✅ 新 | m0_41 修复 (§11.0 收录) |
| `void f() { print("hi"); }` | 同 0.3.1 (合法) | ✅ | §6.1 修订 + §6.2.1.3 |
| `int f() { return; }` | 0.3.2 编译错 (更明确) | ✅ | §6.1 编译期检查 |
| `int f() { print("hi"); }` (无 return) | 0.3.2 编译错 (更明确) | ✅ | §6.1 |
| 函数调用结果作 cast 源 `(long)factorial(5)` | 0.3.2 合法 | ✅ 新 | §4.8.1 + §6.2.1 |
| 函数调用结果作赋值 LHS `f() = x;` | 仍编译错 | ✅ | §4.13 (0.3.1) |
| void 函数调用作赋值 LHS `print("hi") = x;` | 仍编译错 (更明确) | ✅ | §4.13 + §6.2.1.3 |
| `int x = print("hi");` | 0.3.2 编译错 (`VoidUsedAsValueError`) | ✅ 新错 | §6.2.1.3 |
| builtin `malloc` 调用 | 同 0.3.1 (合法) | ✅ | §11.0 收录 |
| builtin `abs_int` 调用 | 0.3.2 合法 (无需 extern 声明) | ✅ 新 | §11.0 收录 |
| builtin `strlen` 调用 | 同 0.3.1 (合法, 类型更准) | ✅ | §11.0 收录 |
| 用户函数 `add(10, 20)` | 同 0.3.1 (合法) | ✅ | §6.2.1 优先级 2 |
| extern 函数 `abs_int(-5)` | 0.3.2 合法 (签名表提供类型) | ✅ 新 | §6.2.1 优先级 3 |
| EBNF `unary_expression` | 加 `'cast' '(' type ',' expression ')'` | ✅ 新 | §12.1 |
| EBNF 现有产生式 | 不变 | ✅ | §12.1 |

**总兼容**: ✅ **完全向后兼容**。新语法 `(T)expr` 是**纯新增**, 不影响现有 `T(expr)` 写法。Bug 修复 (m0_42 + m0_41) 是**正向**, 不破现有行为。

---

## 12. 与 S2 (0.3.1) 的关系

| 维度 | S2 (0.3.1) | 0.3.2 (本文档) |
|------|-----------|---------------|
| **范围** | lvalue / rvalue 概念 | S4 + S5 + bug fixes |
| **新增章节** | §4.13 (5 节) | §4.8.1 + §6.2.1 + §11.0 (共 14 节) |
| **修订章节** | §4.1 + §4.6 + §7.8 + §12.1 + §12.3 (5 个) | §4.8 + §6.1 + §6.2 + §11 + §11.1-11.6 + §12.1 + §12.3 (10 个) |
| **触发** | m0_42 部分修复 (lvalue 路径) | m0_42 完整修复 (cast) + m0_41 修复 (签名表) |
| **代码改动** | codegen.c ~30-40 LOC (lvalue 路径) | codegen.c ~40-50 LOC + parser.c ~15-20 LOC |
| **测试影响** | 27 → 28 PASS (m0_42 部分) | 28 → 29 PASS (m0_42 完整 + m0_41) |

### 12.1 0.3.1 → 0.3.2 升级路径

```
0.3.0 spec
   ↓ (0.3.1 = S2 only)
0.3.1 spec (新增 §4.13, 修订 §4.1/§4.6/§7.8/§12.1/§12.3)
   ↓ (0.3.2 = S4 + S5 + bug fixes)
0.3.2 spec (新增 §4.8.1/§6.2.1/§11.0, 修订 §4.8/§6.1/§6.2/§11/§11.1-11.6/§12.1/§12.3)
```

### 12.2 0.3.1 与 0.3.2 是独立版本, 不是修订关系

- **0.3.1** 仍是**完整** spec 版本, 自身**已发布**
- **0.3.2** 在 0.3.1 基础上**独立发布**新版本, **不修订** 0.3.1
- 0.3.2 spec 文件名 `UltraCPP-v0.3.2-spec-zh-CN.md`, 与 0.3.1 平级
- 0.3.1 spec 文件保持**不可变** (与 0.3.0 关系一致)

---

## 13. 实施依赖顺序 (spec → code → test)

```
1. spec 修订     ← 本文档第 3-6 章 (spec 维护者执行)
2. code 修订     ← 0.3.2 implementation plan 第 5 章 (implementer 执行)
3. 验证          ← 0.3.2 implementation plan 第 7 章 (verifier 执行)
4. release       ← 本文档第 9 章 (0.3.2 tag + 发布)
```

| 阶段 | 工作 | 执行者 | 输出 |
|------|------|--------|------|
| 1. spec 修订 | 加 §4.8.1 + §6.2.1 + §11.0 + 改 §4.8/§6.1/§6.2/§11/§12.1/§12.3 | spec 维护者 | `docs/UltraCPP-v0.3.2-spec-zh-CN.md` |
| 2. code 修订 | parser C-style cast + codegen UC_EXPR_CAST + UC_EXPR_CALL 查表 | implementer | `src-c/src/parser.c` + `src-c/src/codegen.c` + `src-c/src/ast.c` 改动 |
| 3. 验证 | 27 baseline + m0_42 + m0_41 + 505 单元 + 7 e2e | verifier | 全部 PASS (29/48 baseline) |
| 4. release | 0.3.2 tag + 发布 notes | releaser | git tag 0.3.2 + release notes |

**顺序要求**: spec 必须先于 code (spec 是 code 的依据)。

---

## 14. 与 S1, S3, S6-S9 后续 spec gap 的关系

0.3.2 落地 S4 + S5 + bug fixes。其他 7 个 spec gap 仍存在, 标记 **deferred**:

| Gap | 标题 | 推到版本 | 备注 |
|-----|------|---------|------|
| S1 | 一元 op 优先级完整化 | **部分由 0.3.2 解决** + 0.3.3+ 剩余 | 0.3.1 已加 2.5 级, 0.3.2 加 C-style cast |
| S2 | lvalue / rvalue 概念明确化 | **0.3.1** (已落地) | ✅ 见 `.dev/drafts/0.3.1-spec-text-changes.md` |
| S3 | 一元 op 语义完整化 | **部分由 0.3.1 + 0.3.2 解决** + 0.3.3+ 剩余 | `*` / `&` / `++` / `--` 已 covered, cast 加 |
| S4 | cast 显式化 | **0.3.2** (本文档) | ✅ 本次修复 |
| S5 | 函数返回类型推断 | **0.3.2** (本文档) | ✅ 本次修复 |
| S6 | FFI body 来源策略 | 0.3.3+ / 0.4.0 | 重大议题, 涉及 `extern "C" { }` body 来源 |
| S7 | CJK 真支持 (标识符) | 0.4.0 | 需 spec 重大修订 (lexer + parser) |
| S8 | 隐式 mod 规则全面化 | **部分由 0.3.1 解决** + 0.3.3+ 剩余 | S2 解决 "隐式 mod 目标是 lvalue" |
| S9 | spec 风格修订 (markdownlint / 一致性) | 0.3.3 | 风格统一, 不影响语义 |

**总判断**: 0.3.2 落地后, **27 → 29/48 baseline PASS** (m0_42 + m0_41 翻)。剩余 19 FAIL 由其他 gap 处理, **与 0.3.2 无关**。

(详细讨论排队中, 等本次 0.3.2 落地后, 在 `.dev/drafts/0.3.0-m0-priority.md` §9.x 继续跟踪。)

---

## 15. Spec 文档清单 (用户用)

> 0.3.2 发布后, 用户需更新/新增以下文档:

| 文档 | 操作 | 路径 |
|------|------|------|
| `UltraCPP-v0.3.2-spec-zh-CN.md` | **新建** (基于 0.3.1 spec 应用本文档 §3-6 修订) | `docs/UltraCPP-v0.3.2-spec-zh-CN.md` |
| `UltraCPP-v0.3.2-release-notes.md` | **新建** (基于本文档 §9) | `docs/UltraCPP-v0.3.2-release-notes.md` |
| `UltraCPP-v0.3.1-spec-zh-CN.md` | **不变** (0.3.1 已发布, 保持不可变) | `docs/UltraCPP-v0.3.1-spec-zh-CN.md` |
| `UltraCPP-v0.3.0-spec-zh-CN.md` | **不变** | `docs/UltraCPP-v0.3.0-spec-zh-CN.md` |
| `README.md` | **更新** (版本号 0.3.1 → 0.3.2, changelog 加 S4/S5/C1/C2/C3) | `README.md` |
| `CHANGELOG.md` | **更新** (0.3.2 条目) | `CHANGELOG.md` (若存在) |

---

## 附录 A: 与 0.3.2 Implementation Plan 文档的章节对照表

> 方便 spec 维护者与 implementer 互相对照

| 0.3.2 spec modifications (本文档) | 0.3.2 implementation plan | 内容 |
|-----------------------------------|---------------------------|------|
| §1 元信息 | §1 元信息 | 元数据 |
| §2 概述 | §2 Executive Summary | 0.3.2 范围/影响 |
| §3 新增章节 | §5 实施 checklist (Phase 1) | spec 文本新增 (3 章节) |
| §4 修改章节 | §5 实施 checklist (Phase 1) | spec 文本修订 (6 章节) |
| §5 跨章节引用更新 | (impl plan 独有 — code 视角) | spec 视角 |
| §6 EBNF 更新 | §5 Phase 6 (parser C-style cast) | cast_expression 产生式 |
| §7 Before/After Diff | (spec modifications 独有) | 7 维度对比 |
| §8 升级影响 | §7 测试重新评估 | baseline / API / ABI / runtime |
| §9 Release Notes | §9 0.3.2 Release Notes 草稿 | 正式 release notes 草稿 |
| §10 Spec 维护 Checklist | §11 实施 Checklist (给 implementer) | 各自 checklist |
| §11 兼容矩阵 | §7 测试重新评估 (子集) | 兼容写法表 |
| §12 与 S2 关系 | §12 与其他 spec gap 关系 | 版本关系 |
| §13 实施依赖顺序 | §8 实施时间线 | spec → code → test → release |
| §14 与 S1/S3/S6-S9 关系 | §13 与其他 spec gap 关系 | 其他 gap 推到版本 |
| §15 Spec 文档清单 | (spec modifications 独有) | 用户用文档清单 |

---

## 附录 B: 文档引用清单

**本文档引用的所有文件路径**:

| 类型 | 绝对路径 |
|------|----------|
| 基础 spec (0.3.0) | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.0-spec-zh-CN.md` |
| 基础 spec (0.3.1, 待创建) | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.1-spec-zh-CN.md` |
| 0.3.1 S2 plan 文档 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.1-spec-amendment-s2-lvalue.md` |
| 0.3.1 S2 text changes | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.1-spec-text-changes.md` |
| 0.3.2 implementation plan | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.2-implementation-plan.md` |
| 优先级文档 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.0-m0-priority.md` |
| m0_42 测试源 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/test/programs/baseline/m0_42_unsafe_block.uc` |
| m0_41 测试源 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/test/programs/baseline/m0_41_extern_c.uc` |
| 0.3.1 关联 commit | (待 0.3.1 milestone commit) |
| 0.3.2 关联 commit | (待 0.3.2 milestone commit) |
| Output spec 文件 (待创建) | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.2-spec-zh-CN.md` |
| Output release notes (待创建) | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.2-release-notes.md` |

---

**草案结束。** 本文档**不修改任何 spec / 源码文件**, 仅作为 `orch-plan-writer` 阶段产出的 spec 文本变更记录。后续 `orch-implementer` 按 0.3.2 implementation plan §11 checklist 推进 code 修订, spec 维护者按本文档 §10 checklist 推进 spec 修订。