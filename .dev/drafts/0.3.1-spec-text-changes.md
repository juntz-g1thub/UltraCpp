# UltraCPP 0.3.1 — Spec Text Changes (草案)

**Date**: 2026-08-12
**Status**: draft · 待 spec 维护者审
**Owner**: orch-plan-writer
**基础 spec**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.0-spec-zh-CN.md` (2214 行,2026-08-12)
**关联 commit**: `17b7467` (P0 + P1 milestone, baseline 27/48 PASS)
**关联 plan 文档**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.1-spec-amendment-s2-lvalue.md` (实施计划 — 给 implementer 用)
**关联优先级文档**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.0-m0-priority.md` §9.x S2 row
**Estimated effort**: S (spec 维护者 1-2h)

---

## 1. 元信息 (Metadata)

| 字段 | 值 |
|------|-----|
| 文档标题 | UltraCPP Spec 0.3.1 — Text Changes |
| 文档路径 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.1-spec-text-changes.md` |
| 基础 spec | `docs/UltraCPP-v0.3.0-spec-zh-CN.md` (2214 行) |
| 0.3.1 范围 | §4.6 + §4.1 + §7.8 修订 + §4.13 新增 |
| 0.3.1 触发 | m0_42 deref-assign bug 修复 + spec 概念清晰化 |
| 状态 | draft,待 spec 维护者审 |
| 关联 commit | `17b7467` (P0 + P1 milestone) |
| 关联 plan | `0.3.1-spec-amendment-s2-lvalue.md` (实施计划) |
| Output spec 文件 | (待创建) `docs/UltraCPP-v0.3.1-spec-zh-CN.md` |

---

## 2. 概述 (Overview)

### 2.1 0.3.1 修订范围

0.3.1 (minor) 仅修复 spec §4.6 + §4.1 + §7.8 的概念缺失,并新增 §4.13。**不引入新语法,不修改现有语义**。

| 类型 | 章节 | 性质 |
|------|------|------|
| 新增 | §4.13 表达式分类:lvalue / rvalue | 概念术语加入 |
| 修订 | §4.1 运算符优先级表 | 补 2.5 级一元 op 优先级 |
| 修订 | §4.6 赋值运算符 | 11 行统一加 "LHS 必须是 lvalue" 约束 |
| 修订 | §7.8 引用 `&T` | 引用 §4.13 (交叉引用) |
| 修订 | §12.1 EBNF | 加 lvalue / rvalue 规则 |

合计:**1 个新章节 + 4 个章节修订**。

### 2.2 0.3.0 → 0.3.1 升级影响

- **向后兼容**: ✅ 是(无 syntax 变化)
- **API 兼容**: ✅ 无变化
- **ABI 兼容**: ✅ 无变化
- **运行时行为**: Bug fix(m0_42 翻),非 breaking change
- **测试影响**: 27/48 baseline PASS 测试预期全保留 + m0_42 从 FAIL → PASS

### 2.3 触发原因

1. **m0_42 deref-assign 编译失败** — `*view = payload` 当前 codegen 走 rvalue 路径,触发类型错。S2 修复(`gen_expr` 加 `in_lvalue_ctx` 参数)。
2. **spec 概念清晰化** — spec 全文用 "左值" / "右值" 但无 anchor 定义,§7.8 第 1 条 "操作数必须是左值" 与 §4.9 "T& 的左值" 两处用法散落无章节归宿。

---

## 3. 新增章节 (New Chapter)

### 3.1 新增 §4.13 (lvalue / rvalue 概念)

**插入位置**: `docs/UltraCPP-v0.3.0-spec-zh-CN.md` §4.10 之后(行 635 末)、§5 (行 637) 之前。

> **注意**: 当前 0.3.0 spec §4 章只有 §4.1 ~ §4.10,无 §4.11 / §4.12。0.3.1 直接加 §4.13 作为 §4 章的最后一节。

#### 3.1.1 Before (0.3.0 现状 — §4.10 与 §5 之间无内容)

```
### 4.10 `unmod()` 表达式 — 释放修改权 *(0.3.0 新增：Rule 24)*

... (行 610 ~ 635,unmod 完整定义) ...

## 5. 语句
```

#### 3.1.2 After (0.3.1 新增内容 — 插入位置)

```markdown
### 4.13 表达式分类:lvalue 与 rvalue *(0.3.1 新增)*

UltraCPP 中每个表达式属于以下两类之一:**lvalue**(左值)或 **rvalue**(右值)。这一分类是
赋值、地址运算、`mod()` / `unmod()` 申请、引用绑定等核心语义的判定基础。

#### 4.13.1 定义 *(0.3.1 新增)*

**lvalue**(左值)— 标识一个对象,具有持久身份:

- 有 identity(占据明确的存储位置)
- 可作为赋值 `=` 的左侧
- 可被取地址 `&` 运算
- 在 codegen 层:走 **lvalue 路径** — emit 取地址指令(`getelementptr`、`alloca`
  引用等),**不**自动 `load` 值

**rvalue**(右值)— 表示一个临时值,无持久身份:

- 没有 identity(临时计算结果)
- **不**可作为赋值 `=` 的左侧
- **不**可被取地址
- 在 codegen 层:走 **rvalue 路径** — emit 求值指令(`load`、立即数、算术运算结果等)

#### 4.13.2 哪些表达式是 lvalue *(0.3.1 新增)*

| 表达式形式 | 类别 | 原因 |
|-----------|------|------|
| 变量名 `x` | lvalue | 标识声明对象 |
| 解引用 `*p`(p 是 `T*` 指针类型) | lvalue | 标识 p 指向的对象 |
| 字段访问 `s.field` | lvalue | 标识结构体中的成员 |
| 索引 `a[i]` | lvalue | 标识数组中的元素 |
| 引用 `T& r` 的使用 `r` | lvalue | 引用是对象的别名 |
| 前缀自增 `++x` | lvalue | 修改后的对象 |
| 前缀自减 `--x` | lvalue | 修改后的对象 |
| 赋值表达式 `x = v` | lvalue | (整个赋值表达式的结果是 LHS) |
| 函数调用 `f()` 的结果 | rvalue | 临时返回值 |
| 字面量 `42`、`"hello"` | rvalue | 立即数 |
| 算术运算 `a + b`、`a * b` | rvalue | 计算结果 |
| 比较运算 `a < b`、`a == b` | rvalue | bool 值 |
| 逻辑运算 `a && b`、`a \|\| b` | rvalue | bool 值 |
| 后缀自增 `x++` | rvalue | 表达式的值是修改**前**的旧值 |
| 后缀自减 `x--` | rvalue | 同上 |
| 取地址 `&x` 的结果 | rvalue | 返回的是指针值(虽然操作 lvalue) |
| 条件运算 `c ? a : b` | rvalue | 计算结果 |

#### 4.13.3 赋值上下文约束 *(0.3.1 新增)*

赋值运算符(`=` 及 `+=` / `-=` / `*=` 等复合赋值)的**左侧**必须为 lvalue。
**非 lvalue 作左侧**是编译期错误(`AssignmentToRvalueError`):

```cpp
42 = x;          // ❌ 字面量是 rvalue
x + 1 = 2;       // ❌ 算术表达式是 rvalue
f() = x;         // ❌ 函数调用结果是 rvalue
x++ = 1;         // ❌ 后缀自增结果是 rvalue

*x = v;          // ✅ 解引用是 lvalue(§4.13.2 表第 2 行)
s.field = v;     // ✅ 字段访问是 lvalue
a[i] = v;        // ✅ 索引是 lvalue
++x = 1;         // ✅ 前缀自增是 lvalue
x = y = z;       // ✅ 右结合,内层 `y = z` 整体是 lvalue 作为外层 LHS
```

复合赋值(`+=` / `-=` 等)的左侧同样必须为 lvalue,语义约束同 `=`。

#### 4.13.4 codegen 实现约束 *(0.3.1 新增)*

对 lvalue 表达式,调用 `gen_expr` 在不同上下文有不同行为。UltraCPP 编译器在 codegen
阶段对每个表达式维护两个上下文信息:

1. **value category**(lvalue / rvalue)— 由本节定义
2. **concrete type** — 具体类型(如 `int`、`int*`、`Point`)

| 调用上下文 | lvalue 表达式 codegen 行为 |
|-----------|--------------------------|
| 赋值 `=` 的 LHS(`UC_EXPR_ASSIGN.target`) | 走 lvalue 路径:emit 取地址(`getelementptr`、`alloca` 引用) |
| 取地址 `&x` 的操作数 | 走 lvalue 路径:emit 栈 / 全局 / 字段地址 |
| 表达式语句、函数实参、`=` 的 RHS、子表达式 | 走 rvalue 路径:emit `load` 或值复制 |

例:

```c
int x = 42;
int* p = &x;
*p = v;           // LHS: lvalue 路径 → emit gep,store v
y = *p;           // RHS: rvalue 路径 → emit load
int z = *p + 1;   // RHS: rvalue 路径 → emit load + add
&x;               // 取地址操作数: lvalue 路径 → emit 栈地址
```

**与 C/C++ 对比**:UltraCPP 的 lvalue / rvalue 二元分类与 C/C++ 一致(参见 K&R §A7.1、
C++17 [basic.lval])。但 UltraCPP **不**区分 C++11 引入的 xvalue(eXpiring value)、
prvalue(pure rvalue)、glvalue(generalized lvalue)三分法——UltraCPP 只分两类,简单清晰。

#### 4.13.5 与其他章节的交叉引用 *(0.3.1 新增)*

| 章节 | 关系 |
|------|------|
| §4.1 优先级表 | 一元 `*` / `&` / `mod` / `unmod` / `++` / `--` 见 §4.1 第 2.5 级 |
| §4.6 赋值运算符 | "LHS 必须是 lvalue" 引用 §4.13.2 分类表 |
| §4.9 `mod()` | 操作数 lvalue 要求引用 §4.13.2 |
| §4.10 `unmod()` | 操作数 lvalue 要求引用 §4.13.2 |
| §7.1 所有权 + 隐式 mod | 隐式 `mod()` 目标是 lvalue,引用 §4.13.2 |
| §7.8 引用 `&T` | "操作数必须是左值" 引用 §4.13.2 |
| §12.1 EBNF | `lvalue` / `rvalue` 规则见 §12.1 *(0.3.1 新增)* |

```

---

## 4. 修改章节 (Modified Chapters)

### 4.1 修改 §4.1 运算符优先级表

**位置**: `docs/UltraCPP-v0.3.0-spec-zh-CN.md` §4.1(行 477 ~ 497)。

#### 4.1.1 Before (0.3.0 现状)

```markdown
### 4.1 运算符优先级和结合性

| 优先级 | 运算符 | 结合性 |
|--------|--------|--------|
| 1 | `::` | 左到右 |
| 2 | `()` `[]` `.` `->` `++` `--` | 左到右 |
| 3 | `*` `/` `%` | 左到右 |
| 4 | `+` `-` | 左到右 |
| 5 | `<<` `>>` | 左到右 |
| 6 | `<` `>` `<=` `>=` | 左到右 |
| 7 | `==` `!=` | 左到右 |
| 8 | `&` | 左到右 |
| 9 | `^` | 左到右 |
| 10 | `\|` | 左到右 |
| 11 | `&&` | 左到右 |
| 12 | `\|\|` | 左到右 |
| 13 | `=` `+=` `-=` `*=` `/=` `%=` `&=` `\|=` `^=` `<<=` `>>=` | 右到左 |
| 14 | `?:` （三元条件） | 右到左 |
| 15 | `move` `clone` | - |

> **[0.3.0]** 表中第 8 级的 `&` 指**二元中缀**按位与。一元前缀的 `&`（引用）、`mod` /
> `unmod`（修改权）属于一元运算符，详见 §4.9–§4.10。
```

#### 4.1.2 After (0.3.1 修订)

```markdown
### 4.1 运算符优先级和结合性 *(0.3.1 修订)*

| 优先级 | 运算符 | 结合性 |
|--------|--------|--------|
| 1 | `::` | 左到右 |
| 2 | `()` `[]` `.` `->` `++` `--` (后缀) | 左到右 |
| 2.5 | **一元 `*` `&` `+` `-` `!` `~` `mod` `unmod` `++` `--` (前缀)** *(0.3.1 补)* | **右到左** |
| 3 | `*` `/` `%` (二元) | 左到右 |
| 4 | `+` `-` (二元) | 左到右 |
| 5 | `<<` `>>` (二元) | 左到右 |
| 6 | `<` `>` `<=` `>=` | 左到右 |
| 7 | `==` `!=` | 左到右 |
| 8 | `&` (二元,按位与) | 左到右 |
| 9 | `^` | 左到右 |
| 10 | `\|` | 左到右 |
| 11 | `&&` | 左到右 |
| 12 | `\|\|` | 左到右 |
| 13 | `=` `+=` `-=` `*=` `/=` `%=` `&=` `\|=` `^=` `<<=` `>>=` | 右到左 |
| 14 | `?:` （三元条件） | 右到左 |
| 15 | `move` `clone` | - |

> **[0.3.0]** 表中第 8 级的 `&` 指**二元中缀**按位与。一元前缀的 `&`（引用）、`mod` /
> `unmod`（修改权）属于一元运算符，详见 §4.9–§4.10。
> **[0.3.1 补]** 一元 `*`（解引用）见 §4.13;二元 `*`（乘法）见 §4.2。
```

#### 4.1.3 Rationale

- 当前 §4.1 表只列二元算术 / 关系 / 逻辑 / 赋值。一元 `*` / `&` / `mod` / `unmod` /
  前缀 `++` / `--` / 一元 `+` / `-` / `!` / `~` 都没显式列出。
- 0.3.1 补 2.5 级(右到左),与 C/C++ 标准对齐。
- 同步 §12.3 附录优先级表也加 2.5 级(见 §6.3)。

---

### 4.2 修改 §4.6 赋值运算符

**位置**: `docs/UltraCPP-v0.3.0-spec-zh-CN.md` §4.6(行 541 ~ 555)。

#### 4.2.1 Before (0.3.0 现状)

```markdown
### 4.6 赋值运算符

| 运算符 | 描述 |
|--------|------|
| `=` | 简单赋值（`T*` 之间触发**隐式 `mod()`**，不转移所有权，见 §7.1） |
| `+=` | 加法赋值 |
| `-=` | 减法赋值 |
| `*=` | 乘法赋值 |
| `/=` | 除法赋值 |
| `%=` | 取模赋值 |
| `&=` | 按位与赋值 |
| `\|=` | 按位或赋值 |
| `^=` | 按位异或赋值 |
| `<<=` | 左移赋值 |
| `>>=` | 右移赋值 |
```

#### 4.2.2 After (0.3.1 修订)

```markdown
### 4.6 赋值运算符 *(0.3.1 修订)*

> **[0.3.1]** 赋值运算符的**左侧**必须为 lvalue(见 §4.13.2)。非 lvalue 作左侧
> 是编译期错误(`AssignmentToRvalueError`)。

| 运算符 | 描述 |
|--------|------|
| `=` | 简单赋值 — LHS 必须是 lvalue (§4.13.2);RHS 求值后赋给 LHS;`T*` 之间触发**隐式 `mod()`**,不转移所有权,见 §7.1 |
| `+=` | 加法赋值 — LHS 必须是 lvalue;`LHS = LHS + RHS` |
| `-=` | 减法赋值 — LHS 必须是 lvalue;`LHS = LHS - RHS` |
| `*=` | 乘法赋值 — LHS 必须是 lvalue;`LHS = LHS * RHS` |
| `/=` | 除法赋值 — LHS 必须是 lvalue;`LHS = LHS / RHS` |
| `%=` | 取模赋值 — LHS 必须是 lvalue;`LHS = LHS % RHS` |
| `&=` | 按位与赋值 — LHS 必须是 lvalue;`LHS = LHS & RHS` |
| `\|=` | 按位或赋值 — LHS 必须是 lvalue;`LHS = LHS \| RHS` |
| `^=` | 按位异或赋值 — LHS 必须是 lvalue;`LHS = LHS ^ RHS` |
| `<<=` | 左移赋值 — LHS 必须是 lvalue;`LHS = LHS << RHS` |
| `>>=` | 右移赋值 — LHS 必须是 lvalue;`LHS = LHS >> RHS` |

**结合性**:赋值运算符是**右结合**(`a = b = c` 等价于 `a = (b = c)`,内层
`(b = c)` 整体是 lvalue,可作外层 LHS — 见 §4.13.2 表)。

**lvalue 上下文**:赋值 LHS 走 lvalue 路径(§4.13.4),RHS 走 rvalue 路径。`=` 的结果
(整个赋值表达式)是 lvalue,值为赋值后的 LHS。
```

#### 4.2.3 Rationale

- 0.3.0 spec §4.6 表格 11 行只描述运算符含义,**没说 "LHS 必须是 lvalue" 这一基本
  约束**——这是 C/C++ 教科书写明的核心语义,但 spec 当前缺失。
- 0.3.1 补:lvalue 约束(LHS)+ RHS 求值顺序 + 隐式 mod 提示(对 `=`)+ 结合性补充。

---

### 4.3 修改 §7.8 引用 `&T`

**位置**: `docs/UltraCPP-v0.3.0-spec-zh-CN.md` §7.8(行 1009 ~ 1059)。

#### 4.3.1 Before (0.3.0 现状)

```markdown
### 7.8 引用 `&T`（重写 0.2.0 §7.8） *(0.3.0 改写：Rule 22, Rule 24, 删除 T&mut)*

> **[0.3.0 改写]** 0.3.0 删除 `T&mut` 类型。`T&` 是唯一的引用类型，是否可写由 `mod()` +
> `#modlaw` 决定。

**语法**（见 §12.1）：
```
'&' unary_expression
```

`&expr` 创建对 `expr` 所指对象的引用，结果类型为 `T&`。

**创建规则：**

1. 操作数必须是左值。
2. owner 必须处于活跃且已初始化的状态。
3. 引用不能超出 owner 的生命周期（违反时报 DanglingReference，见 §7.9）。
4. `&` 不改变 owner 的所有权状态。
```

#### 4.3.2 After (0.3.1 修订)

```markdown
### 7.8 引用 `&T`（重写 0.2.0 §7.8） *(0.3.0 改写，0.3.1 修订)*

> **[0.3.0 改写]** 0.3.0 删除 `T&mut` 类型。`T&` 是唯一的引用类型，是否可写由 `mod()` +
> `#modlaw` 决定。
> **[0.3.1 修订]** 0.3.1 显式定义 lvalue / rvalue 概念(§4.13),本节"操作数必须是左值"
> 的"左值"引用 §4.13.2 表达式分类表。

**语法**（见 §12.1）：
```
'&' unary_expression
```

`&expr` 创建对 `expr` 所指对象的引用，结果类型为 `T&`。

**创建规则：**

1. 操作数必须是 lvalue(见 §4.13.2 表达式分类表 — 哪些表达式是 lvalue)。
   - ✅ 合法:`&x`(变量)、`&*p`(解引用)、`&s.field`(字段访问)、`&a[i]`(索引)、`&++x`(前缀自增)
   - ❌ 非法:`&42`(字面量)、`&x + 1`(算术结果)、`&f()`(函数调用结果)、`&x++`(后缀自增结果)
2. owner 必须处于活跃且已初始化的状态。
3. 引用不能超出 owner 的生命周期（违反时报 DanglingReference，见 §7.9）。
4. `&` 不改变 owner 的所有权状态。
```

#### 4.3.3 Rationale

- §7.8 第 1 条"操作数必须是左值"中"左值"在 0.3.0 spec 中**无 anchor 定义**。
- 0.3.1 §4.13.2 提供 anchor,§7.8 交叉引用并扩展合法/非法列表,使规则更精确。

---

## 5. 跨章节引用更新 (Cross-Reference Updates)

下表列出 spec 全文涉及 "左值" / "右值" / "lvalue" / "rvalue" 的位置,以及 0.3.1
修订建议(若有)。**仅 spec 修订者需关注,不影响 implementer**。

| 章节 | 行号 | 当前引用 | 0.3.1 修订 |
|------|------|----------|------------|
| §0.1 概述 | 25 | "声明 Q2: 引用 `T& a = b`(右侧是左值);指针 `T* a = &b`(右侧是取地址)" | **不变**(概念性引用,§4.13 已涵盖) |
| §0.2 版本差异 | 38 | "对 §3.3 的引用声明语法**改写**为「`T& a = b`(右侧是左值)」" | **不变**(版本历史) |
| §0.2 表格 | 50 | "Q2 §3.3, §12.1 — 引用声明右侧是左值" | **不变** |
| §3.3 (示例) | 303 | `// ✓ 引用声明:右侧是左值` | **不变**(示例注释) |
| §3.3 | 336 | `int& r = x;          // 引用声明:右侧是左值(不写 &)` | **不变** |
| §3.3 | 339 | `// int& r4 = 42;      // ❌ 错:T& 右侧必须是左值` | **不变** |
| §4.1 优先级表 | 495 (注) | "一元前缀的 `&`(引用)、`mod` / `unmod`(修改权)属于一元运算符,详见 §4.9–§4.10" | **加** "一元 `*` deref 见 §4.13.4 codegen 实现" |
| §4.5 位运算符 | 529 (注) | "[0.3.0] 本表仅描述**二元中缀**形式。一元前缀 `&` 不是位运算符,而是引用(见 §3.3)" | **不变** |
| §4.6 表头 | 542 | 简单赋值描述(只说隐式 mod) | **改** 加 "LHS 必须是 lvalue (见 §4.13.2)" |
| §4.9 mod 操作数 | 578 | "`reference_expression` 必须是对引用类型 `T&` 的左值(即 `int&`、`Point&` 等)" | **改** "必须是对引用类型 `T&` 的 lvalue(见 §4.13.2)" |
| §4.10 unmod 操作数 | (612 附近) | "**操作数**:`reference_expression` 必须是对引用类型 `T&` 的左值" *(实际原文 "一般省略",看上下文 — 见 §4.10 完整)* | **改** "lvalue(见 §4.13.2)" |
| §7.1 所有权 | 856 起 | 隐式 mod + 赋值规则 | **加** "隐式 `mod(p1)` 的目标 `p1` 必须是 lvalue(见 §4.13.2)" |
| §7.8 引用 | 1022 | "1. 操作数必须是左值" | **改** "操作数必须是 lvalue (见 §4.13.2)" |
| §7.10 const T* | 1100 起 | 指针的可 rebind / 不可写 / owning heap 禁止 | **不变**(与 lvalue 正交,见 S2 plan §3.5) |
| §12.1 EBNF | 1685+ | `unary_expression` 含 `&` / `*` / `mod` / `unmod` | **改** 加 `lvalue` / `rvalue` 规则 |
| §12.1.1 | (注记) | "0.3.0 删除 `&mut` 产生式" | **不变** |

**注**: §7.1 当前文本**未**显式说 "隐式 mod 目标是 lvalue",S2 让这一隐含约束
显式化。修订者可在 §7.1 隐式 mod 段落加一行说明。

---

## 6. EBNF 语法更新 (§12.1)

**位置**: `docs/UltraCPP-v0.3.0-spec-zh-CN.md` §12.1 完整 EBNF 语法(行 1685 起)。

### 6.1 Before (0.3.0 EBNF 现状 — 片段)

```ebnf
// === Expressions ===
expression        ::= assignment_expression

assignment_expression
                   ::= conditional_expression
                    | unary_expression '=' assignment_expression
                    | unary_expression compound_assignment assignment_expression

// ... (logical / bitwise / equality / relational / shift / additive / multiplicative)

unary_expression   ::= postfix_expression
                    | ('+' | '-' | '!' | '~' | '*' | '&') unary_expression
                    | 'sizeof' '(' type ')'
                    | 'move' '(' expression ')'
                    | 'clone' '(' expression ')'
                    | 'mod' '(' expression ')'                       // [0.3.0 Rule 24]
                    | 'unmod' '(' expression ')'                     // [0.3.0 Rule 24]
                    | 'move_to_thread' '(' expression ',' expression ')'

postfix_expression ::= primary_expression
                    | postfix_expression '[' expression ']'
                    | postfix_expression '(' argument_list? ')'
                    | postfix_expression '.' identifier
                    | postfix_expression '->' identifier
                    | postfix_expression '++'
                    | postfix_expression '--'

primary_expression ::= identifier
                    | literal
                    | '(' expression ')'
                    | 'null'
```

### 6.2 After (0.3.1 EBNF 修订)

```ebnf
// === Expressions ===
expression        ::= assignment_expression

// [0.3.1] Assignment LHS must be an lvalue (see §4.13.2). The grammar permits
// unary_expression on the LHS; the compiler enforces lvalue category in semantic
// analysis (or codegen for legacy paths).
assignment_expression
                   ::= lvalue '=' assignment_expression             // [0.3.1] LHS constraint
                    | lvalue compound_assignment assignment_expression

// [0.3.1 Rule S2] lvalue / rvalue categorization — see §4.13 for full semantics.
lvalue            ::= identifier                                     // variable
                    | '*' cast_expression                            // unary deref
                    | postfix_expression '[' expression ']'         // index
                    | postfix_expression '.' identifier              // field access
                    | '(' lvalue ')'                                 // parenthesized lvalue
                    | lvalue '++'                                    // prefix increment (§4.13.2 row 6)
                    | lvalue '--'                                    // prefix decrement
                    | assignment_expression                          // result of `x = v` is lvalue

rvalue            ::= literal                                        // integer / float / string / char / bool / null
                    | postfix_expression '(' argument_list? ')'      // function call result
                    | unary_expression '+' unary_expression          // (for completeness)
                    | postfix_expression '++'                        // postfix increment
                    | postfix_expression '--'                        // postfix decrement
                    | '(' rvalue ')'                                 // parenthesized rvalue
                    | '&' unary_expression                           // address-of result (pointer rvalue)
                    | 'move' '(' expression ')'
                    | 'clone' '(' expression ')'
                    | 'mod' '(' expression ')'                       // [0.3.0 Rule 24]
                    | 'unmod' '(' expression ')'                     // [0.3.0 Rule 24]
                    | 'move_to_thread' '(' expression ',' expression ')'

// (rest of expression grammar unchanged)

unary_expression   ::= postfix_expression
                    | ('+' | '-' | '!' | '~' | '*' | '&') unary_expression
                    | 'sizeof' '(' type ')'

postfix_expression ::= primary_expression
                    | postfix_expression '[' expression ']'
                    | postfix_expression '(' argument_list? ')'
                    | postfix_expression '.' identifier
                    | postfix_expression '->' identifier
                    | postfix_expression '++'
                    | postfix_expression '--'

primary_expression ::= identifier
                    | literal
                    | '(' expression ')'
                    | 'null'
```

### 6.3 同步:§12.3 附录优先级表

§12.3(行 1924 起)的运算符优先级表与 §4.1 同步:加 2.5 级一元 op 行(右到左)。

**Before (0.3.0 现状)**:

```markdown
| 级别 | 运算符 | 描述 |
|------|--------|------|
| 1 | `::` | 作用域解析 |
| 2 | `()` `[]` `.` `->` `++` `--` | 后缀 |
| 3 | `*` `/` `%` | 乘法 |
| ... |
```

**After (0.3.1 修订)**:

```markdown
| 级别 | 运算符 | 描述 |
|------|--------|------|
| 1 | `::` | 作用域解析 |
| 2 | `()` `[]` `.` `->` `++` `--` | 后缀 |
| 2.5 | `*` `&` `+` `-` `!` `~` `mod` `unmod` `++` `--` (前缀) | 一元 *(0.3.1 补)* |
| 3 | `*` `/` `%` | 乘法 |
| ... |
```

### 6.4 Rationale

- EBNF 当前**无** `lvalue` / `rvalue` 概念。
- 0.3.1 加 lvalue 规则用于:
  - 在 `assignment_expression` 产生式显式标 "LHS must be lvalue" 语义(虽然语法
    上仍接受 unary_expression,语义层校验)。
- §12.3 附录表与 §4.1 主表保持一致。

---

## 7. Before / After Diff 完整对比

### 7.1 §4.13 (新增)

| 维度 | 0.3.0 | 0.3.1 |
|------|-------|-------|
| 章节存在 | ❌ 不存在 | ✅ 新增(§4.13 全章) |
| lvalue 定义 | ❌ 无 | ✅ §4.13.1(4 特征) |
| rvalue 定义 | ❌ 无 | ✅ §4.13.1(4 特征) |
| lvalue 表达式表 | ❌ 无 | ✅ §4.13.2(完整表) |
| 赋值 LHS 约束 | ❌ 隐含 | ✅ §4.13.3(显式编译期错误) |
| codegen 实现约束 | ❌ 无 | ✅ §4.13.4(双路径) |

### 7.2 §4.1 优先级表 (修订)

| 维度 | 0.3.0 | 0.3.1 |
|------|-------|-------|
| 级别数 | 14 级(1-15 不连续) | 15 级(加 2.5 级) |
| 一元 op 显式列出 | ❌(只在注里) | ✅(2.5 级) |
| 一元 `*` / `&` | ❌ | ✅ |
| 一元 `mod` / `unmod` | ❌ | ✅ |
| 前缀 `++` / `--` | ❌ | ✅ |

### 7.3 §4.6 赋值运算符 (修订)

| 维度 | 0.3.0 | 0.3.1 |
|------|-------|-------|
| 行数 | 11 行 | 11 行(描述扩展) |
| LHS lvalue 约束 | ❌ 缺失 | ✅ 11 行统一加 |
| `=` 隐式 mod | ✅(原) | ✅(保留) + LHS lvalue 提示 |
| 结合性说明 | ❌ | ✅ 加(右结合 + 内层是 lvalue) |
| 头注 | ❌ | ✅ 加(指向 §4.13.2) |

### 7.4 §7.8 引用 (修订)

| 维度 | 0.3.0 | 0.3.1 |
|------|-------|-------|
| 创建规则 1 | "操作数必须是左值" | "操作数必须是 lvalue (见 §4.13.2)" |
| 合法/非法示例 | ❌ 无 | ✅ 加(5 行合法 + 4 行非法) |
| 修订注 | ❌ | ✅ 0.3.1 修订段 |
| 章节头注 | 0.3.0 改写注 | 0.3.0 改写 + 0.3.1 修订双注 |

### 7.5 §12.1 EBNF (修订)

| 维度 | 0.3.0 | 0.3.1 |
|------|-------|-------|
| `lvalue` 规则 | ❌ 无 | ✅ 新增(7 产生式) |
| `rvalue` 规则 | ❌ 无 | ✅ 新增(11 产生式) |
| `assignment_expression` LHS 标记 | 无 lvalue 锚点 | 加 lvalue 引用 + §4.13.2 注 |

---

## 8. 0.3.0 → 0.3.1 升级影响分析

### 8.1 测试影响

- **现有 PASS 测试**: 27 baseline PASS 测试预期全保留(S2 plan §6.1 逐项评估过)
- **现有 FAIL 测试**: m0_42 deref-assign 预期从 compile_failed → PASS(其他 20 FAIL 保持)
- **单元测试**: 505/505 预期保留(codegen.c 改动 ~30-40 LOC 不触及单元测试断言)
- **e2e sample**: 7 sample (`m0_01`/`04`/`09`/`13`/`19`/`22`/`47`)预期保留

### 8.2 API 兼容

- ✅ **无变化**(S2 是内部 codegen 实现细节,不暴露公共 API)

### 8.3 ABI 兼容

- ✅ **无变化**(0.3.0 生成的 LLVM IR 与 0.3.1 生成的 IR 在功能上等价;m0_42 修
  复只是 bug,不是 ABI 变化)

### 8.4 运行时行为

- ✅ **无 breaking change**(只是 bug 修复 + 概念术语加入)
- ✅ 现有合法 UltraCPP 程序预期**全部**保持编译/运行行为不变

### 8.5 编译器实现

- **codegen.c**: ~30-40 LOC(加 `in_lvalue_ctx` 参数 + `UC_EXPR_ASSIGN` LHS 路径)
- **parser.c**: 可选 ~10 LOC(LHS 编译期检查;S2 plan 推荐 skip,留给 codegen 自然错)
- **测试基础设施**: 无变化

### 8.6 spec 本身

- **新章节**: 1 个(§4.13)
- **修订章节**: 4 个(§4.1 / §4.6 / §7.8 / §12.1)
- **总改动**: ~150 行 spec 文本新增/修订

---

## 9. Spec 0.3.1 Release Notes(正式版)

> 目标文件:`docs/UltraCPP-v0.3.1-release-notes.md`(0.3.1 发布时新建)

```markdown
# UltraCPP 0.3.1 Release Notes

**Release date**: 2026-08-XX (待定)
**Source spec**: `docs/UltraCPP-v0.3.1-spec-zh-CN.md`
**Base spec**: `docs/UltraCPP-v0.3.0-spec-zh-CN.md`

## Spec 修订

### 新增章节

- **§4.13 表达式分类:lvalue 与 rvalue** (5 节,完整定义 + 分类表 + 约束 + codegen)
  - §4.13.1 定义(lvalue + rvalue 各自 4 特征)
  - §4.13.2 哪些表达式是 lvalue(完整表)
  - §4.13.3 赋值上下文约束(7 合法 + 4 非法示例)
  - §4.13.4 codegen 实现约束(双路径)
  - §4.13.5 与其他章节的交叉引用

### 修改章节

- §4.1 运算符优先级表 — 加 2.5 级一元 op(右到左)
- §4.6 赋值运算符 — 11 行统一加 "LHS 必须是 lvalue (§4.13.2)" 约束
- §7.8 引用 `&T` — 创建规则 1 引用 §4.13.2,加合法/非法示例
- §12.1 完整 EBNF — 加 `lvalue` / `rvalue` 规则
- §12.3 附录优先级表 — 同步 §4.1 加 2.5 级

### 关联章节交叉引用

- §4.9 mod() 操作数引用 §4.13.2
- §4.10 unmod() 操作数引用 §4.13.2
- §7.1 隐式 mod 目标是 lvalue 引用 §4.13.2
- §7.8 创建规则 1 引用 §4.13.2
- §4.6 表格头注引用 §4.13.2
- §4.1 优先级表注引用 §4.13.4

### 不变

- 所有 27 个 P0/P1 baseline PASS 测试预期保留
- 单元测试 505/505 预期保留
- m0_42 (`*view = payload`) 从 compile_failed → PASS(S2 bug fix)
- API 兼容、ABI 不变
- 语法不变化(只是概念术语加入 + 一个 bug 修复)
```

---

## 10. Spec 维护 Checklist(供 spec maintainer 用)

> 以下是 spec 维护者按本草案应用 spec 修订时的 checklist。**plan 阶段不执行**,
> 留给 spec maintainer。

- [ ] **Step 1**: 复制 0.3.0 spec 文件到 0.3.1 working copy
  - `cp docs/UltraCPP-v0.3.0-spec-zh-CN.md docs/UltraCPP-v0.3.1-spec-zh-CN.md`
- [ ] **Step 2**: 在 §4.10 之后、§5 之前插入 §4.13 完整内容(见本文档 §3.1.2)
- [ ] **Step 3**: 修改 §4.1 优先级表(见本文档 §4.1.2)— 加 2.5 级
- [ ] **Step 4**: 修改 §4.6 赋值运算符(见本文档 §4.2.2)— 11 行加 lvalue 约束 + 头注
- [ ] **Step 5**: 修改 §7.8 引用(见本文档 §4.3.2)— 加修订注 + 合法/非法示例
- [ ] **Step 6**: 修改 §12.1 EBNF(见本文档 §6.2)— 加 lvalue / rvalue 规则
- [ ] **Step 7**: 修改 §12.3 附录优先级表(见本文档 §6.3)— 同步 §4.1
- [ ] **Step 8**: 跨章节引用更新(见本文档 §5)— §4.9 / §4.10 / §7.1 加 §4.13.2 引用
- [ ] **Step 9**: §0 / §版本历史 加 0.3.1 changelog 条目
- [ ] **Step 10**: 全文 search "左值" 出现位置,核对是否需要加 §4.13 引用
- [ ] **Step 11**: 全文 search "右值"(若有)统一加 §4.13 引用
- [ ] **Step 12**: markdownlint / 拼写检查
- [ ] **Step 13**: 章节编号核对(§4.13 不与 §4.11 / §4.12 冲突 — 当前 spec 无 §4.11/12,无冲突)
- [ ] **Step 14**: 内部 review(spec 维护者 + implementer 互审)
- [ ] **Step 15**: commit 到 spec 0.3.1 release

---

## 11. Spec 0.3.0 → 0.3.1 兼容矩阵

| spec 0.3.0 写法 | 0.3.1 行为 | 兼容性 | 备注 |
|----------------|-----------|--------|------|
| `*p = v;` (p 是 T*) | 同 0.3.0(codegen 修复,功能等价) | ✅ | m0_42 bug 修 |
| `42 = x;` | 仍编译错(更明确的 err msg) | ✅ | §4.13.3 显式约束 |
| `x + 1 = 2;` | 仍编译错 | ✅ | §4.13.3 |
| `f() = x;` (函数结果赋值) | 仍编译错 | ✅ | §4.13.2 rvalue |
| `x++ = 1;` (后缀自增) | 仍编译错 | ✅ | §4.13.2 rvalue |
| `++x = 1;` (前缀自增) | ✅ 合法 | ✅ | §4.13.2 lvalue |
| `mod(x)` | 同 0.3.0(合法) | ✅ | §4.9 |
| `mod(42)` | 仍编译错(更明确) | ✅ | §4.13.2 rvalue |
| `mod(*p)` | ✅ 合法 | ✅ | §4.13.2 lvalue(deref) |
| `mod(s.field)` | ✅ 合法 | ✅ | §4.13.2 lvalue |
| `mod(arr[i])` | ✅ 合法 | ✅ | §4.13.2 lvalue |
| `mod(x + 1)` | 仍编译错 | ✅ | §4.13.2 rvalue |
| `&x` | 同 0.3.0(合法) | ✅ | §7.8 |
| `&42` | 仍编译错(更明确) | ✅ | §4.13.2 rvalue |
| `&(x + 1)` | 仍编译错 | ✅ | §4.13.2 rvalue |
| `&f()` | 仍编译错 | ✅ | §4.13.2 rvalue |
| `&*p` | ✅ 合法(取 *p 地址) | ✅ | §4.13.2 |
| `&s.field` | ✅ 合法 | ✅ | §4.13.2 lvalue |
| `&a[i]` | ✅ 合法 | ✅ | §4.13.2 lvalue |
| `int& r = x;` | 同 0.3.0(合法) | ✅ | §7.8 创建规则 |
| `int& r = 42;` | 仍编译错 | ✅ | §7.8 第 1 条 |
| 函数调用 `f()` 作右值 | 同 0.3.0 | ✅ | §4.13.2 rvalue |
| 函数调用 `f()` 作左值 `f() = x;` | 仍编译错 | ✅ | §4.13.2 rvalue |
| 算术运算结果作左值 | 仍编译错 | ✅ | §4.13.2 rvalue |
| 字段访问结果作左值 | ✅ 合法 | ✅ | §4.13.2 lvalue |
| 索引结果作左值 | ✅ 合法 | ✅ | §4.13.2 lvalue |
| `x = y = z;` (右结合) | ✅ 合法 | ✅ | §4.6 结合性 + §4.13.2 |
| 隐式 `mod(p1)` 在赋值 | 同 0.3.0 | ✅ | §7.1 隐式 mod |

**总兼容**: ✅ **完全向后兼容**。无任何现有合法写法行为变化。

---

## 12. 与 S2 Plan 文档的关系

| 维度 | S2 plan 文档 | 本文档 (text changes) |
|------|-------------|----------------------|
| 路径 | `.dev/drafts/0.3.1-spec-amendment-s2-lvalue.md` | `.dev/drafts/0.3.1-spec-text-changes.md` |
| 读者 | implementer | spec 维护者 |
| 关注 | "如何做"(code 改动 / test 评估 / 实施 checklist) | "spec 本身怎么改"(before/after 文本) |
| 内容 | §10 checklist,§6 测试,§5 code 映射 | §3-§7 完整 spec 文本 diff,§10 spec 维护 checklist |
| 性质 | 实施计划 | spec 变更记录 |
| 时间 | implementer 阶段使用 | spec 修订阶段使用 |
| 关系 | **互补** — plan 文档讲"如何做",本文档讲"spec 本身怎么改" |

### 同步 commit 要求

两个文档**必须同步 commit**(本文档 0.3.1 final 后,plan 文档可以 archive):

1. plan 文档在 implementer 完成 0.3.1 代码后,从 "draft" → "done"
2. 本文档在 spec 维护者完成 0.3.1 spec 后,从 "draft" → "done"
3. release notes 在 0.3.1 tag 时引用两个文档

---

## 13. 实施依赖(spec → code → test 顺序)

```
1. spec 修订      ← 本文档第 3-6 章 (spec 维护者执行)
2. code 修订      ← S2 plan 文档第 6 章 (implementer 执行)
3. 验证           ← S2 plan 文档第 7 章 (verifier 执行)
4. release        ← 本文档第 9 章 (0.3.1 tag + 发布)
```

| 阶段 | 工作 | 执行者 | 输出 |
|------|------|--------|------|
| 1. spec 修订 | 加 §4.13 + 改 §4.1/§4.6/§7.8/§12.1/§12.3 | spec 维护者 | `docs/UltraCPP-v0.3.1-spec-zh-CN.md` |
| 2. code 修订 | codegen.c lvalue 路径 | implementer | `src-c/src/codegen.c` 改动 |
| 3. 验证 | 27 baseline + m0_42 + 505 单元 + 7 e2e | verifier | 全部 PASS |
| 4. release | 0.3.1 tag + 发布 notes | releaser | git tag 0.3.1 + release notes |

**顺序要求**: spec 必须先于 code(spec 是 code 的依据)。

---

## 14. 与 S1, S3-S9 后续 spec gap 的关系

S2 是 0.3.1 **唯一**修订。其他 8 个 spec gap 仍存在,标记 **deferred**:

| Gap | 标题 | 推到版本 | 备注 |
|-----|------|---------|------|
| S1 | 优先级表完整化(已部分纳入 S2 2.5 级) | 0.3.2 | S2 修了 2.5 级,剩余完整化推到 0.3.2 |
| S2 | lvalue / rvalue 概念明确化 | **0.3.1 (本文档)** | ✅ 本次修复 |
| S3 | 一元 op 完整化(已在 S2 §4.13.2 处理) | 0.3.2 | S2 涵盖 `*` / `&` / `++` / `--`,剩余 `+` / `-` / `!` / `~` 推到 0.3.2 |
| S4 | cast 显式化 | 未来版本 | 视方案复杂度 |
| S5 | 函数返回类型推断 | 未来版本 | 同上 |
| S6 | FFI body 来源策略 | 未来版本 | 重大议题 |
| S7 | CJK 真支持(标识符) | 0.3.x+ spec | 需 spec 重大修订(lexer + parser) |
| S8 | 隐式 mod 规则全面化 | **部分由 S2 解决** + 0.3.2 剩余 | S2 解决"隐式 mod 目标是 lvalue";剩余推到 0.3.2 |
| S9 | spec 风格修订(markdownlint / 一致性) | 0.3.2 | 风格统一,不影响语义 |

**总判断**: S2 落地后,**27 → 28/48 baseline PASS**(只 m0_42 翻)。剩余 20 FAIL
由其他 gap 处理,**与 S2 无关**。

(详细讨论排队中,等本次 0.3.1 落地后,在 `.dev/drafts/0.3.0-m0-priority.md` §9.x
继续跟踪。)

---

## 附录 A: 与 S2 Plan 文档的章节对照表

> 方便 spec 维护者与 implementer 互相对照

| S2 plan 文档章节 | 本文档对应章节 | 内容 |
|----------------|---------------|------|
| §1 Executive Summary | §2 概述 | 0.3.1 范围/影响 |
| §2 问题描述 | §2.3 触发原因 | m0_42 + spec 概念 |
| §3 现有 spec 章节交互 | (S2 plan 独有) | 5 章节冲突分析 |
| §4.1 新增 §4.13 | **本文档 §3.1** | §4.13 完整内容 |
| §4.2 修改 §4.1 | **本文档 §4.1** | §4.1 before/after |
| §4.3 修改 §4.6 | **本文档 §4.2** | §4.6 before/after |
| §4.4 修改 §7.8 | **本文档 §4.3** | §7.8 before/after |
| §5 Code 修改映射 | (S2 plan 独有) | spec → src-c.c 映射 |
| §6 测试重新评估 | (S2 plan 独有) | 27 PASS 逐项评估 |
| §7 实施时间线 | **本文档 §13** | spec→code→test→release |
| §8 0.3.1 Release Notes | **本文档 §9** | 正式 release notes |
| §9 风险评估 | (S2 plan 独有) | 5 风险 |
| §10 实施 Checklist | **本文档 §10** | spec 维护 checklist |
| §11 参考:与其他章关联 | **本文档 §5** | 跨章节引用 |
| §12 待确认问题 | (S2 plan 独有) | 4 待确认问题 |
| §13 版本号决策记录 | (S2 plan 独有) | 4 决策 |
| (本文档独有) | **§6 EBNF** | §12.1 EBNF 修订 |
| (本文档独有) | **§7 Diff 完整对比** | 5 维度对比 |
| (本文档独有) | **§8 升级影响** | API/ABI/runtime 兼容 |
| (本文档独有) | **§11 兼容矩阵** | 28 写法兼容表 |
| (本文档独有) | **§12 与 S2 plan 关系** | 文档互补说明 |
| (本文档独有) | **§14 其他 gap** | S1/S3-S9 推到版本 |

---

## 附录 B: 文档引用清单

**本文档引用的所有文件路径**:

| 类型 | 绝对路径 |
|------|----------|
| 基础 spec | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.0-spec-zh-CN.md` |
| S2 plan 文档 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.1-spec-amendment-s2-lvalue.md` |
| 优先级文档 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.0-m0-priority.md` |
| m0_42 测试源 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/test/programs/baseline/m0_42_unsafe_block.uc` |
| 关联 commit | `17b7467` (P0 + P1 milestone) |
| Output spec 文件(待创建) | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.1-spec-zh-CN.md` |
| Output release notes(待创建) | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.1-release-notes.md` |

---

**草案结束。** 本文档**不修改任何 spec / 源码文件**,仅作为 `orch-plan-writer`
阶段产出的 spec 文本变更记录。后续 `orch-implementer` 按 S2 plan 文档 §10 checklist
推进 code 修订,spec 维护者按本文档 §10 checklist 推进 spec 修订。