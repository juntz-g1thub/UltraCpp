# 0.3.6 整合实施计划

**Date**: 2026-08-28
**Status**: draft (待实施)
**Owner**: orchestrator + implementer
**Working dir**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification`
**基线 HEAD**: `9b286a0` (0.3.5 实施 + docs sync phase 5/6 + 14g' lib test infra)
**基线状态**: m0 baseline **36/49 PASS / 13 FAIL**; C unit **554/554 PASS**; lib tests 0/5

---

## §1 范围 (Scope)

整合 5 个来源 + 0.3.6 用户决策，形成唯一 canonical 0.3.6 plan：
- **来源 A**: `.dev/drafts/0.3.0-post-0.3.3-plan.md §4.3` (runtime main return + exit code 8-bit)
- **来源 B**: `.dev/drafts/0.3.5-implementation-process.md §11` (14g'/14h/14i deferred + lib/io.uc delete + m0_38 typedef codegen)
- **来源 C**: 14g' 揭露的 2 个未文档化 blockers (lib/print.uc array + lib/alloc.uc namespace)
- **来源 D**: `.dev/drafts/0.3.0-m0-priority.md` (13 FAILs P2/P3 分类 + S-items)
- **来源 E**: `.dev/drafts/0.3.0-runtime-architecture.md` §3.1 "UltraCPP 不接 libc" + §5 BUILTIN_SIGS[] + §10 inline asm 路径
- **用户决策 G1**: `sys::` namespace 解读B（保留 user-facing API + lib 内部绕开 `sys::` 直接 `uc_syscall`）

### A. 主目标 (In scope)

#### A1. m0 baseline 推进
1. **m0_38** (typedef codegen) → PASS — per 0.3.5 process §11.2 (14e lexer 已加，codegen 补完)
2. **m0_02/03/08** (runtime main return) → PASS — per post-0.3.3-plan §4.3
3. **m0_48** (ternary runner regex) → PASS — runner extract_expected_exit 修

#### A2. m0 baseline 扩 array/struct (cross-check 关键 gap)
4. **m0_26/27/28** (array type [N] + init list + GEP) → PASS — P2 提前
5. **m0_32/33** (struct type + field access GEP) → PASS — P2 提前

#### A3. lib 收尾 (per 来源 B + G1 修订)
6. **lib/print.uc rewrite**：移除 `i8 buf[16]` + `i8 out[16]` + 改用 `uc_syscall(SYS_WRITE, ...)` 直接调用（绕开 `sys::write` namespace）
7. **lib/alloc.uc rewrite**：`uc_alloc/uc_free` 用 `uc_syscall(SYS_BRK, ...)` brk 链直接实现（绕开 `sys::malloc/free` namespace gap）
8. **lib/print.uc::uc_print_float** 完整 ftoa (2 位小数) — per 0.3.5 plan §3.1 commit 14h
9. **lib/sys/raw.uc arm64 支持** (`@ifdef(TARGET_ARCH_ARM64)` + `svc #0`) — per 0.3.5 plan §3.1 commit 14i
10. **lib/io.uc DELETE** — per 0.3.5 spec §11.0.2 Note 2

#### A4. lib test 激活 (per 来源 B + 14g' 已建 infra)
11. **test_print_str/num + test_alloc + test_free → PASS** (run_lib_tests.sh 已 9b286a0 commit 存在，16-3 仅激活)
12. **test_print_float 激活** (16-5 ftoa 完成后)

#### A5. 文档同步
13. **HANDOFF.md + AGENTS.md 刷新** (0.3.6 启动 commit 16-0; 顺手完成 0.3.5 commit 15 phase 5+6 收尾)
14. **0.3.6 spec 双语新写** (cp 0.3.5 + header swap + §10.4 加注 1 段："lib 实现可绕过 sys:: 直接 uc_syscall" + 删除 §11.0.2 注 2 lib/io.uc 提及)
15. **0.3.6 release notes + process ledger** (commit 16-13)
16. **milestones.md + m0-priority.md refresh** (baseline 36 → 46/49)
17. **0.3.5 process §11 errata closure** (14g' 标 committed @ 9b286a0)
18. **.gitignore 加 .pi/ 规则** (避免未来 commit 误包含)

### B. 明确不做 (Out of scope)

- ❌ M1-M5 borrow-check 完整实施 (0.4.0+)
- ❌ 13 m0 FAILs 中除 02/03/08/38/48/26/27/28/32/33 之外 (含位运算/字符串/三元等, 0.3.7+)
- ❌ lib/sync.uc (mutex/atomic) (M5)
- ❌ Stage 1 bootstrap (0.4.0+)
- ❌ 1.0 远期目标
- ❌ 修改 0.3.4 spec / 0.3.5 spec (immutable)
- ❌ 创建 lib/uc_runtime.c (永远 NO)
- ❌ 完整 arm64 runtime 验证 (仅加 ifdef 分支)
- ❌ 修改 .pi/ 内容 (pre-existing untracked, 仅 gitignore)

### C. 核心定位

> **0.3.6 = 0.3.5 fixup + 8 m0 flips + lib rewrite (G1 解读B) + lib/io delete + lib tests activation + arm64/print_float 补完 + HANDOFF/AGENTS/spec 同步**
> 
> **末态预期**: m0 baseline **36 → 46/49 (94%)**; C unit **554 → 590-610**; lib tests **0 → 5/5**
> 
> **G1 sys:: 决策** (per 用户): 保留 `sys::` 公共 API (spec §10.4 不变), lib 内部**直接 `uc_syscall`** 绕开 `sys::` namespace (G1 解读B)

---

## §2 决策一览 (Decisions)

| ID | 决策 | 理由 |
|---|---|---|
| **D1** | 0.3.6 唯一 canonical plan = 本文件 `.dev/drafts/0.3.6-implementation-plan.md` | 来源 A + B + C + D + E 分散；G1 决策后必须合并为唯一 source |
| **D2** | lib/print.uc array fix = 完整重写避免 `i8[N]` (不依赖 P2 array parser) | P2 提前需 array parser work；lib 改造最小 churn；保持 0.3.6 scope 聚焦 |
| **D3** | lib/alloc.uc fix = `uc_alloc/uc_free` 用 `uc_syscall(SYS_BRK, ...)` brk 链 (G1) | 最小修复；**绕开** `sys::malloc/free` namespace gap；不需新写 `sys::` wrapper |
| **D4** | m0_38 typedef codegen 完整化纳入 0.3.6 (per 0.3.5 process §11.2) | 14e lexer 已加；M1 完整实施前需 codegen 补完；0.3.6 链是合理窗口 |
| **D5** | HANDOFF.md + AGENTS.md 刷新纳入 0.3.6 启动 commit 16-0 | 当前 HEAD stale (HANDOFF 仍指 fe649be)；不 refresh 0.3.6 dispatch agent 看不到现状 |
| **D6** | lib/print_float STUB 在 14g' 阶段保留；16-5 完整化 + 16-6 激活 | per 0.3.5 plan §5.3 |
| **D7** | lib/io.uc 删除 = 0.3.6 commit 16-4 (per 0.3.5 spec §11.0.2 Note 2) | deprecate 已 0.3.5；0.3.6 commit `git rm` 顺手 |
| **D8** | m0_26/27/28 (array) + m0_32/33 (struct) 提前到 0.3.6 (per cross-check gap 1+2) | 0.3.6 末态目标 46/49 = 94% PASS |
| **D9** | 0.3.5 commit 15 phase 5+6 收尾纳入 16-0 (per cross-check gap 6) | 0.3.6 启动前完成 0.3.5 尾巴 |
| **D10** | 0.3.6 spec 双语新写 (per cross-check gap 5) | cp 0.3.5 + header swap + §10.4 加注 1 段 + 删除 §11.0.2 注 2 lib/io.uc 提及 |
| **D11** | .gitignore 加 .pi/ 规则 (per cross-check gap 9) | 避免未来 commit 误包含 |
| **S1** | `sys::` namespace **保留** (G1 解读B) | user-facing API 概念 (per spec §10.4 文档化稳定); lib 内部不强制走 |
| **S2** | lib/ 内部实现**绕开 `sys::`** 直接 `uc_syscall` (G1 解读B) | 用户核心论点 "库函数内部直接内联汇编即可" 成立; 已在 lib/memory.uc / lib/string.uc / lib/math.uc 验证 |
| **S3** | `lib/sys/sys.uc` 7 个 `sys::*` wrapper **保留** | 用户面向 API 仍需; lib 内部不再走; 隔离 user-facing API 与 lib implementation |
| **S4** | 0.3.6 spec §10.4 加注 1 段: "lib 实现可绕过 sys:: 直接 uc_syscall" | 明确架构决策; 避免未来 contributor 误以为 lib 必须走 sys:: |
| **S5** | `uc_alloc/uc_free` 实现 = brk 链 (`uc_syscall(SYS_BRK, 0, ...)` get current + `uc_syscall(SYS_BRK, old+n, ...)` extend) | 最小 (5-8 行); 不依赖未定义的 `sys::malloc/free` |

---

## §3 Commit 计划 (15 commits, 按依赖顺序)

| # | Commit 标题 | 主要文件 | LOC | 风险 | m0 flip |
|---|---|---|---|---|---|
| **16-0** | `docs(0.3.6): refresh HANDOFF + AGENTS + 0.3.5 phase 5+6 closure` | HANDOFF.md + AGENTS.md + .dev/drafts/0.3.4-process §SUPERSEDED + milestones.md + m0-priority.md + .gitignore | docs 200-300 + .gitignore 1 | 低 | — |
| **16-1a** | `lib(0.3.6): rewrite lib/alloc.uc with uc_syscall+brk (per S5)` | lib/alloc.uc | UltraCPP ~15-25 | 中-低 | — |
| **16-1b** | `lib(0.3.6): rewrite lib/print.uc avoid array + use uc_syscall (per S2)` | lib/print.uc | UltraCPP ~50-80 | 中 | — |
| **16-1c** | `lib(0.3.6): activate test_print_str/num/alloc/free in run_lib_tests.sh` | test/programs/lib_unit/test_*.uc (如需微调) + test/e2e/run_lib_tests.sh (skip 标志移除) | shell + uc ~5-15 | 低 | 间接 +4 lib tests |
| **16-2** | `lib(0.3.6): delete deprecated lib/io.uc (per spec §11.0.2 Note 2)` | lib/io.uc (`git rm`) + test/README.md (如提及) | shell + docs ~5-15 | 低 | — |
| **16-3** | `parser(0.3.6): array type [N] + init list + GEP codegen (m0_26/27/28)` | src-c/include/uc_ast.h + src-c/src/{parser,codegen}.c + test/programs/baseline/m0_26/27/28 (如需 .uc) | C ~100-200 + 单元测试 | 中-高 | **m0_26/27/28 → PASS** (+3 = 39/49) |
| **16-4** | `parser(0.3.6): struct type + field access GEP (m0_32/33)` | src-c/include/uc_ast.h + src-c/src/{parser,codegen}.c + test/programs/baseline/m0_32/33 (如需) | C ~80-150 | 中-高 | **m0_32/33 → PASS** (+2 = 41/49) |
| **16-5** | `lib(0.3.6): implement uc_print_float full ftoa (2 decimal places)` | lib/print.uc (uc_print_float 函数体) | UltraCPP ~30-50 | 中 | — |
| **16-6** | `lib(0.3.6): activate test_print_float in run_lib_tests.sh` | test/e2e/run_lib_tests.sh (1 行 skip flag 移除) | shell ~5 | 低 | 间接 +1 lib test |
| **16-7** | `lib(0.3.6): add lib/sys/raw.uc arm64 support (@ifdef blocks)` | lib/sys/raw.uc | UltraCPP ~50-80 | 中 | — |
| **16-8** | `src-c(0.3.6): complete typedef codegen (per 0.3.5 process §11.2)` | src-c/include/uc_ast.h (如需) + src-c/src/{parser,codegen}.c + src-c/test/test_*.c + test/programs/m1_lexparse/ (新) | C ~80-150 + M1 测试目录 | 中-高 | **m0_38 → PASS** (+1 = 42/49) |
| **16-9a** | `src-c(0.3.6): codegen exit(N) full + .uc trailing comments (m0_02/03/08)` | src-c/src/codegen.c (main exit path) + test/programs/baseline/m0_02/03/08 (.uc trailing `// N` 注释) | C ~50-100 + 3 .uc | 高 | **m0_02/03/08 → PASS** (+3 = 45/49) |
| **16-9b** | `test(0.3.6): fix run_baseline.sh extract_expected_exit regex` | test/e2e/run_baseline.sh | shell ~10 | 低 | — |
| **16-10** | `test(0.3.6): fix m0_48 runner regex (nested ternary)` | test/e2e/run_baseline.sh | shell ~10 | 低 | **m0_48 → PASS** (+1 = 46/49) |
| **16-11** | `spec(0.3.6): bilingual spec new write (cp 0.3.5 + §10.4 注 1 + 删除 §11.0.2 lib/io.uc 提及)` | docs/UltraCPP-v0.3.6-spec-{zh-CN,en}.md (NEW) | docs 300-500 | 低 | — |
| **16-12** | `docs(0.3.6): release notes + process ledger + 0.3.5 errata closure` | docs/UltraCPP-v0.3.6-release-notes.md (NEW) + .dev/drafts/0.3.6-implementation-process.md (NEW) + .dev/drafts/0.3.5-process.md §11 errata 标 closure + .dev/plans/0.3.0-borrow-check-milestones.md + .dev/drafts/0.3.0-m0-priority.md | docs 500-800 | 低 | — |

### m0 baseline 演化

```
36/49 (0.3.5 末态 HEAD 9b286a0)
  + m0_26/27/28 (16-3 array)     = 39/49
  + m0_32/33   (16-4 struct)     = 41/49
  + m0_38      (16-8 typedef)    = 42/49
  + m0_02/03/08 (16-9a main)     = 45/49
  + m0_48      (16-10 runner)    = 46/49
= 46/49 (94% PASS)
```

**注意**：lib tests (16-1c + 16-6) 不计入 m0 baseline 49 个套件, 单独走 `test/e2e/run_lib_tests.sh` (5/5 PASS)。

### C unit 演化

```
554/554 (0.3.5 末态)
  + 16-3 array (~5-10 tests)         = 559-564
  + 16-4 struct (~3-5 tests)         = 562-569
  + 16-8 typedef (~12 tests)        = 574-581
  + 16-9a main return (~5-10 tests)  = 579-591
  + 16-1a alloc brk (~3-5 tests)     = 582-596
= ~580-600 PASS
```

---

## §4 风险登记

| # | 风险 | 等级 | 触发 | 缓解 |
|---|---|---|---|---|
| **R1** | 16-1b lib/print.uc rewrite 需 ~50-80 行 itoa 替代实现（移除 `i8 buf[16]` + `i8 out[16]`, 改用 digit-by-digit `uc_syscall(SYS_WRITE, 1, &c, 1, ...)`）| 中 | 16-1b | 0.3.4 commit 10a 完整 itoa 模板可参考; 替代: 用 `uc_alloc` 分配 buffer + `uc_print_str` 调 (但 16-1a 必须先) |
| **R2** | 16-1a `uc_alloc/uc_free` brk 链 thread-unsafe (per process single-thread) | 低-中 | 16-1a | 0.3.6 不需要 thread-safe; 加 TODO comment; M5 lib/sync.uc 0.4.0+ 处理 |
| **R3** | 16-3 array parser + 16-4 struct parser 都涉及 `uc_ast.h` 枚举扩展 + parser 消费 + codegen GEP emit, 互相影响 | 中-高 | 16-3, 16-4 | (a) 16-3 单独 commit; 16-4 单独 commit; 各自 max_turns=80; (b) 如复杂可再拆 16-3a (type) + 16-3b (init) + 16-3c (GEP) |
| **R4** | 16-8 typedef codegen 涉及 parser 改 + codegen 改 + 新建 m1 测试目录 (per M1 §3.1) | 中-高 | 16-8 | 14e lexer 已加; 参考 14b/14c/14d pattern; max_turns=150 准备 |
| **R5** | 16-9a runtime main return code 涉及 codegen 函数退出流程 + spec §6.5 引用 | 高 | 16-9a | 0.3.4 spec §S9 + §6.5 已定义 exit code 8-bit 限制; post-0.3.3-plan §4.3 列具体步骤; bash `$?` 8-bit 截断对齐 spec; 如复杂可拆 16-9a (codegen) + 16-9b (.uc trailing) |
| **R6** | lib/sys/raw.uc arm64 实现路径未在 Linux x86_64 上验证 | 中 | 16-7 | arm64 实机验证推 0.3.7; x86_64 上用 `@ifdef` 跳过; C unit 测试条件编译 |
| **R7** | 0.3.6 范围膨胀诱惑 (lib/sync / M1-M5 完整 / Stage 1 bootstrap) | 中 | 全程 | D1-D11 + S1-S5 决策 + §1.B 严格执行; scope-creep 推 0.3.7+ |
| **R8** | 15 commits 跨多次 dispatch 风险 (turn limit) | 中-高 | 全程 | per commit 独立 dispatch; FAILED commit 不影响后续; 16-12 docs 是纯 docs 收尾可后置 |
| **R9** | HANDOFF/AGENTS 16-0 + 0.3.5 phase 5+6 收尾 + .gitignore 多个文件改动 | 中 | 16-0 | 单 commit 包含所有 6 个 docs/.gitignore 改动; 保持 §1.A 范围清晰 |
| **R10** | 0.3.6 spec 16-11 双语新写 + header 模板 + 行数对齐 ±5 + §10.4 注 1 + 删除 §11.0.2 注 2 | 中 | 16-11 | 0.3.5 双语模板可参考; AGENTS.md Translation Rules 严格遵守; max_turns=40 |
| **R11** | lib/print.uc 14g' STUB test_print_float 与 16-5 ftoa 完整实现的 test 期望值差异 | 低 | 16-5, 16-6 | 16-5 commit 必含 test_print_float.uc 改; 16-6 同步激活 |

---

## §5 测试策略

### §5.1 末态预期

| 项 | 末态 |
|---|---|
| **m0 baseline** | 36/49 → **46/49 (94%)** |
| **C unit** | 554 → **~580-600** |
| **lib tests** | 0/5 → **5/5** |
| **spec** | 0.3.6 spec 双语新写 (cp 0.3.5 + §10.4 注 1 + §11.0.2 注 2 删除) |
| **lib 状态** | lib/io.uc DELETE; lib/print.uc 完整重写 (no i8[N]); lib/alloc.uc 改用 brk 链; lib/sys/raw.uc + arm64; lib/print_float 完整 ftoa |
| **heads** | `9b286a0` → ~`XXXXXXX` (commit 16-12 末态) |

### §5.2 新增 C unit tests

| 文件 | 增量 commit | 增量 |
|---|---|---|
| `src-c/test/test_parser.c` | 16-3, 16-4, 16-8 | +15 (array parse + struct parse + typedef parse) |
| `src-c/test/test_codegen.c` | 16-3, 16-4, 16-8, 16-9a, 16-1a | +25 (array emit + struct emit + typedef emit + main return + brk chain) |
| `src-c/test/test_ast.c` | 16-3, 16-4, 16-8 | +5 (array/struct/typedef type nodes) |

### §5.3 新增 lib tests

| 文件 | 激活 commit | 状态 |
|---|---|---|
| `test/programs/lib_unit/test_print_str.uc` | 16-1c | 14g' 已建; 16-1b lib/print.uc 重写后 PASS |
| `test/programs/lib_unit/test_print_num.uc` | 16-1c | 14g' 已建; 16-1b 重写后 PASS |
| `test/programs/lib_unit/test_alloc.uc` | 16-1c | 14g' 已建; 16-1a brk 链后 PASS |
| `test/programs/lib_unit/test_free.uc` | 16-1c | 14g' 已建; 16-1a brk 链后 PASS |
| `test/programs/lib_unit/test_print_float.uc` | 16-6 | 14g' STUB; 16-5 ftoa 完整后激活 |

### §5.4 verification 必做命令

```bash
# 1. C unit tests (末态 ~580-600 PASS)
cd src-c && make clean && make && make test

# 2. m0 baseline (末态 46/49 = 3 FAIL)
bash test/e2e/run_baseline.sh 2>&1 | tee /tmp/m0.log

# 3. lib tests (末态 5/5 PASS)
bash test/e2e/run_lib_tests.sh 2>&1 | tee /tmp/lib_tests.log

# 4. banned-word grep (per AGENTS.md Translation Rules)
git grep -nE '\bauthoritative\b|\bcompanion\b|\b配套译本\b|\b翻译滞后\b|\b权威\b|\b配套\b'

# 5. CJK NO-OP 锚点 (per 0.3.5 spec §2.4)
git grep -nE 'CJK|非 ASCII|non-ASCII' src-c/src/lexer.c
# 期望: 仅 reject 路径

# 6. lib/io.uc 不应存在
git ls-files lib/ | grep -E '^lib/io\.uc$'
# 期望: 无输出

# 7. sys:: namespace §10.4 保留 (spec en §10.4 / spec zh-CN §10.4)
grep -E '^### 10\.4' docs/UltraCPP-v0.3.6-spec-{zh-CN,en}.md
# 期望: §10.4 仍在, + 注 1 段

# 8. .pi/ gitignore
git status --short
# 期望: .pi/ 不出现 (gitignored)
```

---

## §6 范围外 (明确不做)

- ❌ 修改 0.3.4 spec / 0.3.5 spec (immutable)
- ❌ 修改 `.dev/drafts/0.3.4-implementation-process.md` (已 SUPERSEDED 标; 16-0 仅加 SUPERSEDED 标)
- ❌ 修改 `.dev/drafts/0.3.3-implementation-process.md` (已 SUPERSEDED)
- ❌ 创建 `lib/uc_runtime.c` (永远 NO)
- ❌ 13 m0 FAILs 中除 02/03/08/38/48/26/27/28/32/33 之外 (推 0.3.7+: m0_05/06/24/25 + m0_07/m0_48 已 PASS)
- ❌ M1-M5 borrow-check 完整实施 (推 0.4.0+)
- ❌ lib/sync.uc (mutex/atomic, 推 M5)
- ❌ arm64 完整 runtime 验证 (仅 ifdef 分支)
- ❌ Stage 1 bootstrap (0.4.0+)
- ❌ 修改 .pi/ 内容 (pre-existing untracked, 仅 gitignore)
- ❌ 1.0 远期目标

---

## §7 实施顺序 (按依赖)

```
0.3.6 启动
├─ 16-0  docs refresh (HANDOFF/AGENTS + 0.3.5 phase 5+6 closure + .gitignore)
│
├─ Group L (lib 串行)
│   ├─ 16-1a lib/alloc.uc rewrite (brk 链)
│   ├─ 16-1b lib/print.uc rewrite (no array + uc_syscall)
│   ├─ 16-1c lib test activation (4/4: test_print_str/num/alloc/free)
│   ├─ 16-2  lib/io.uc DELETE
│   ├─ 16-5  lib/print_float ftoa (lib/print.uc 增函数)
│   ├─ 16-6  test_print_float activation
│   └─ 16-7  lib/sys/raw.uc arm64
│
├─ Group S (src-c 串行/并行)
│   ├─ 16-3  array parser + codegen + m0_26/27/28
│   ├─ 16-4  struct parser + codegen + m0_32/33
│   ├─ 16-8  typedef codegen + m0_38
│   ├─ 16-9a main return codegen + .uc trailing
│   ├─ 16-9b run_baseline.sh extract_expected_exit regex
│   └─ 16-10 m0_48 runner fix
│
├─ 16-11 spec 0.3.6 双语新写
│
└─ 16-12 docs sync 收官 (release notes + process + milestones + m0-priority + 0.3.5 errata closure)
```

### agent 分组 (per 0.3.4 D6 + 0.3.5 D5)

- **Group L (1-2 agents 串行)**: 16-1a → 16-1b → 16-1c → 16-2 → 16-5 → 16-6 → 16-7
- **Group S (1-2 agents 串行)**: 16-3 → 16-4 → 16-8 → 16-9a → 16-9b → 16-10
- **Group D (1 agent)**: 16-0 + 16-11 + 16-12

### 并行机会

- 16-2 (lib/io delete) 与 16-7 (arm64) 独立 → 可派给 2 个 sub-agent
- 16-3 (array) 与 16-4 (struct) 独立 → 可派给 2 个 sub-agent (但共享 ast.h enum 扩展需串行)
- 16-9b (runner regex) 与 16-10 (m0_48 runner) 独立
- 16-0 (docs refresh) 必须最先

---

## §8 关联文档

### 上游 (已存在, 本次只读)
- `.dev/drafts/0.3.0-post-0.3.3-plan.md` §4.3 — 0.3.6 runtime main return
- `.dev/drafts/0.3.5-implementation-process.md` §7 / §11 — 0.3.6 范围外 + errata
- `.dev/drafts/0.3.5-implementation-plan.md` §3.1 — 14g'/14h/14i drafted
- `.dev/drafts/0.3.0-m0-priority.md` — 13 FAILs P2/P3 + S-items
- `.dev/drafts/0.3.0-runtime-architecture.md` — lib bootstrap + sys:: + uc_syscall

### 本计划完成后需新写
- **`.dev/drafts/0.3.6-implementation-plan.md`** ← 本文件
- `.dev/drafts/0.3.6-implementation-process.md` (commit 16-12 写)
- `docs/UltraCPP-v0.3.6-spec-zh-CN.md` (commit 16-11 写)
- `docs/UltraCPP-v0.3.6-spec-en.md` (commit 16-11 写)
- `docs/UltraCPP-v0.3.6-release-notes.md` (commit 16-12 写)

### 下游 (受 0.3.6 影响)
- `HANDOFF.md` (commit 16-0 刷新 + 16-12 二次)
- `AGENTS.md` (commit 16-0 刷新)
- `.dev/drafts/0.3.5-implementation-process.md` §11 (commit 16-12 errata closure)
- `.dev/plans/0.3.0-borrow-check-milestones.md` (commit 16-12)
- `.dev/drafts/0.3.0-m0-priority.md` (commit 16-12)
- `.gitignore` (commit 16-0 加 .pi/)

### 不修改 (immutable)
- 0.3.4 spec / 0.3.5 spec (仅 cp 为 0.3.6 spec base)
- `.dev/drafts/0.3.4-implementation-process.md` (16-0 仅加 SUPERSEDED 标, 不改内容)
- `.dev/drafts/0.3.3-implementation-process.md` (已 SUPERSEDED)

---

## §9 Open Questions (15 + G1 决策已闭合)

| Q# | 问题 | 状态 |
|---|---|---|
| Q1 | 0.3.6 唯一 canonical plan = 本文件 (D1) | ✅ CLOSED |
| Q2 | lib/print.uc array fix = 完整重写避免 `i8[N]` (D2) | ✅ CLOSED |
| Q3 | lib/alloc.uc fix = brk 链 via `uc_syscall` (D3 + S5) | ✅ CLOSED (G1 修订: 不经 sys:: namespace) |
| Q4 | m0_38 typedef codegen 纳入 0.3.6 (D4) | ✅ CLOSED |
| Q5 | HANDOFF/AGENTS refresh 16-0 (D5 + D9) | ✅ CLOSED |
| Q6 | lib/print_float STUB 保留 14g' (D6) | ✅ CLOSED |
| Q7 | lib/io.uc 删除 0.3.6 (D7) | ✅ CLOSED |
| Q8 | 13 m0 FAILs 中翻 5 个 (02/03/08/38/48) | ✅ CLOSED (扩展为 8 个含 array/struct) |
| Q9 | C unit 末态 ~580-600 | ✅ CLOSED |
| Q10 | 11 commits 分组 (L+S+D) | ✅ CLOSED (扩展为 15 commits) |
| Q11 | array (m0_26/27/28) + struct (m0_32/33) 提前 (D8) | ✅ CLOSED (cross-check 关键 gap fix) |
| Q12 | m0_02/03/08 .uc trailing 注释 16-9a 配套 | ✅ CLOSED |
| Q13 | 0.3.6 spec 双语新写 (D10) | ✅ CLOSED |
| Q14 | 0.3.5 commit 15 phase 5+6 收尾 (D9) | ✅ CLOSED |
| Q15 | .gitignore 加 .pi/ (D11) | ✅ CLOSED |
| **S1-S5** | **G1 sys:: 决策 (保留 + 解读B)** | ✅ **CLOSED** (per user) |

---

## §10 修订历史

| 日期 | 版本 | 改动 | 作者 |
|---|---|---|---|
| 2026-08-28 | v1 (draft) | 初稿：11 commits 整合 2 来源 (post-0.3.3-plan + 0.3.5 process) | orch-plan-writer |
| 2026-08-28 | v2 | **G1 修订**: sys:: namespace 解读B (S1-S5 决策); 15 commits (含 array/struct 8 m0 flips + 0.3.6 spec 新写 + .gitignore); cross-check 5 critical gap 闭合 | orchestrator + implementer |

---

## §11 等待状态

- ⏸ 当前 plan v2 待写盘到 `.dev/drafts/0.3.6-implementation-plan.md` (本任务)
- ⏸ 写盘 + commit 后 0.3.6 实施启动 (commit 16-0 docs refresh 开始)
- ⏸ per commit 独立 dispatch (15 commits 跨 3-5 sessions)