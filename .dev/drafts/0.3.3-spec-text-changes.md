# UltraCPP 0.3.3 — Spec Text Changes (草案)

**Date**: 2026-08-12 (初稿) · 2026-08-17 (与 runtime-architecture 决策集成修订)
**Status**: draft · 待 spec 维护者审
**Owner**: orch-plan-writer
**基础 spec**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.0-spec-zh-CN.md` (2214 行, 2026-08-12)
**关联 0.3.2 文档**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.2-spec-modifications.md` (0.3.2 = bug fix, 已落地 m0_42/m0_41)
**关联 plan 文档**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.3-implementation-plan.md` (实施计划 — 给 implementer 用)
**关联优先级文档**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.0-m0-priority.md` §9.x
**关联决策记录**: `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.0-runtime-architecture.md` (runtime 架构与 BUILTIN_SIGS[] 划分决策, 取代本文档原 7 项 builtin 划分)
**关联 commit**: (待 0.3.3 milestone commit)
**Estimated effort**: S (spec 维护者 1.5-2h)

---

## 1. 元信息 (Metadata)

| 字段 | 值 |
|------|-----|
| 文档标题 | UltraCPP Spec 0.3.3 — Spec Text Changes (S1 + S3 + mod/unmod/move/clone 函数化) |
| 文档路径 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.3-spec-text-changes.md` |
| 基础 spec | `docs/UltraCPP-v0.3.2-spec-zh-CN.md` (0.3.2 修订后, 待创建) |
| 0.3.3 范围 | §4.1 运算符优先级表 + §4.13.5 deref 语义(新) + §7.0 概念清晰化(新) + §7.8 / §11.0 / §12.1 / §12.3 修订 |
| 0.3.3 性质 | **spec 清晰化, 非 bug fix** — 与 0.3.2(修 m0_42/m0_41)严格分开 |
| 状态 | draft, 待 spec 维护者审 |
| 关联 commit | (待 0.3.3 milestone commit) |
| 关联 plan | `0.3.3-implementation-plan.md` (实施计划) |
| Output spec 文件 | (待创建) `docs/UltraCPP-v0.3.3-spec-zh-CN.md` |

### 1.1 0.3.2 → 0.3.3 升级影响 (摘要)

| 维度 | 0.3.2 → 0.3.3 | 备注 |
|------|---------------|------|
| 范围 | **spec 清晰化 (clarification), 非 bug fix** | 不修任何 PASS/FAIL 测试 |
| 新语法 | 无 | 0.3.3 不引入新语法 |
| 新章节 | §7.0 (运算符 vs builtin) + §4.13.5 (deref 语义) | 概念清晰化 |
| 修改章节 | §4.1 (优先级表 + 注脚) + §4.9/§4.10 (mod/unmod 明示) + §7.4/§7.5 (move/clone 明示) + §7.8 (引用) + §11.0 (双分类) + §12.1 (EBNF) + §12.3 (附录优先级表) | 7 个章节 |
| mod/unmod/move/clone 性质 | 从“运算符”(§4.1 旧表 15 级) → “builtin 函数”(§11.0.1) | 重大 spec 修正 |
| §11.0 分类 | 单表 → §11.0.1 (语言原语) + §11.0.2 (stdlib 约定) | 双分类 |
| mod/unmod 实施 | **builtin 函数 (A 类)** — codegen emit IR marker (借用检查器状态机入口/出口, ~5 LOC each) | per `.dev/drafts/0.3.0-runtime-architecture.md` §3.2 + §5.1 |
| codegen 行为 | 6 个 builtin (alloc/free/move/clone/mod/unmod) 全部 emit; `null`/`is_null`/`sizeof`/`alignof` 为 intrinsic (codegen 特殊处理, 不进 BUILTIN_SIGS[]) | 0.3.3 实施层需补 ~100-115 LOC (move/clone 80-100 + free 10 + mod/unmod IR markers 10) |
| stdlib 划分 | `BUILTIN_SIGS[]` 仅 6 项语言原语 (§11.0.1); stdlib (print/strlen/memcpy 等) 全部由 UltraCPP 自己写在 `lib/*.uc`, 不进 BUILTIN_SIGS[]、不接 libc | per `.dev/drafts/0.3.0-runtime-architecture.md` §3.1 + §5.1 |
| 测试 | 27 baseline 预期全保留 | m0_36 (move/free) 由 0.3.3 实施后翻 PASS |

### 1.2 关联 doc 引用

- **0.3.2 spec modifications**: `.dev/drafts/0.3.2-spec-modifications.md` (0.3.2 = bug fix + 概念补全)
- **0.3.2 implementation plan**: `.dev/drafts/0.3.2-implementation-plan.md` (0.3.2 实施)
- **0.3.3 implementation plan**: `.dev/drafts/0.3.3-implementation-plan.md` (本文档配套, 给 implementer)

---

## 2. 概述 (Overview)

### 2.1 0.3.3 修订范围

0.3.3 (minor) 是 **spec 清晰化 (clarification)**, **不是 bug fix**。**核心特征**:

| ID | 类型 | 章节 | 性质 |
|----|------|------|------|
| **S1** | spec 清晰化 | §4.1 一元 op 优先级表 | 改表 (加 2.5 级 + 删 15 级) + 改注脚 |
| **S3** | spec 清晰化 | §4.13.5 deref 语义边界 | 新增 §4.13.5 完整子节 |
| **§7.0** | spec 清晰化 | §7.0 概念清晰化 | 新增 §7.0 (运算符 vs builtin) |
| **mod/unmod/move/clone 函数化** | spec 修正 | §4.1 注脚 + §7.0 + §7.4/§7.5 + §11.0.1 | 把"操作符"重新分类为 builtin 函数 |
| **§11.0 双分类** | spec 清晰化 | §11.0.1 (6 项语言原语 builtin) + §11.0.2 (UltraCPP stdlib 路径 `lib/*.uc`) | 0.3.2 单表 → 0.3.3 双分类 (per runtime-architecture §5) |
| **§12.1 EBNF** | spec 清晰化 | builtin_function_call 产生式 (新) | 区分函数调用 vs 运算符 |
| **§12.3 附录表** | spec 同步 | §12.3 与 §4.1 同步修订 | 同步 |
| **§10.2 / §10.3 / §10.4 (新增, runtime-architecture 集成)** | spec 新增 | §10.2 跨平台预编译宏 (`@ifdef` C 风格); §10.3 内联汇编 (`asm { ... }` C/GCC 风格); §10.4 `sys::` 系统调用命名空间 | per `.dev/drafts/0.3.0-runtime-architecture.md` §6 + §7 + §8 |

合计：**3 个新章节 (§7.0 + §4.13.5 + §11.0 重分组) + 7 个章节修订 (§4.1 + §4.9 + §4.10 + §7.4 + §7.5 + §7.8 + §11.0 + §12.1 + §12.3) + 3 个 §10 子章节新增 (§10.2 + §10.3 + §10.4) + 0 个语法破坏性改动 + 0 个 bug fix**。

> **2026-08-17 runtime-architecture 集成修订** (per `.dev/drafts/0.3.0-runtime-architecture.md`):
> - `BUILTIN_SIGS[]` 从 7 项**改为 6 项** — 移除 `null` (改为 intrinsic, 不进 BUILTIN_SIGS[]); `free` 从 FFI 提升为 builtin; `mod`/`unmod` 从 intrinsic 提升为 builtin (emit IR marker, 由 codegen 处理, 由借用检查器同步)
> - §11.0.2 (stdlib 约定) 14 项**删除** — stdlib 全部由 UltraCPP 自己写在 `lib/*.uc` (per runtime-architecture §3.1 "UltraCPP 不使用 libc 作为 runtime"); §11.0.2 改写为"UltraCPP stdlib 路径"指明 `lib/print.uc`、`lib/string.uc`、`lib/memory.uc`、`lib/sys.uc` 等
> - mod/unmod 的 codegen 行为修正 — **不再是"编译期决策、无 runtime 函数"**; 它们现在是 builtin, emit IR marker (~5 LOC each, 入口/出口标计为 `@uc_borrow_mod_enter` / `@uc_borrow_mod_exit` intrinsic)
> - free 的 codegen 行为修正 — 提升为 builtin 后, emit `call void @free(i8* %p)` + 标记 p 为 freed (p 之后视为 dangling)

### 2.2 触发原因

1. **spec 内部矛盾** — §4.1 表第 15 级列 `move clone` 为"运算符",但 §7.4 / §7.5 描述 `move(p)` / `clone(s)` 是"内建 primitive / 函数"。0.3.0 / 0.3.2 都没修正此矛盾
2. **mod / unmod 性质模糊** — §4.1 注脚说"mod / unmod 是**一元运算符**",但 §4.9 / §4.10 描述为函数调用 `mod(ref)` / `unmod(ref)`
3. **deref 语义边界模糊** — `*p` (lvalue) vs `**pp` (多级 deref) vs `&*p` (恒等) vs `*&x` (恒等) 4 种情况没有明确子节
4. **§11.0 缺分组** — 0.3.2 加了 builtin 签名表,但 18 个 builtin 性质混杂 (语言原语 + stdlib 约定),需要分类
5. **§12.1 EBNF 缺 builtin_function_call** — `mod` / `move` / `clone` 等当前混在 `unary_expression` 中,与真正的运算符 (`*` / `&`) 不区分

### 2.3 0.3.2 → 0.3.3 不破向后兼容

| 维度 | 兼容性 |
|------|--------|
| 语法 | **无变化** — 0.3.3 不引入新语法,只重新分类 |
| 语义 | 0.3.3 把 `mod/unmod/move/clone` 从"运算符"分类改为"builtin 函数"分类,但**调用语法不变** (`mod(ref)` 仍是函数调用形式) |
| 函数调用 | 0.3.3 §11.0.1 收录 `mod/unmod/move/clone` builtin 签名,codegen 查表得正确返回类型 (与 0.3.2 `abs_int` 收录方式一致) |
| API | 无变化 |
| ABI | 无变化 |

### 2.4 0.3.3 与 0.3.2 严格区分

| 0.3.2 | 0.3.3 |
|------|------|
| Bug fix (m0_42 deref-assign + m0_41 abs_int 链接) | Spec clarification (无 bug fix) |
| 加新语法 `(T)expr` | 不加新语法 |
| 加 §11.0 builtin 签名表(单分类, 18 项) | §11.0 重新分组 (§11.0.1 6 项语言原语 builtin + §11.0.2 UltraCPP stdlib 路径 `lib/*.uc`) — stdlib 不再列入 BUILTIN_SIGS[]; per runtime-architecture §5 |
| 无 FFI/syscall 抽象 | 加 §10.2 (跨平台宏) + §10.3 (内联汇编) + §10.4 (`sys::` 命名空间) — 3 个新子节 (C 风格, per runtime-architecture §6-§8) |
| 改 §4.8 (cast) + §6.2 (返回类型) | 改 §4.1 (优先级表) + §7.8 (引用) |
| 0 个新概念章节 | 2 个新概念章节 (§7.0 + §4.13.5) |
| 0.5-1 天 | 1-2 天 |

---

## 3. 新增章节 (New Chapters)

### 3.1 新增 §7.0 概念清晰化:运算符 vs builtin 函数

**插入位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §7 标题(行 854)之后,§7.1 之前。

#### 3.1.1 Before (0.3.2 现状 — §7 直接进入 §7.1)

```markdown
---

## 7. 内存管理

### 7.1 所有权语义（**两条权限拆分**） *(0.3.0 重写：Rule 22, Q4, Q5)*
...
```

#### 3.1.2 After (0.3.3 新增内容 — 在 §7 标题后, §7.1 前插入 §7.0)

```markdown
---

## 7. 内存管理

> **[0.3.3 新增]** 本章开始前, 先建立**核心概念分类**:
> - **运算符 (§4.1)** vs **builtin 函数 (§11.0)** 是两类不同的语言原语
> - 历史上(0.2.0 / 0.3.0) `mod` `unmod` `move` `clone` 列为运算符, 0.3.3 修正为 builtin 函数
> - 详见 §7.0 概念清晰化

### 7.0 概念清晰化:运算符 vs builtin 函数 *(0.3.3 新增)*

UltraCPP 中"语言原语"分两类:

#### 7.0.1 运算符 (§4.1 优先级表)

一元 / 二元 / 三元运算符, 有**结合性**, 在优先级表 §4.1 中明确:

- **一元前缀**: `*` (deref), `&` (addr-of), `+` `-` `!` `~` `++` `--` (2.5 级, **右到左**)
- **一元后缀**: `++` `--` (2 级, **左到右**)
- **二元**: `*` `/` `%` `+` `-` `<<` `>>` `<` `>` `<=` `>=` `==` `!=` `&` (按位与) `^` `|` `&&` `||` `=` `+=` `-=` `*=` `/=` `%=` `&=` `|=` `^=` `<<=` `>>=` (3-13 级, **左到右**)
- **三元**: `?:` (14 级, **右到左**)
- **作用域**: `::` (1 级, **左到右**)

> **注意**: `*` 在 2.5 级(前缀,deref)和 3 级(二元,乘法)**同名异义**, 语法上通过**操作数位置**区分。
> `&` 在 2.5 级(前缀,addr-of)和 8 级(二元,按位与)**同名异义**, 同上。

#### 7.0.2 builtin 函数 (§11.0 签名总表)

**函数调用语法**(带 `()` 括号), 在编译器内部 codegen 路径处理:

> **[0.3.3 + runtime-architecture 集成]** §11.0.1 builtin 语言原语 (**6 项**) + 编译器 intrinsic (4 项, 不进 BUILTIN_SIGS[]) + §11.0.2 UltraCPP stdlib (`lib/*.uc`, 不进 BUILTIN_SIGS[]):

- §11.0.1 **builtin 语言原语 (6 项, 进 BUILTIN_SIGS[])**:
  - §7.2 `alloc(T)` — 堆分配, 返回 `T*` (owning)
  - §7.3 `free(p)` — 释放 owning 指针 (per runtime-arch §3.2 提升从 FFI 到 builtin)
  - §7.4 `move(p)` — 所有权转移, 源 moved-out
  - §7.5 `clone(s)` — 独立副本 (深拷贝)
  - §4.9 `mod(ref)` — 申请修改权, codegen emit `@uc_borrow_mod_enter` IR marker
  - §4.10 `unmod(ref)` — 释放修改权, codegen emit `@uc_borrow_mod_exit` IR marker
- **编译器 intrinsic (4 项, 不进 BUILTIN_SIGS[], per runtime-arch §5.2)**:
  - §7.6 `null` — 空指针常量 (codegen emit `i8* null`)
  - `is_null(p)` — codegen emit `icmp eq i8* %p, null`
  - `sizeof(T)` — 编译期常量
  - `alignof(T)` — 编译期常量
- §11.0.2 **UltraCPP stdlib 路径 (由用户 `import "lib/*.uc"` 提供, 不进 BUILTIN_SIGS[])**:
  - `lib/print.uc` — `uc_print` / `uc_print_num` / `uc_print_float`
  - `lib/string.uc` — `uc_strlen` / `uc_strcpy` / `uc_strcmp` (UltraCPP 自己实现, 不接 libc, per runtime-arch §3.1)
  - `lib/memory.uc` — `uc_memcpy` / `uc_memmove` / `uc_memset`
  - `lib/math.uc` — `uc_abs` (替代 `abs_int`)
  - `lib/alloc.uc` / `lib/sys.uc` (sys:: namespace, §10.4)
  - `lib/uc_runtime.c` (C 临时 bootstrap, 0.4.0+ 替换)

**调用形式**: `name(arg1, arg2, ...)` (所有 6 项 builtin) 或 `name` (intrinsic `null` 是常量)。

**返回值类型**: 查 §11.0.1 签名表确定 (与 §6.2.1 函数返回类型规则一致, 由 0.3.2 引入)。stdlib 函数返回类型由 `lib/*.uc` 的函数定义决定 (不进 builtin 表)。

#### 7.0.3 关键澄清 *(0.3.3 新增)*

`mod` `unmod` `move` `clone` 在 0.2.0 / 0.3.0 spec 中曾被列为"运算符":

- 0.2.0 spec §4.1 第 15 级: `move` `clone` 是运算符
- 0.3.0 spec §4.1 注脚: `mod` `unmod` 是一元运算符
- 0.3.0 spec §12.3 附录优先级表 15 级: `move` `clone` `move_to_thread` 是"所有权操作"

**0.3.3 修正**: 这 4 个都是 **builtin 函数调用**, 与 `alloc` / `free` / `move` / `clone` 同级 (§11.0.1 语言原语 builtin)。

**修正原因**:

1. 它们**总是带括号调用** (`mod(ref)` 不是 `mod ref`), 没有 prefix/postfix 形式
2. 它们**有签名** (返回类型 + 参数类型), 符合 builtin 函数特征
3. 它们**优先级不固定** — `mod(ref)` 中 `ref` 是完整表达式, 与 `+` `-` 等运算符的优先级语义不同
4. 把它们混在优先级表中造成**误读** (e.g. `a + move(b)` 是否要查表?)

**`mod` / `unmod` 的特殊性质** *(0.3.3 + runtime-architecture §3.2 + §5.1 集成)*:

- `mod` / `unmod` 是 **builtin (A 类)** — 不再是"编译期决策、无 runtime 函数"的半成品状态 (2026-08-17 runtime-architecture 集成修订)
- 与 `alloc` / `free` / `move` / `clone` 同列 §11.0.1, 进入 BUILTIN_SIGS[] 表 (BUILTIN_SIGS[] 共 6 项)
- codegen 层**会** emit LLVM IR 调用 — 但发出的是 **IR marker** (`@uc_borrow_mod_enter` / `@uc_borrow_mod_exit` intrinsic), 不是普通函数调用:
  - `@uc_borrow_mod_enter(%ref)` — 标计「进入可修改借权作用域」(该 marker 可被优化为 no-op, 因为校验在编译期完成)
  - `@uc_borrow_mod_exit(%ref)` — 标计「退出可修改借权作用域」
- mod 状态由三个机制维护:
  1. **IR 标记** (codegen emit) — 每个 `UCExprBuiltinCall` 节点对应 `@uc_borrow_mod_enter` / `@uc_borrow_mod_exit` IR 调用
  2. **UCExprCall.mod_state 字段** (编译期) — 申请中 / 已授予 / 已释放
  3. **借用检查器** (compile-time) — 在作用域结束时校验 mod 配对 (Rule 24)
- **提升为 builtin 的理由** (per runtime-architecture §3.2): `mod` / `unmod` 是 borrow-checker 状态入口/出口, 编译器必须**显式**在 IR 中记录; 之前的"intrinsic / 编译期决策"路径无法保证所有 call site 都正确 emit, 因此统一为 builtin
- **历史** (0.2.0-0.3.3): mod/unmod 在 0.2.0 / 0.3.0 / 0.3.1 / 0.3.2 中均被反复重新分类 (`运算符` → `intrinsic` → `builtin`), 0.3.3 + runtime-architecture 集成版是最终版

**修正后**:

- §4.1 优先级表**删除**第 15 级 `move clone`
- §4.1 注脚**改写**, 说明 `mod` / `unmod` 是函数调用
- §12.3 附录优先级表**删除** 15 级, 改放到 §11.0.1 builtin 签名表
- §11.0 重新分组为 §11.0.1 (语言原语 6 项 builtin: `alloc` / `free` / `move` / `clone` / `mod` / `unmod`) + §11.0.2 (UltraCPP stdlib 路径 `lib/*.uc`, 不进 BUILTIN_SIGS[]; per runtime-architecture §3.1 + §5.3)
- §4.9 / §4.10 加 [0.3.3 修订] 注: mod/unmod 是 **builtin 函数**, codegen emit IR marker (`@uc_borrow_mod_{enter,exit}`); 不是单纯编译期决策

#### 7.0.4 历史回顾 *(0.3.3 新增)*

| 版本 | §4.1 表第 15 级 | 注脚对 mod/unmod 描述 | §11.0 builtin 表 | 章节归属 |
|------|----------------|----------------------|-------------------|----------|
| 0.2.0 | `move` `clone` 是运算符 | (无 mod/unmod) | (无表) | §4.1 表 |
| 0.3.0 | `move` `clone` 是运算符 (沿用) | "mod / unmod 是一元运算符" | (无表) | §4.1 表 + §12.3 |
| 0.3.1 (S2) | (无变化) | (无变化) | (无表) | §4.1 表 + §12.3 |
| 0.3.2 (S4+S5) | (无变化) | (无变化) | "单表 18 项" | §4.1 表 + §12.3 + §11.0 |
| **0.3.3 + runtime-arch (本文档当前版)** | 删除 15 级 | "mod/unmod 是 builtin 函数 (A 类, codegen emit IR marker)" | **§11.0.1 (6 项语言原语: alloc/free/move/clone/mod/unmod) + §11.0.2 (UltraCPP stdlib 路径 `lib/*.uc`, 不进 BUILTIN_SIGS[])** | §11.0.1 + §10.2/§10.3/§10.4 |

0.3.3 是首次明确"运算符 vs builtin 函数"分类的版本; runtime-architecture 集成修订 (2026-08-17) 进一步把 mod/unmod 从"半 builtin"提升为**完整** builtin (A 类, emit IR marker), 并把 stdlib 从 BUILTIN_SIGS[] 表移走 (UltraCPP 自带)。

#### 7.0.5 与其他章节的交叉引用 *(0.3.3 新增)*

| 章节 | 关系 |
|------|------|
| §3 类型系统 | 运算符和 builtin 函数操作数的类型约束来自 §3 |
| §4.1 运算符优先级 | 运算符完整列表见 §4.1 |
| §4.9 `mod(ref)` | `mod` 是 builtin 函数 (§11.0.1, A 类, codegen emit IR marker), 不是运算符 |
| §4.10 `unmod(ref)` | `unmod` 是 builtin 函数 (§11.0.1, A 类, codegen emit IR marker), 不是运算符 |
| §6.2.1 函数返回类型 | builtin 函数返回类型由 §11.0 签名表确定 |
| §7.1 所有权 | `move` / `clone` 是 §7.1 概念的核心 builtin (`null` 是 intrinsic, 不在 §11.0.1 表, per runtime-arch §5.2) |
| §7.3 `free(p)` | `free` 是 builtin (§11.0.1, A 类, emit `@free` + 标记 dangling), 不是 FFI |
| §7.4 `move(p)` | 完整语义见 §7.4 (本节是分类, §7.4 是语义) |
| §7.5 `clone(s)` | 完整语义见 §7.5 |
| §11.0.1 builtin 语言原语 (6 项) | §11.0.1 收录 **6** 个语言原语 builtin: alloc / free / move / clone / mod / unmod (per runtime-arch §5.1) |
| §11.0.2 UltraCPP stdlib 路径 | §11.0.2 指明 `lib/print.uc` / `lib/string.uc` / `lib/memory.uc` / `lib/math.uc` / `lib/alloc.uc` / `lib/sys.uc`; stdlib 不进 BUILTIN_SIGS[] (per runtime-arch §5.3 + §9.2) |
| §12.1 EBNF | `builtin_function_call` 产生式见 §12.1 (0.3.3 新增) — 仅含 6 项语言原语; stdlib 走 `import "lib/*.uc"`, 不在 builtin_function_call |
| §12.3 附录优先级表 | 同步 §4.1 修订 |

```

---

### 3.2 新增 §4.13.5 deref 语义

**插入位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §4.13.4 之后(若 0.3.1 S2 已加 §4.13), §4.13.5 之前(若后续章节存在); 若 0.3.2 spec 无 §4.13, 则插入 §4.13 之后(原 §4.14 之前)。

> **前提**: §4.13 lvalue/rvalue 概念由 0.3.1 (S2) 引入。0.3.3 加 §4.13.5 作为 deref 语义专门子节。

#### 3.2.1 Before (0.3.2 现状)

§4.13 (0.3.1 S2 新增) 现有子节:
- §4.13.1 lvalue 定义
- §4.13.2 lvalue 类别表
- §4.13.3 rvalue 定义
- §4.13.4 codegen 路径 (lvalue / rvalue)

**注意**: 0.3.2 spec 没有专门的 deref 语义子节, deref 散落在 §4.1 表(2.5 级 `*`) + §3.5 (指针类型) + §7 (指针使用)。

#### 3.2.2 After (0.3.3 新增 §4.13.5 — 在 §4.13.4 后插入)

```markdown
### 4.13.5 deref 语义 *(0.3.3 新增)*

UltraCPP 中 deref 操作符 `*` (一元前缀) 的完整语义:

#### 4.13.5.1 基本 deref `*p` (p 是 `T*`) *(0.3.3 新增)*

```cpp
T* p = alloc(T);   // p: T*
T v = *p;          // 解引用: 读 p 指向的对象
*p = new_val;      // 解引用: 写 p 指向的对象
```

- **类型**: `T` (p 的目标类型)
- **类别**: **lvalue** (见 §4.13.2 lvalue 类别表 — deref 总是产生 lvalue, 因为它表示内存位置)
- **含义**: 访问 p 指向的对象
- **codegen**: emit `load T, T* p_slot` (读) 或 `store T new_val, T* p_slot` (写)

#### 4.13.5.2 复合 deref 形式 *(0.3.3 新增)*

##### `&*p` (取地址后 deref)

```cpp
T* p = alloc(T);
T** pp = &*p;  // 等价于 pp = p
```

- **等价**: `&*p ≡ p` (恒等)
- **codegen**: 编译器优化, 不 emit load + gep, 直接返回 p 的 SSA 值

##### `*&x` (x 是 lvalue 时)

```cpp
int x = 42;
int v = *&x;  // 等价于 v = x
```

- **前提**: `x` 必须是 lvalue (有内存地址)
- **等价**: `*&x ≡ x` (恒等)
- **codegen**: 编译器优化, 不 emit gep + load, 直接返回 x 的 SSA 值

##### 多级 deref `**pp` (pp 是 `T**`)

```cpp
T** pp = alloc(T*);   // 二级指针
*pp = alloc(T);        // *pp: lvalue, 类型 T*
T* p = *pp;            // 读
**pp = new_val;        // 多级 deref + 写
```

- **类型**: `T` (剥皮 pp 的两层指针)
- **类别**: **lvalue**
- **codegen**: emit 两次 `load T*, T** pp_slot` + `load T, T* inner_slot` (读)
                或 `getelementptr` + `store` (写)

##### `p->field` (deref + 字段访问)

```cpp
struct Point { int x; int y; };
Point* p = alloc(Point);
int v = p->x;          // ≡ (*p).x
p->y = 100;            // ≡ (*p).y = 100
```

- **等价**: `p->field ≡ (*p).field` (§4.13.5.2.4 与 C/C++ 一致)
- **codegen**: emit `getelementptr` + `load` / `store`

#### 4.13.5.3 与 §3.5 指针类型的关系 *(0.3.3 新增)*

`p->field` 和 `(*p).field` 涉及 struct 字段访问:

- struct 字段由 §3.7 定义
- `Point* p` 通过 deref 得到 `Point` lvalue, 再用 `.field` 访问
- `p->field` 是语法糖, codegen 与 `(*p).field` 等价

#### 4.13.5.4 优先级 *(0.3.3 新增)*

`*` (deref) 作为一元前缀在 §4.1 的 **2.5 级**, **右到左**。

与算术 `*` (3 级, **左到右**) 不同 — 语法上通过**操作数位置**区分:

```cpp
int a = *p;     // 2.5 级: * 是 deref, 一元前缀
int b = a * b;  // 3 级: * 是乘法, 二元中缀
int c = **pp;   // 2.5 级: 两个 deref, 右到左, 等价于 *(*pp)
```

#### 4.13.5.5 错误情况 *(0.3.3 新增)*

| 错误 | 错误类型 | 触发 |
|------|----------|------|
| `*p` 中 `p` 不是指针类型 | `InvalidDereferenceError` | e.g. `*42` (int 不能 deref) |
| `**pp` 中 `pp` 不是 `T**` | `InvalidDereferenceError` | e.g. `**42` |
| `*null` | 编译期合法, 运行时 segfault | `null` 是 `T*`, 可 deref, 但 runtime 触发段错误 |

#### 4.13.5.6 与其他章节的交叉引用 *(0.3.3 新增)*

| 章节 | 关系 |
|------|------|
| §3.5 指针类型 | `p` 必须是指针类型才能 deref |
| §3.7 struct 字段 | `p->field` 访问 struct 字段 |
| §4.1 优先级表 | deref 在 2.5 级 (一元前缀) |
| §4.13.2 lvalue 类别 | deref 结果是 lvalue |
| §4.13.3 rvalue | deref 不产生 rvalue (有内存位置) |
| §4.13.4 codegen 路径 | deref 走 lvalue 路径 (load / store) |
| §7.10 自定义指针 | `const T*` deref 仍是 lvalue (但读受限) |
| §12.1 EBNF | `unary_expression` 中 `*` 产生式 |

```

---

### 3.3 新增 §11.0.1 + §11.0.2 builtin 双分类 *(runtime-architecture 集成修订)*

**插入位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §11.0 (0.3.2 新增的签名表) 之后, §11.1 之前。

> **重要**: 0.3.2 spec 已有 §11.0 builtin 签名表 (18 项, 单分类)。0.3.3 重新分组为:
> - §11.0.1 **builtin 语言原语 (6 项)** — `alloc` / `free` / `move` / `clone` / `mod` / `unmod` (per runtime-architecture §5.1)
> - §11.0.2 **UltraCPP stdlib 路径 (`lib/*.uc`)** — 替代 0.3.2 原 stdlib 约定表 (14 项全部删除, 改由 UltraCPP 自己写, per runtime-architecture §3.1 + §5.3)
>
> **同步修订**:
> - `null` / `is_null` / `sizeof` / `alignof` **不**在 §11.0.1 表 — 改为 intrinsic (codegen 特殊处理, per runtime-architecture §5.2)
> - `free` 从「外部 FFI (extern "C" libc free)」**提升**为 builtin §11.0.1 (per runtime-architecture §3.2 + §5.1)
> - `mod` / `unmod` 从「intrinsic (编译期决策, 无 runtime 函数)」**提升**为 builtin §11.0.1 (per runtime-architecture §3.2 + §5.1)

#### 3.3.1 Before (0.3.2 §11.0 现状 — 单分类 18 项)

```markdown
### 11.0 builtin 签名总表 *(0.3.2 新增)*

| 函数名 | 签名 | 返回类型 | 分类 | 详细说明 |
|--------|------|----------|------|----------|
| `print` | `void print(const char* s)` | `void` | I/O | §11.1 |
| `print_num` | `void print_num(int n)` | `void` | I/O | §11.1 |
| `print_float` | `void print_float(double f)` | `void` | I/O | §11.1 |
| `strlen` | `int strlen(const char* s)` | `int` | 字符串 | §11.2 |
| `strcpy` | `char* strcpy(char* dest, const char* src)` | `char*` | 字符串 | §11.2 |
| `strcmp` | `int strcmp(const char* a, const char* b)` | `int` | 字符串 | §11.2 |
| `memcpy` | `void* memcpy(void* dest, const void* src, int n)` | `void*` | 内存 | §11.3 |
| `memmove` | `void* memmove(void* dest, const void* src, int n)` | `void*` | 内存 | §11.3 |
| `memset` | `void* memset(void* s, int c, int n)` | `void*` | 内存 | §11.3 |
| `sizeof_impl` | `int sizeof_impl()` | `int` | 工具 | §11.4 |
| `alignof_impl` | `int alignof_impl()` | `int` | 工具 | §11.4 |
| `is_null` | `bool is_null(int* ptr)` | `bool` | 工具 | §11.4 |
| `clone_impl` | `int* clone_impl(int* ptr)` | `int*` | 工具 | §11.4 |
| `move` | `T* move(T* p)` | `T*` | 内存 | §7.5, §11.3 |
| `alloc` | `T* alloc(T)` | `T*` | 内存 | §7.4, §11.3 |
| `abs_int` | `int abs_int(int x)` | `int` | 数学 | 0.3.2 收录 |
```

#### 3.3.2 After (0.3.3 §11.0 修订 — 双分类 §11.0.1 + §11.0.2, **per runtime-architecture §5**)

```markdown
### 11.0 builtin 签名总表 *(0.3.2 新增, 0.3.3 修订, 双分类, runtime-architecture 集成)*

> **[0.3.3 修订 — runtime-architecture 集成]** 0.3.2 §11.0 是单分类(18 项)。0.3.3 按 §7.0 概念 + `.dev/drafts/0.3.0-runtime-architecture.md` §3-§5 重新分组为:
>
> - §11.0.1 **builtin 语言原语 (6 项)** — 涉及所有权/内存/修改权: `alloc` / `free` / `move` / `clone` / `mod` / `unmod` (per runtime-architecture §5.1 表)
> - §11.0.2 **UltraCPP stdlib 路径** — stdlib 全部由 UltraCPP 自己写在 `lib/*.uc` (per runtime-architecture §3.1 「UltraCPP 不使用 libc 作为 runtime」, §9.2 自举); 不在 BUILTIN_SIGS[] 表
>
> **退入 intrinsic 的项** (B 类, codegen 特殊处理, **不进 BUILTIN_SIGS[]**): `null` / `is_null(p)` / `sizeof(T)` / `alignof(T)` — 编译期常量或一元指令, 不需要函数调用 (per runtime-architecture §5.2)
>
> **被废弃的项** (per runtime-architecture §5.3): `sizeof_impl()` / `alignof_impl()` / `clone_impl()` / `print*` (作为 builtin) / `strlen` / `strcpy` / `strcmp` / `memcpy` / `memmove` / `memset` / `abs_int` (作为 builtin 或 FFI) — 一律改为 UltraCPP stdlib (`lib/*.uc`), 或改为 intrinsic (sizeof(T) / alignof(T))
>
> **遗留 libc 转发的项**: `malloc` — 仍走 `extern "C"` (§10.1 修订版), 但仅作为**用户自己声明的 C 函数**, **不用于 stdlib**; `free` **不再走 extern "C"**, 改为 builtin 收录 §11.0.1

#### 11.0.1 builtin 语言原语 *(0.3.3 新分类, runtime-architecture §5.1 集成)*

§7.0 描述的"语言原语" builtin, 与内存所有权/借用检查直接相关。**共 6 项**, 全部为 A 类 builtin, 全部 codegen emit, 全部进入 `BUILTIN_SIGS[]`:

| 函数 | 返回类型 | 参数签名 | spec 章节 | 备注 |
|------|----------|----------|----------|------|
| `alloc(T)` | `T*` (owning) | `T` 类型 | §7.2 | 堆分配, 返回 owning 指针; codegen emit `call i8* @malloc(sizeof(T))` |
| `free(p)` | `void` | `T*` (owning) | §7.3 | 释放 owning 指针; codegen emit `call void @free(i8* %p)` + 标记 p 为 freed |
| `move(p)` | `T*` (新 owner, 源 moved-out) | `T*` | §7.4 | 所有权转移; codegen emit IR 标记 + 加载 + 返回新 owner |
| `clone(s)` | `T*` (独立 owning 副本) | `T*` | §7.5 | 深拷贝 / 独立副本; codegen emit `alloc(T) + LLVMBuildMemcpy` |
| `mod(ref)` | `void` | `T&` (引用) | §4.9 | 申请修改权 (Rule 24); codegen emit IR marker (`@uc_borrow_mod_enter`, 借用检查器状态入口) |
| `unmod(ref)` | `void` | `T&` (引用) | §4.10 | 释放修改权 (Rule 24); codegen emit IR marker (`@uc_borrow_mod_exit`, 借用检查器状态出口) |

**类型参数化**: `alloc(T)` / `move(p)` / `clone(s)` 是类型参数化 builtin —
参数 `T` 在 codegen 阶段由上下文推导 (e.g. `alloc(int)` → 分配 int 大小)。

**优先级**: `mod` `unmod` `move` `clone` **不在 §4.1 运算符优先级表** (0.3.3 修订),
详见 §7.0.3 关键澄清。

**`free` 提升说明** *(runtime-architecture §3.2 + §5.1)*:
- 0.3.2 / 0.3.3 原计划中 `free` 走 `extern "C"` FFI; 现在提升为 builtin
- 理由: free 是 borrow-checker 必须追踪的状态变更点 (p 之后视为 dangling), 不能透明穿过 libc (否则 IR 无法标记 freed 状态), 必须由 compiler 显式 emit

**`mod` / `unmod` 提升说明** *(runtime-architecture §3.2 + §5.1)*:
- 0.3.2 / 0.3.3 原计划中 `mod` / `unmod` 是「编译期决策、无 runtime 函数」; 现在提升为 builtin
- 理由: 它们是 borrow-checker 状态入口 / 出口, 编译器必须在 IR 中显式记录 (emit `@uc_borrow_mod_enter` / `@uc_borrow_mod_exit` intrinsic); 不再需要专门的 `UC_INTRINSIC_MOD` / `UC_INTRINSIC_UNMOD` 间接路径

#### 11.0.2 UltraCPP stdlib 路径 *(0.3.3 新分类, runtime-architecture §3.1 + §9.2 集成)*

> **[0.3.3 重大修订 — runtime-architecture 集成]** 0.3.2 §11.0 原"stdlib 约定 (14 项)"表中所有项目 (`print` / `strlen` / `strcpy` / `strcmp` / `memcpy` / `memmove` / `memset` / `sizeof_impl` / `alignof_impl` / `is_null` / `clone_impl` / `print_num` / `print_float` / `abs_int`) **全部删除**, **不进 BUILTIN_SIGS[]**, **不接 libc**。这些 stdlib 函数全部由 UltraCPP 自己写在 `lib/*.uc`, **自举路径见 .dev/drafts/0.3.0-runtime-architecture.md §9.2**。

**path**:

| 文件 | 提供函数 | 备注 |
|------|----------|------|
| `lib/print.uc` | `uc_print(s)` / `uc_print_num(n)` / `uc_print_float(f)` | 替代原 builtin `print` / `print_num` / `print_float` |
| `lib/string.uc` | `uc_strlen` / `uc_strcpy` / `uc_strcmp` | UltraCPP 实现, 不接 libc |
| `lib/memory.uc` | `uc_memcpy` / `uc_memmove` / `uc_memset` | UltraCPP 实现 |
| `lib/math.uc` | `uc_abs` (替代 `abs_int`) | UltraCPP 实现 |
| `lib/alloc.uc` | `uc_alloc` / `uc_free` thin wrapper | 备用 (测试用); 主路径走 §11.0.1 builtin |
| `lib/sync.uc` | (远期) `uc_mutex` / `uc_atomic` | Stage 1 后落地 |
| `lib/sys.uc` + `lib/sys/raw.uc` | `sys::` namespace 完整实现 | §10.4 |
| `lib/test/test_runtime.uc` | runtime 单元测试 | Stage 1 后 |

**bootstrap 路径** (per runtime-architecture §9):
- 0.3.x 阶段 (0.3.1-0.3.7): **`lib/uc_runtime.c` (C 实现) 是临时 bootstrap** — 提供 print / strlen / memcpy 等最小集, 让 0.3.x 阶段能工作
- 0.4.0+ 阶段: **首次自举** — 用 uc_compiler 编译 `lib/*.uc` 替换 C 临时 runtime
- 1.0 阶段: 完全自举 (`src-c/` 可移除)

> **`lib/uc_runtime.c` 与 `lib/*.uc` 的关系**: 0.3.3 起, 用户应**首选在 `lib/*.uc` 用 UltraCPP 自身写 stdlib**; `lib/uc_runtime.c` 仅作为临时过渡, 在 `lib/*.uc` 启动后逐文件替换。最终目标: 1.0 时, `lib/uc_runtime.c` 全部被 `lib/*.uc` 替代, `src-c/` 目录可移除 (除非作为保留后端实现)。

> **[0.3.3 过渡特例]** codegen 当前仍 emit `declare i8* @malloc(i64)` 和 `declare void @free(i8*)`(来自 libc),且 `UC_STMT_FREE` / `UC_EXPR_ALLOC` emit 直接调 libc。这是为了与现有 `lib/uc_runtime.c` C bootstrap 兼容。**0.4.0+ 阶段**当 `lib/*.uc` 自建 stdlib 完成后,移除这些 libc 声明,全部由 UltraCPP stdlib 提供。

#### 11.0.3 codegen 集成 *(0.3.2 §11.0.2 改, 0.3.3 §11.0.3 改, runtime-architecture 集成)*

```c
typedef struct {
    const char* name;
    const char* ret_type;       // LLVM IR 类型字符串
    int is_void;
} BuiltinSig;

// [0.3.3 + runtime-architecture §5.1] BUILTIN_SIGS[] 简化为 **6 项** 语言原语:
// - alloc / free / move / clone / mod / unmod
// - null / is_null / sizeof / alignof 不在此表 (intrinsic, codegen 特殊处理, per runtime-architecture §5.2)
// - stdlib (print / strlen / memcpy / ...) 不在此表 (改 UltraCPP stdlib lib/*.uc, per runtime-architecture §5.3)
//
// 类型参数化由 `UCExprCall.return_type` 字段承担 (0.3.2 §6.2.1 已加);
// 不需要 `is_type_parametric` 字段.
//
// mod/unmod 是 **builtin**, emit IR marker (`@uc_borrow_mod_enter` / `@uc_borrow_mod_exit`),
// 不是占位签名; 也不再走单独的 UC_INTRINSIC_MOD / UC_INTRINSIC_UNMOD 路径.
static const BuiltinSig BUILTIN_SIGS[] = {
    { "alloc",   "T*",   0 },  // emit: call @malloc(sizeof(T)) + 类型标记
    { "free",    "void", 1 },  // emit: call @free(%p) + 标记 %p 为 freed (NEW, 从 FFI 提升为 builtin)
    { "move",    "T*",   0 },  // emit: IR 标记 + 加载 + 返回新 owner
    { "clone",   "T*",   0 },  // emit: alloc(T) + LLVMBuildMemcpy
    { "mod",     "void", 1 },  // emit: call @uc_borrow_mod_enter(%ref) IR marker (NEW, 从 intrinsic 提升为 builtin)
    { "unmod",   "void", 1 },  // emit: call @uc_borrow_mod_exit(%ref) IR marker (NEW, 从 intrinsic 提升为 builtin)
    { NULL, NULL, 0 }  // sentinel
};
```

> **[0.3.3 plan A 简化 + runtime-architecture 集成]**
>
> - `BUILTIN_SIGS[]` **仅收录 §11.0.1 语言原语 6 项** (此前为 7 项; `null` 改为 intrinsic, per runtime-architecture §5.2)
> - **stdlib 不进 BUILTIN_SIGS[]** — 改为 UltraCPP stdlib (`lib/*.uc`); 不再走 `extern "C" libc`
> - **不**需要 `is_type_parametric` 字段 — 类型参数化由 `UCExprCall.return_type` 字段承担 (0.3.2 §6.2.1 已加)
> - **不**需要 stdlib 14 项 — 全部由 UltraCPP 自带 stdlib 提供
> - **不**需要 `UC_INTRINSIC_MOD` / `UC_INTRINSIC_UNMOD` 单独路径 — mod/unmod 移入 BUILTIN_SIGS[], 走 `BUILTIN_SIGS[]` 查表 + 统一的 `UC_EXPR_CALL` emit 路径 (与 §6.2.1 优先级链一致)
> - **不再**有占位签名 — mod/unmod 全部是真实 emit (~5 LOC each, 输出 `@uc_borrow_mod_enter` / `@uc_borrow_mod_exit` IR marker)

#### 11.0.4 添加新 builtin 的流程 *(0.3.2 §11.0.3 改, 0.3.3 §11.0.4 改, runtime-architecture 集成)*

未来要加新 builtin (语言原语, 6 项之一):

1. 在 §11.0.1 签名表中加一行 (函数名 + 返回类型 + 参数签名 + spec 章节引用)
2. 在 `src-c/src/codegen.c` 的 `BUILTIN_SIGS[]` 加对应 C 结构项
3. codegen 层 emit 路径:
   - **alloc / free / move / clone**: emit runtime 调用 (LLVM IR 指令 + 所有权标记)
   - **mod / unmod**: emit `call @uc_borrow_mod_{enter,exit}` IR marker (借用检查器状态入口/出口)
4. **stdlib 函数** (print / strlen / memcpy / abs_int 等) **不进 BUILTIN_SIGS[]** — 在 `lib/*.uc` 用 UltraCPP 自己实现 (per runtime-architecture §3.1 + §5.3); 用户通过 `import "lib/print.uc"` 或类似机制使用
5. **intrinsic 函数** (null / is_null / sizeof / alignof) 不进 BUILTIN_SIGS[] — codegen 直接翻译 (per runtime-architecture §5.2); 在 AST / parser 用关键字 + codegen 用 `UC_EXPR_IS_NULL` / `UC_EXPR_SIZEOF` / `UC_EXPR_ALIGNOF` / `UC_EXPR_NULL` 特殊节点

#### 11.0.5 与其他章节的交叉引用 *(0.3.2 §11.0.4 改, 0.3.3 §11.0.5 改, runtime-architecture 集成)*

| 章节 | 关系 |
|------|------|
| §7.0 概念清晰化 | §11.0.1 收录的是 §7.0.2 builtin 函数 (§6.2.1 优先级 1) |
| §7.2 / §7.3 / §7.4 / §7.5 alloc / free / move / clone | §11.0.1 对应行 |
| §4.9 / §4.10 mod / unmod | §11.0.1 列表行 (**builtin** — codegen emit `@uc_borrow_mod_{enter,exit}` IR marker, 借用检查器状态由 codegen + borrow checker 同步维护) |
| §6.2.1 函数返回类型 | builtin 签名是优先级 1 的依据 |
| §7.6 `null` 常量 | `null` **不**在 §11.0.1 表 (intrinsic, per runtime-architecture §5.2); codegen emit `i8* null` 常量 |
| §7.4 / §11.3 `malloc` (C stdlib, 非 UltraCPP) | `malloc` 通过 §10.1 extern "C" 引入; **仅供用户自己声明外部 C 函数使用**, **不用于 stdlib** (per runtime-architecture §3.1) |
| `lib/*.uc` (UltraCPP stdlib 路径) | §11.0.2 改为指明 lib/print.uc / lib/string.uc / lib/memory.uc / lib/math.uc / lib/alloc.uc / lib/sys.uc (per runtime-architecture §9.2) |
| §10.4 `sys::` namespace | 系统调用抽象 (per runtime-architecture §6); stdlib/sys.uc / stdlib/sys/raw.uc |
| §10.2 跨平台宏 | `@ifdef(TARGET_OS_*)` / `@if defined(...)` 等 (per runtime-architecture §7) |
| §10.3 内联汇编 | `asm { "..." : ... }` GCC 约束风格 (per runtime-architecture §8) |
| §12.1 EBNF | `builtin_function_call` 产生式见 §12.1 (0.3.3 新增) — 仅包含 6 项语言原语 + 0 项 stdlib (stdlib 走 `import "lib/*.uc"`, 不在 builtin_function_call) |

```

---

### 3.4 新增 §10.2 / §10.3 / §10.4 (runtime-architecture 集成, 新增 3 子节)

**插入位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §10 FFI 章节 (0.3.0 spec)。在 §10.1 (`extern "C"` 块) 之后插入 §10.2 / §10.3 / §10.4。

> **2026-08-17 新增 (runtime-architecture 集成)**: 0.3.3 在 §10 FFI 章节加 3 个子节, 引入 C 风格的预编译宏、内联汇编、和 `sys::` 系统调用 namespace。这是 `.dev/drafts/0.3.0-runtime-architecture.md` §6 + §7 + §8 的 spec 层落地。

#### 3.4.0 Before (0.3.2 §10 现状)

```markdown
---

## 10. FFI 与外部调用 *(0.3.0 引入)*

> **[0.3.0]** FFI 是与外部代码 (C / 汇编 / 系统调用) 交互的机制。

### 10.1 `extern "C"` 块 *(0.3.0 引入)*

```c
extern "C" {
    void print(const char* s);
    int strlen(const char* s);
    int* malloc(int n);
    void free(int* p);
}
```

- 用户在 `extern "C"` 块中声明外部 C 函数
- 编译器在 codegen 阶段将这些签名转发给 LLVM, 由 LLVM 通过平台 ABI 链接外部实现
- `extern "C"` 块**仅用于用户自己声明外部 C 函数** (少见场景, 如调用系统库、遗留 C 代码)
- 不用于 stdlib —— stdlib 由 UltraCPP 自己写在 `lib/*.uc` (per runtime-architecture §3.1)

```

#### 3.4.1 After (0.3.3 §10 修订 — 加 §10.2 / §10.3 / §10.4)

```markdown
---

## 10. FFI 与外部调用 *(0.3.0 引入, 0.3.3 修订)*

> **[0.3.0 引入]** FFI 是与外部代码 (C / 汇编 / 系统调用) 交互的机制。
>
> **[0.3.3 修订]** §10 补充 3 个子节以描述完整的 FFI / 系统调用机制:
> - §10.1 `extern "C"` 块 — 用户声明外部 C 函数
> - §10.2 (新) 预编译宏机制 (`@ifdef`, C 风格)
> - §10.3 (新) 内联汇编机制 (`asm { ... }`, C/GCC 风格)
> - §10.4 (新) `sys::` 系统调用 namespace

### 10.2 预编译宏机制 *(0.3.3 新增, per runtime-architecture §7)*

UltraCPP 采用 **C 风格的预编译宏语法**, 不采用 Rust 的 `#[cfg(...)]` 风格。理由: 与 C 生态工具链兼容; 与 `src-c/` 现有的 C 实现无缝; 学习曲线友好。`@` 前缀避免与用户代码标识符冲突。

```c
@ifdef(TARGET_OS_LINUX)
    // Linux 实现
@end

@ifdef(TARGET_OS_DARWIN)
    // macOS 实现
@end

@if defined(TARGET_OS_LINUX) && defined(TARGET_ARCH_X86_64)
    // x86_64 Linux 特定代码
@end

@if !defined(TARGET_OS_LINUX)
    @error("lib/sys/raw.uc 仅支持 Linux，其他平台需单独实现")
@end
```

#### 10.2.1 语法子集 *(0.3.3 新增)*

| 语法 | 等效 C | 说明 |
|------|--------|------|
| `@ifdef(MACRO) ... @end` | `#ifdef MACRO ... #endif` | 单条件 |
| `@ifndef(MACRO) ... @end` | `#ifndef MACRO ... #endif` | 反向 |
| `@if defined(MACRO) && defined(MACRO2) ... @end` | `#if defined(MACRO) && defined(MACRO2) ... #endif` | 复合 |
| `@if !defined(MACRO) ... @end` | `#if !defined(MACRO) ... #endif` | 否定 |
| `@else` / `@elif(EXPR)` | `#else` / `#elif` | 分支 |
| `@define(NAME, VALUE)` | `#define NAME VALUE` | 宏定义 |
| `@error("msg")` | `#error "msg"` | 编译期错误 |
| `@warning("msg")` | `#warning "msg"` | 编译期警告 |

#### 10.2.2 预定义宏 *(0.3.3 新增, per runtime-architecture §7.2)*

| 宏 | 值 | 用途 |
|---|----|------|
| `TARGET_OS_LINUX` | 1 | Linux 平台 |
| `TARGET_OS_DARWIN` | 1 | macOS 平台 |
| `TARGET_OS_WINDOWS` | 1 | Windows 平台 |
| `TARGET_OS_FREEBSD` | 1 | FreeBSD 平台 |
| `TARGET_ARCH_X86_64` | 1 | x86_64 架构 |
| `TARGET_ARCH_AARCH64` | 1 | ARM 64 架构 |
| `TARGET_ARCH_RISCV64` | 1 | RISC-V 64 架构 |
| `TARGET_ULTRA_VERSION` | "0.3.3" | UltraCPP 编译器版本 |
| `TARGET_ULTRA_FEATURES` | 位标志 | 启用的语言特性 |

- **预定义宏来源** (per runtime-architecture §11): uc compiler 启动时根据 `uname` 自动检测; 可被 `-D NAME=VALUE` 显式覆盖
- **检测时机**: lexer / parser 阶段; 解析 `@ifdef` / `@if defined()` 时立即求值, 不进入 AST

### 10.3 内联汇编机制 *(0.3.3 新增, per runtime-architecture §8)*

UltraCPP 采用 **C / GCC 风格的内联汇编语法**, 不采用 Rust 的 `asm!` 宏。理由: LLVM 工具链直接理解; 与 `src-c/` 当前 C 实现无缝; 与 §10.2 预编译宏一致使用 C 生态约定。

```c
fn raw_syscall(num: int, arg1: int) -> int {
    int result;
    asm {
        "syscall"
        : "=a"(result)               // 输出操作数
        : "0"(num), "D"(arg1)         // 输入操作数（0 = 与输出 0 复用）
        : "rcx", "r11", "cc", "memory"  // clobbers
    }
    return result;
}
```

#### 10.3.1 语法格式 *(0.3.3 新增)*

格式（4 段, 引号分隔）:

1. **指令模板**：`"syscall"` —— GCC 风格的字符串字面量指令
2. **输出操作数**：`: "=a"(result)` —— `=` 表示只写
3. **输入操作数**：`: "0"(num), "D"(arg1)` —— `"0"` 表示与输出 0 同寄存器
4. **Clobbers**：`: "rcx", "r11", "cc", "memory"` —— 调用者保存 / 内存可能被改

#### 10.3.2 约束字符 *(0.3.3 新增, per runtime-architecture §8.2, 参考 GCC)*

| 约束 | 含义 |
|------|------|
| `r` | 任意通用寄存器 |
| `a` | rax/eax（x86_64 syscall num） |
| `D` | rdi/edi（x86_64 第 1 参） |
| `S` | rsi/esi（x86_64 第 2 参） |
| `d` | rdx/edx（x86_64 第 3 参） |
| `c` | rcx/ecx（x86_64 第 4 参，syscall 后被 clobber） |
| `b` | rbx/ebx（x86_64 第 5 参） |
| `=r` | 输出寄存器（只写） |
| `+r` | 读写操作数 |
| `0`-`7` | 与第 N 个操作数复用同一寄存器 |
| `r0`-`r7` | ARM / RISC-V 寄存器编号 |
| `m` | 内存操作数 |
| `i` | 立即数 |
| `cc` | 条件码寄存器（flags） |
| `memory` | 内存可能被改（编译器需刷新寄存器缓存） |

#### 10.3.3 vs Rust `asm!` *(0.3.3 新增)*

UltraCPP 选择 C 风格, 不采用 Rust `asm!` 宏:

| UltraCPP（C 风格）| Rust `asm!` |
|---|---|
| `asm { "syscall" : "=a"(r) : "0"(n) : "cc" }` | `asm!("syscall", out("rax") r, in("rax") n, lateout("rcx") _, lateout("r11") _, options(preserves_flags))` |
| Clobber list 第四段 | `lateout(...)` / `options(...)` |
| `"0"` 复用前操作数 | 直接传同名变量 |
| `asm volatile { ... }` | `options(preserves_flags, nostack)` 等 |

### 10.4 `sys::` 系统调用 namespace *(0.3.3 新增, per runtime-architecture §6)*

UltraCPP 引入 `sys::` 命名空间作为跨平台系统调用抽象层。模仿 glibc 模式 (high-level wrapper + low-level entry + inline assembly), 但全部用 UltraCPP 自己写, 不接 libc。

#### 10.4.1 架构 *(0.3.3 新增, per runtime-architecture §6.1)*

```
用户代码
    sys::read(fd, buf, len)
        ↓
UltraCPP stdlib 层 (lib/sys/sys.uc, UltraCPP 写)
        ↓
UltraCPP low-level 层 (lib/sys/raw.uc, asm { "syscall" ... })
        ↓
LLVM IR 层
        ↓
机器码 (syscall / svc / ecall)
        ↓
内核态 (Linux / macOS / Windows NT / FreeBSD)
```

#### 10.4.2 `sys::` API 第一版 *(0.3.3 新增, per runtime-architecture §6.2)*

| 类别 | 函数 | 说明 |
|------|------|------|
| **文件 I/O** | `sys::read(fd, buf, len)` | 从 fd 读 len 字节到 buf |
| | `sys::write(fd, buf, len)` | 写 len 字节从 buf 到 fd |
| | `sys::open(path, flags)` | 打开/创建文件, 返回 fd |
| | `sys::close(fd)` | 关闭 fd |
| | `sys::lseek(fd, offset, whence)` | 移动文件指针 |
| **内存映射** | `sys::mmap(addr, len, prot, flags, fd, offset)` | 内存映射 |
| | `sys::munmap(addr, len)` | 取消映射 |
| | `sys::mprotect(addr, len, prot)` | 修改内存保护 |
| **进程** | `sys::exit(code)` | 终止进程 |
| | `sys::getpid()` | 获取进程 ID |
| | `sys::fork()` | fork 子进程 |
| | `sys::execve(path, argv, envp)` | 执行新程序 |
| | `sys::wait4(pid, status, options, rusage)` | 等待子进程 |
| **线程** | `sys::clone(...)` | 创建线程/子进程 |
| | `sys::futex_wait(addr, val)` | futex 等待 |
| | `sys::futex_wake(addr, max)` | futex 唤醒 |
| **时间** | `sys::clock_gettime(clock_id, ts)` | 获取时钟时间 |
| | `sys::nanosleep(req, rem)` | 高精度睡眠 |
| **raw** | `sys::raw_syscall(num, arg1, arg2, ...)` | low-level entry (直通内核) |

#### 10.4.3 跨平台统一命名原则 *(0.3.3 新增, per runtime-architecture §6.3)*

- **用户代码用 `sys::read(fd, buf, len)`** —— 所有平台相同
- **实现按 `@ifdef(TARGET_OS_*)` 区分** —— 同一接口名在不同平台分发到不同实现 (using §10.2 macros)
- **实现按 `@ifdef(TARGET_ARCH_*)` 区分** —— 同一接口在不同架构分发到不同 `asm { ... }` 块 (using §10.3 inline assembly)
- **目录组织** (lib/sys/):
  ```
  lib/sys/
  ├── sys.uc          // sys:: namespace API 公共头
  ├── raw.uc          // sys::raw_syscall low-level 实现
  ├── linux_uc        // Linux-specific 实现 (@ifdef 标记)
  ├── darwin_uc       // macOS-specific 实现
  ├── windows_uc      // Windows-specific 实现 (Win32 syscalls)
  └── freebsd_uc      // FreeBSD-specific 实现
  ```

#### 10.4.4 与 stdlib 的关系 *(0.3.3 新增)*

- `sys::` 是 **UltraCPP 自带 stdlib** (写在 `lib/sys/*.uc`), **不**是 builtin (§11.0.1 不收录)
- 用户通过 `import "lib/sys/sys.uc"` 使用; 不需要 `BUILTIN_SIGS[]` 表 (per runtime-architecture §5.3)
- **raw_syscall**: `sys::raw_syscall` 接受 syscall number + args, 由 `lib/sys/raw.uc` 用 `asm { "syscall" ... }` (x86_64) / `asm { "svc #0" ... }` (aarch64) / `asm { "ecall" ... }` (riscv64) 实现 (per §10.3 + runtime-architecture §6.1)

### 10.5 与其他章节的交叉引用 *(0.3.3 新增)*

| 章节 | 关系 |
|------|------|
| §10.1 `extern "C"` 块 | 用户声明外部 C 函数; **不用于 stdlib** (per runtime-arch §3.1) |
| §10.2 预编译宏 | C 风格 @ifdef / @if defined(); sys:: 跨平台分发靠它 |
| §10.3 内联汇编 | C / GCC 风格 asm { }; sys::raw_syscall 实现靠它 |
| §10.4 sys:: namespace | 跨平台系统调用抽象 (用户在 lib/ 里实现, 不进 builtin 表) |
| §11.0.2 UltraCPP stdlib 路径 | lib/sys.uc 属于 UltraCPP stdlib (§11.0.2) |
| §12.1 EBNF | `preprocessor_directive` / `asm_block` / `sys_call` 产生式 (0.3.3 加) |

```

---

## 4. 修改章节 (Modified Chapters)

### 4.1 修改 §4.1 运算符优先级表 (S1 核心)

**位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §4.1 (行 477 ~ 498)。

#### 4.1.1 Before (0.3.2 现状)

```markdown
| 优先级 | 运算符 | 结合性 |
|--------|--------|--------|
| 1 | `::` | 左到右 |
| 2 | `()` `[]` `.` `->` `++` `--` | 左到右 |
| 3 | `*` `/` `%` | 左到右 |
| 4 | `+` `-` | 左到右 |
| 5 | `<<` `>>` | 左到右 |
| 6 | `<` `>` `<=` `>=` | 左到右 |
| 7 | `==` `!=` | 左到右 |
| 8 | `&` | 左到右 |
| 9 | `^` | 左到右 |
| 10 | `\|` | 左到右 |
| 11 | `&&` | 左到右 |
| 12 | `\|\|` | 左到右 |
| 13 | `=` `+=` `-=` `*=` `/=` `%=` `&=` `\|=` `^=` `<<=` `>>=` | 右到左 |
| 14 | `?:` （三元条件） | 右到左 |
| 15 | `move` `clone` | - |

> **[0.3.0]** 表中第 8 级的 `&` 指**二元中缀**按位与。一元前缀的 `&`（引用）、`mod` / `unmod`（修改权）属于一元运算符，详见 §4.9–§4.10。
```

#### 4.1.2 After (0.3.3 修订)

```markdown
| 优先级 | 运算符 | 结合性 |
|--------|--------|--------|
| 1 | `::` | 左到右 |
| 2 | `()` `[]` `.` `->` `++` `--` (后缀) | 左到右 |
| **2.5** | **`*` `&` `+` `-` `!` `~` `++` `--` (一元前缀)** | **右到左** |
| 3 | `*` `/` `%` (二元) | 左到右 |
| 4 | `+` `-` | 左到右 |
| 5 | `<<` `>>` | 左到右 |
| 6 | `<` `>` `<=` `>=` | 左到右 |
| 7 | `==` `!=` | 左到右 |
| 8 | `&` (按位与, 二元中缀) | 左到右 |
| 9 | `^` | 左到右 |
| 10 | `\|` | 左到右 |
| 11 | `&&` | 左到右 |
| 12 | `\|\|` | 左到右 |
| 13 | `=` `+=` `-=` `*=` `/=` `%=` `&=` `\|=` `^=` `<<=` `>>=` | 右到左 |
| 14 | `?:` （三元条件） | 右到左 |

> **[0.3.3 修订]** 表中第 8 级的 `&` 指**二元中缀**按位与。一元前缀的 `&` (引用) 在 2.5 级。
>
> **`mod` `unmod` `move` `clone` 不在运算符优先级表** — 它们是 **builtin 函数调用**
> (`mod(ref)`、`unmod(ref)` §4.9-§4.10; `move(p)`、`clone(s)` §7.4-§7.5),
> 与 `alloc` / `malloc` / `free` / `print` 同级 (§11.0.1 builtin + §11.0.2 stdlib)。
> 详见 §7.0 概念清晰化。
>
> **0.3.3 删除**原第 15 级 `move clone` (它们是函数, 不是运算符)。
> **0.3.3 加** 2.5 级一元前缀 (`*` deref / `&` addr-of / `+` / `-` / `!` / `~` / `++` / `--`)
> 显式标注**右到左**结合性。
```

#### 4.1.3 Rationale

- **加 2.5 级**: 0.3.1 S2 引入了"一元 op 走 lvalue 路径",但 §4.1 表中**没有**显式 2.5 级行(原表 2 级混了后缀与前缀)。0.3.3 显式区分 2 级(后缀,左到右)和 2.5 级(前缀,右到左)
- **删 15 级**: `move` / `clone` 是函数,不是运算符。0.3.0 / 0.3.2 spec 把它列在 §4.1 表是错误
- **改注脚**: 0.3.0 注脚说 `mod` / `unmod` 是一元运算符,这是错的; 0.3.3 注脚说明它们是 builtin 函数
- **明确 `&` 同名异义**: 2.5 级(addr-of) vs 8 级(按位与)

#### 4.1.4 跨章节影响

- §12.3 附录优先级表 → **同步** §4.1 修订 (见 §4.6)
- §12.1 EBNF → `unary_expression` 中 `*` / `&` 加 2.5 级标注 (见 §4.5)
- §4.9 / §4.10 mod / unmod → 引用 §7.0 + §11.0.1 (见 §4.3)

---

### 4.2 修改 §7.8 引用 (加 deref 边界交叉引用)

**位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §7.8 (行 1009 起)。

#### 4.2.1 Before (0.3.2 §7.8 现状)

```markdown
### 7.8 引用 `&T`（重写 0.2.0 §7.8） *(0.3.0 改写：Rule 22, Rule 24, 删除 T&mut)*

> **[0.3.0 改写]** 0.3.0 删除 `T&mut` 类型。`T&` 是唯一的引用类型，是否可写由 `mod()` + `#modlaw` 决定。
```

#### 4.2.2 After (0.3.3 §7.8 修订)

```markdown
### 7.8 引用 `&T` 与 addr-of *(0.3.0 改写, 0.3.3 修订, Rule 22, Rule 24, 删除 T&mut)*

> **[0.3.0 改写]** 0.3.0 删除 `T&mut` 类型。`T&` 是唯一的引用类型，是否可写由 `mod()` + `#modlaw` 决定。
>
> **[0.3.3 修订]** `&` 既是一元运算符(`&T` 引用类型, §4.1 的 2.5 级),
> 也是 §11.0.1 builtin 函数 `mod(ref)` / `unmod(ref)` / `move(p)` / `clone(s)` 的接受者
> (都接受 `T&` 或 `T*`)。`&` 与 `*` 的语义边界见 §4.13.5 deref 语义章节。
> `mod` / `unmod` 是 **builtin 函数**, 不是运算符, 详见 §7.0 概念清晰化。
```

#### 4.2.3 Rationale

- 0.3.0 §7.8 只说 "T&mut 已删除",没说 `&` 本身的多重身份(运算符 + builtin 接受者)
- 0.3.3 加交叉引用到 §4.13.5 (deref 边界) + §7.0 (概念分类)

---

### 4.3 修改 §4.9 / §4.10 mod / unmod (加 §7.0 / §11.0.1 引用)

**位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §4.9 (行 569 起) + §4.10 (行 610 起)。

#### 4.3.1 Before (0.3.2 §4.9 / §4.10 现状)

```markdown
### 4.9 `mod()` 表达式 — 申请修改权 *(0.3.0 新增：Rule 24)*

> **[0.3.0 · Rule 24]** `mod(ref_expr)` 申请对**引用**所指对象的修改权。
...
```

#### 4.3.2 After (0.3.3 §4.9 / §4.10 修订 — 加 §7.0 / §11.0.1 引用)

```markdown
### 4.9 `mod()` 表达式 — 申请修改权 *(0.3.0 新增, 0.3.3 修订, Rule 24)*

> **[0.3.0 · Rule 24]** `mod(ref_expr)` 申请对**引用**所指对象的修改权。
>
> **[0.3.3 修订]** `mod` 是 **builtin 函数** (§11.0.1 builtin 语言原语),
> **不是**运算符 (§4.1 优先级表中已删除 `mod` 一元位置)。
> 调用形式始终是 `mod(ref)`, 没有 prefix 形式。
> 完整 builtin 签名见 §11.0.1; 概念分类见 §7.0.2 builtin 函数。
>
> **`mod` 是 builtin emit IR marker** — `mod(ref)` 调用 emit `call void @uc_borrow_mod_enter(%ref)` IR marker, 由借用检查器同步维护 mod 状态机 (Rule 24: 申请中 → 已授予 → 已释放)。详见 §7.0.3 关键澄清 + §11.0.1 builtin 签名 + §11.0.3 codegen 集成。

```markdown
### 4.10 `unmod()` 表达式 — 释放修改权 *(0.3.0 新增, 0.3.3 修订, Rule 24)*

> **[0.3.0 · Rule 24]** `unmod(ref_expr)` 释放之前由 `mod()` 获取的修改权。
>
> **[0.3.3 修订]** `unmod` 是 **builtin 函数** (§11.0.1 builtin 语言原语),
> **不是**运算符 (§4.1 优先级表中已删除 `unmod` 一元位置)。
> 调用形式始终是 `unmod(ref)`, 没有 prefix 形式。
> 完整 builtin 签名见 §11.0.1; 概念分类见 §7.0.2 builtin 函数。
>
> **`unmod` 是 builtin emit IR marker** — `unmod(ref)` 调用 emit `call void @uc_borrow_mod_exit(%ref)` IR marker, 与 `mod` 配对, 由借用检查器关闭活跃的 mod 作用域。详见 §7.0.3 关键澄清 + §11.0.1 builtin 签名 + §11.0.3 codegen 集成。
```

#### 4.3.3 Rationale

- 0.3.0 §4.1 注脚把 mod/unmod 列为"一元运算符",与 §4.9/§4.10 的函数调用语法不一致
- 0.3.3 明确 `mod`/`unmod` 是 builtin 函数 (§11.0.1),不是运算符

---

### 4.4 修改 §11 标题 (加 §11.0.1 / §11.0.2 双分类引用)

**位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §11 标题 (行 1563)。

#### 4.4.1 Before (0.3.2 §11 现状)

```markdown
---

## 11. 标准库约定 *(0.3.2 修订)*

> **[0.3.2]** 本章描述 UltraCPP 标准库函数的**签名约定**和**语义约定**。
> **builtin 签名总表**见 §11.0 (0.3.2 新增)。编译器在 codegen 阶段查询 §11.0
> 表以确定函数调用的返回类型, 详见 §6.2.1。

### 11.0 builtin 签名总表

(单分类, 18 项, 见 §3.3.1)
```

#### 4.4.2 After (0.3.3 §11 修订)

```markdown
---

## 11. 标准库约定 *(0.3.2 修订, 0.3.3 修订, runtime-architecture 集成)*

> **[0.3.2]** 本章描述 UltraCPP 标准库函数的**签名约定**和**语义约定**。
> **builtin 签名总表**见 §11.0 (0.3.2 新增, 0.3.3 双分类, 2026-08-17 runtime-architecture 重修订)。
>
> **[0.3.3 修订]** §11.0 重新分组为 (per `.dev/drafts/0.3.0-runtime-architecture.md` §5):
> - §11.0.1 **builtin 语言原语 (6 项)**: `alloc` / `free` / `move` / `clone` / `mod` / `unmod` (全部 codegen emit, 全部进 BUILTIN_SIGS[])
> - §11.0.2 **UltraCPP stdlib 路径** (`lib/*.uc`): 替代 0.3.2 原 14 项 stdlib 约定; stdlib 不再进 BUILTIN_SIGS[], 由 UltraCPP 自己写
>
> **退入 intrinsic 的项** (4 项, 不进 BUILTIN_SIGS[]): `null` / `is_null(p)` / `sizeof(T)` / `alignof(T)` — codegen 特殊处理 (per runtime-architecture §5.2)
>
> 概念分类详见 §7.0 (运算符 vs builtin 函数)。编译器在 codegen 阶段查询 §11.0.1 表以确定 6 项 builtin 语言原语的返回类型; intrinsic 直接翻译; stdlib 走 `import "lib/*.uc"` 不查表。

### 11.0 builtin 签名总表 *(0.3.2 新增, 0.3.3 双分类, runtime-architecture 集成)*

(双分类, 见 §3.3.2 完整 §11.0.1 + §11.0.2)
```

#### 4.4.3 Rationale

- 0.3.2 §11.0 单分类,18 个 builtin 混在一起
- 0.3.3 按 §7.0 概念分类, §11.0.1 是语言原语(影响所有权/借用检查),
  §11.0.2 是 stdlib 约定(普通库函数,不影响借用检查)

---

### 4.5 修改 §12.1 EBNF (加 `builtin_function_call` 产生式)

**位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §12.1 (行 1685 起)。

#### 4.5.1 Before (0.3.2 §12.1 `unary_expression` 现状)

```
unary_expression   ::= postfix_expression
                    | ('+' | '-' | '!' | '~' | '*' | '&') unary_expression
                    | 'sizeof' '(' type ')'
                    | 'move' '(' expression ')'
                    | 'move_to_thread' '(' expression ',' expression ')'  // [0.3.0 Rule 26]
```

#### 4.5.2 After (0.3.3 §12.1 修订 — 加 `builtin_function_call` + `intrinsic_call` + `preprocessor_directive` + `asm_block`)

```
// [0.3.3 + runtime-architecture] Builtin function calls (§11.0.1) — 6 项语言原语 only
builtin_function_call
                   ::= 'alloc' '(' type ')'
                    | 'free' '(' expression ')'
                    | 'move' '(' expression ')'
                    | 'clone' '(' expression ')'
                    | 'mod' '(' expression ')'
                    | 'unmod' '(' expression ')'

// [0.3.3 + runtime-architecture] Intrinsic calls (§11.0.1 外, per runtime-arch §5.2) — 不进 BUILTIN_SIGS[]
intrinsic_call     ::= 'null' '(' ')'
                    | 'is_null' '(' expression ')'
                    | 'sizeof' '(' type ')'
                    | 'alignof' '(' type ')'

// [0.3.3 + runtime-architecture] Preprocessor directives (§10.2, per runtime-arch §7)
preprocessor_directive
                   ::= '@ifdef' '(' identifier ')' directive_body_list '@end'
                    | '@ifndef' '(' identifier ')' directive_body_list '@end'
                    | '@if' preprocessor_expr directive_body_list '@end'
                    | '@else' directive_body_list '@end'
                    | '@define' '(' identifier ',' preprocessor_expr ')'
                    | '@error' '(' string_literal ')'
                    | '@warning' '(' string_literal ')'

// [0.3.3 + runtime-architecture] Inline asm block (§10.3, per runtime-arch §8, GCC 约束风格)
asm_block          ::= 'asm' '{' asm_template '}'
                    | 'asm' 'volatile' '{' asm_template '}'
                    // 4 段格式: instructions : outputs : inputs : clobbers

// [0.3.3 + runtime-architecture] sys:: namespace qualified call (§10.4, per runtime-arch §6)
qualified_call     ::= identifier '::' identifier '(' argument_list? ')'
                    // e.g. sys::read(fd, buf, len), sys::open(path, flags)

// [0.3.3] Unary expression — operators only (§4.1 priority table)
//          Note: mod/unmod/move/clone removed from here, moved to builtin_function_call
//          stdlib (print/strlen/...) 完全不在 EBNF builtin, 走 import lib/*.uc
unary_expression   ::= postfix_expression
                    | ('+' | '-' | '!' | '~' | '*' | '&') unary_expression  // [0.3.3] 2.5 level
                    | 'sizeof' '(' type ')'
                    | 'move_to_thread' '(' expression ',' expression ')'  // [0.3.0 Rule 26, 待 review]

postfix_expression ::= primary_expression
                    | postfix_expression '[' expression ']'
                    | postfix_expression '(' argument_list? ')'
                    | postfix_expression '.' identifier
                    | postfix_expression '->' identifier
                    | postfix_expression '++'
                    | postfix_expression '--'

primary_expression ::= identifier
                    | literal
                    | '(' expression ')'
                    | builtin_function_call                                       // [0.3.3]
                    | intrinsic_call                                             // [0.3.3 + runtime-arch]
                    | asm_block                                                  // [0.3.3 + runtime-arch]
                    | qualified_call                                             // [0.3.3 + runtime-arch]
```

> **[0.3.3 + runtime-architecture 变更说明]**:
> 1. `builtin_function_call` 现在**仅**含 §11.0.1 的 6 项语言原语 builtin; 原 14 项 stdlib (`print` / `strlen` / `memcpy` / ...) **完全移出** builtin_function_call
> 2. 加 `intrinsic_call` 处理 4 项编译器 intrinsic (`null` / `is_null` / `sizeof` / `alignof`, per runtime-arch §5.2)
> 3. 加 `preprocessor_directive` 产生式支持 §10.2 C 风格 `@ifdef` (per runtime-arch §7)
> 4. 加 `asm_block` 产生式支持 §10.3 GCC 风格 `asm { ... }` (per runtime-arch §8)
> 5. 加 `qualified_call` 产生式支持 §10.4 `sys::` namespace 调用 (per runtime-arch §6)
> 6. `move_to_thread` 仍有 (Rule 26), 但需要后续 review 是否该移除 (不在 runtime-arch 范围内)

#### 4.5.3 Rationale

- 0.3.2 §12.1 EBNF 把 `move` / `move_to_thread` 混在 `unary_expression` 中,语义模糊
- 0.3.3 加 `builtin_function_call` 产生式,显式区分 builtin 函数调用与运算符
- `mod` / `unmod` / `move` / `clone` 从 `unary_expression` 移到 `builtin_function_call`
- **0.3.3 + runtime-architecture 加 4 个新产生式** (`intrinsic_call` / `preprocessor_directive` / `asm_block` / `qualified_call`), 完整覆盖 §11.0.1 (6 项 builtin) + intrinsic (4 项) + §10.2 (宏) + §10.3 (asm) + §10.4 (sys:: namespace)

---

### 4.6 修改 §12.3 附录优先级表 (与 §4.1 同步)

**位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §12.3 (行 1924 起)。

#### 4.6.1 Before (0.3.2 §12.3 现状)

```markdown
### 12.3 运算符优先级表 *(0.3.0 修订)*

| 级别 | 运算符 | 描述 |
|------|--------|------|
| 1 | `::` | 作用域解析 |
| 2 | `()` `[]` `.` `->` `++` `--` | 后缀 |
| 3 | `*` `/` `%` | 乘法 |
| 4 | `+` `-` | 加法 |
| 5 | `<<` `>>` | 移位 |
| 6 | `<` `>` `<=` `>=` | 关系 |
| 7 | `==` `!=` | 相等 |
| 8 | `&` | 按位与（**二元中缀**） |
| 9 | `^` | 按位异或 |
| 10 | `\|` | 按位或 |
| 11 | `&&` | 逻辑与 |
| 12 | `\|\|` | 逻辑或 |
| 13 | `?:` | 三元条件 |
| 14 | `=` `+=` `-=` `*=` `/=` `%=` `&=` `\|=` `^=` `<<=` `>>=` | 赋值 |
| 15 | `move` `clone` `move_to_thread` *(0.3.0 新增)* | 所有权操作 |

**一元运算符**（优先级高于上表所有二元运算符，右到左结合）：

| 运算符 | 描述 |
|--------|------|
| `+` `-` | 正号 / 取负 |
| `!` | 逻辑非 |
| `~` | 按位非 |
| `*` | 解引用 |
| `&` | **引用**（§3.3） |
| `mod` `unmod` *(0.3.0 新增)* | 申请 / 释放修改权（Rule 24） |
```

#### 4.6.2 After (0.3.3 §12.3 修订 — 与 §4.1 同步)

```markdown
### 12.3 运算符优先级表 *(0.3.0 修订, 0.3.3 修订)*

> **[0.3.3 修订]** 本附录表与 §4.1 主表同步修订:
> - 加 2.5 级(显式标注一元前缀,**右到左**)
> - 删 15 级(`move` `clone` `move_to_thread` — 它们是 builtin 函数,不是运算符,见 §7.0 + §11.0.1)
> - 一元运算符表中删除 `mod` `unmod` (它们是 builtin 函数,见 §11.0.1)

| 级别 | 运算符 | 描述 |
|------|--------|------|
| 1 | `::` | 作用域解析 |
| 2 | `()` `[]` `.` `->` `++` `--` | 后缀（左到右） |
| **2.5** | **`*` `&` `+` `-` `!` `~` `++` `--` (前缀)** | **一元前缀（右到左）** |
| 3 | `*` `/` `%` | 乘法（二元，左到右） |
| 4 | `+` `-` | 加法（二元，左到右） |
| 5 | `<<` `>>` | 移位（左到右） |
| 6 | `<` `>` `<=` `>=` | 关系（左到右） |
| 7 | `==` `!=` | 相等（左到右） |
| 8 | `&` | 按位与（**二元中缀**，左到右） |
| 9 | `^` | 按位异或（左到右） |
| 10 | `\|` | 按位或（左到右） |
| 11 | `&&` | 逻辑与（左到右） |
| 12 | `\|\|` | 逻辑或（左到右） |
| 13 | `?:` | 三元条件（右到左） |
| 14 | `=` `+=` `-=` `*=` `/=` `%=` `&=` `\|=` `^=` `<<=` `>>=` | 赋值（右到左） |

> **[0.3.3 删除]** 原 15 级 `move` `clone` `move_to_thread` — 它们是 builtin 函数,
> 不是运算符。`move` / `clone` 签名见 §11.0.1; `move_to_thread` 是 §11.5 线程原语 builtin。

**一元运算符**（优先级 2.5, 右到左）：

| 运算符 | 描述 |
|--------|------|
| `+` `-` | 正号 / 取负 |
| `!` | 逻辑非 |
| `~` | 按位非 |
| `*` | 解引用 (deref) — 详见 §4.13.5 |
| `&` | **引用** (addr-of) — 详见 §3.3, §7.8 |

> **[0.3.3 删除]** `mod` `unmod` 从一元运算符表移除。它们是 builtin 函数,见 §4.9 / §4.10 + §11.0.1。
```

#### 4.6.3 Rationale

- 0.3.2 §12.3 附录表与 §4.1 主表**不一致**: §4.1 表把 `move` `clone` 列在 15 级, §12.3 又把它们与 `move_to_thread` 一起列在 15 级并加"一元运算符"表
- 0.3.3 让 §12.3 与 §4.1 同步: 删 15 级 + 加 2.5 级 + 删 `mod`/`unmod` 一元表行

---

### 4.7 修改 §7.4 / §7.5 move / clone (加 builtin 函数分类 + codegen 路径说明)

**位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §7.4 (行 951 起) + §7.5 (行 980 起)。

#### 4.7.1 Before (0.3.2 §7.4 / §7.5 现状)

```markdown
### 7.4 `move(p)` 表达式 — 所有权转移 *(0.3.0 改写：Rule 22)*

> **[0.3.0 · Rule 22]** `move(p)` 将指针 `p` 的所有权转移给新变量, 原变量变为 moved-out 状态。
...
```

#### 4.7.2 After (0.3.3 §7.4 / §7.5 修订 — builtin 函数化 + codegen 说明)

```markdown
### 7.4 `move(p)` 表达式 — 所有权转移 *(0.3.0 改写, 0.3.3 修订, Rule 22)*

> **[0.3.0 · Rule 22]** `move(p)` 将指针 `p` 的所有权转移给新变量, 原变量变为 moved-out 状态。
>
> **[0.3.3 修订]** `move` 是 **builtin 函数** (§11.0.1 builtin 语言原语),
> **不是**运算符 (§4.1 优先级表 15 级已删除)。调用形式始终是 `move(p)`, 没有 prefix 形式。
> 概念分类见 §7.0.2 builtin 函数; builtin 签名见 §11.0.1。
>
> **codegen 路径** (与简单函数调用**不同**): `move(p)` 不走普通函数 emit,
> 而是通过特殊 IR 标记 (`UCExprCall.ownership_xfer = MOVE`) + LLVM IR 重新绑定 SSA 值
> 实现所有权转移。源 `p` 在 IR 中标记为 moved-out (后续 use 触发 use-after-move 错误)。
> codegen 代码集中在 `src-c/src/codegen.c` 的 `emit_move_call()`, 约 30-50 LOC。
> 详见 §11.0.3 codegen 集成。
```

```markdown
### 7.5 `clone(s)` 表达式 — 深拷贝 *(0.3.0 改写, 0.3.3 修订)*

> **[0.3.2]** `clone(s)` 创建 `s` 指向对象的**独立深拷贝副本**, 返回新的 owning 指针。
> 原 `s` 不受影响。
>
> **[0.3.3 修订]** `clone` 是 **builtin 函数** (§11.0.1 builtin 语言原语),
> **不是**运算符 (§4.1 优先级表 15 级已删除)。调用形式始终是 `clone(s)`, 没有 prefix 形式。
> 概念分类见 §7.0.2 builtin 函数; builtin 签名见 §11.0.1。
>
> **codegen 路径** (与简单函数调用**不同**): `clone(s)` 不走普通函数 emit,
> 而是通过特殊 LLVM IR 生成 (按 struct 大小 emit `load` + `malloc` + 逐字段 `store`)
> 实现深拷贝。浅拷贝 (如 `int*` + `size`) 与深拷贝 (struct 递归) 走两套 emit。
> codegen 代码集中在 `src-c/src/codegen.c` 的 `emit_clone_call()`, 约 50-80 LOC。
> 详见 §11.0.3 codegen 集成。
```

#### 4.7.3 Rationale

- 0.3.0 / 0.3.2 spec §7.4 / §7.5 把 `move` / `clone` 描述为"内建 primitive / 函数",
  但 §4.1 又把它们列在 15 级"运算符",**内部矛盾**
- 0.3.3 三层修复:
  1. 概念分类: builtin 函数 (§7.0.2 + §11.0.1), 不是运算符 (§4.1 删 15 级)
  2. 语义保留: §7.4 / §7.5 不动 (所有权转移 / 深拷贝语义不变)
  3. codegen 说明: 加特殊 emit 路径说明 (不是 pass-through 函数调用),
     让读者理解 `move(p)` / `clone(s)` 与 `print(s)` 在 LLVM IR 层走完全不同的路径

---

## 5. 跨章节引用更新 (Cross-Reference Updates)

下表列出 spec 全文涉及 "运算符" / "builtin 函数" / "mod/unmod/move/clone" / "优先级表" 的位置, 以及 0.3.3 修订建议。

| 章节 | 行号 (0.3.2) | 当前引用 | 0.3.3 修订 |
|------|---------------|----------|------------|
| §3 类型系统 | 100+ | 类型 ↔ LLVM IR 映射 | 不变 |
| §3.5 指针类型 | 280+ | 指针类型 | 不变 |
| §3.7 struct | 380+ | struct 字段 | **加** §4.13.5 deref 边界 (`p->field` ≡ `(*p).field`) |
| §4.1 优先级表 | 477 | 单表 15 级 | **改** 加 2.5 级 + 删 15 级 + 改注脚 (见 §4.1) |
| §4.2 - §4.7 | 500+ | 二元运算符 | 不变 |
| §4.8 带括号 | 563+ | 括号表达式 | 不变 (0.3.2 已加 cast) |
| §4.9 mod | 569+ | mod 操作数 | **改** 加 §7.0 + §11.0.1 引用 (见 §4.3) |
| §4.10 unmod | 610+ | unmod 操作数 | **改** 加 §7.0 + §11.0.1 引用 (见 §4.3) |
| §4.13 (0.3.1 S2) | (S2 加) | lvalue / rvalue | **加** §4.13.5 deref 语义 (新章节, 见 §3.2) |
| §6.1 函数定义 | 749+ | 函数定义 | 不变 |
| §6.2.1 返回类型 (0.3.2 加) | (0.3.2 加) | builtin 签名 | 不变 (§11.0.1 + §11.0.2 替代) |
| §7 标题 | 854 | 内存管理 | **改** 加 §7.0 引用 (见 §3.1) |
| §7.0 (新) | (待插入) | — | §7.0 完整 (见 §3.1) |
| §7.1 所有权 | 856 | 所有权语义 | 不变 |
| §7.2 alloc | 919+ | alloc 函数 | **加** §11.0.1 引用 |
| §7.3 free | 933+ | free 函数 | **加** §11.0.1 引用 |
| §7.4 move | 951+ | `move(p)` 函数 | **改** 加 §7.0 + §11.0.1 引用 + codegen emit 路径说明 (0.3.3 函数化, 见 §4.7) |
| §7.5 clone | 980+ | `clone(s)` 函数 | **改** 加 §7.0 + §11.0.1 引用 + codegen emit 路径说明 (0.3.3 函数化, 见 §4.7) |
| §7.6 null | 989+ | null 常量 | **加** §11.0.1 引用 |
| §7.8 引用 | 1009+ | T& 引用 | **改** 加 §4.13.5 + §7.0 引用 (见 §4.2) |
| §11 标题 | 1563 | 标准库约定 | **改** 加 §11.0.1 / §11.0.2 引用 (见 §4.4) |
| §11.0 (0.3.2) | (0.3.2 加) | builtin 单表 | **改** 双分类 §11.0.1 + §11.0.2 (见 §3.3) |
| §11.1 - §11.6 | 1565-1680 | 各子节 | **改** 各子节加 §11.0.2 双向引用 |
| §12.1 EBNF | 1685+ | 完整 EBNF | **改** 加 `builtin_function_call` 产生式 (见 §4.5) |
| §12.3 附录优先级表 | 1924+ | 优先级表附录 | **改** 与 §4.1 同步 (见 §4.6) |

**注**: 跨章节引用改动 ~15 处。spec 维护者按本表逐项应用。

---

## 6. EBNF 语法更新 (§12.1)

**位置**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md` §12.1 完整 EBNF 语法 (行 1685 起)。

### 6.1 Before (0.3.2 EBNF 现状 — `unary_expression` 片段)

```ebnf
unary_expression   ::= postfix_expression
                    | ('+' | '-' | '!' | '~' | '*' | '&') unary_expression
                    | 'sizeof' '(' type ')'
                    | 'move' '(' expression ')'
                    | 'move_to_thread' '(' expression ',' expression ')'  // [0.3.0 Rule 26]
```

### 6.2 After (0.3.3 EBNF 修订 — 加 `builtin_function_call` + 修订 `intrinsic_*` + 加 `preprocessor_directive` / `asm_block`)

```ebnf
// [0.3.3 NEW + runtime-architecture 集成] Builtin function calls — distinguished from operators (§7.0, §11.0.1)
// 仅包含 §11.0.1 的 6 项语言原语 builtin. stdlib (print / strlen / memcpy / ...)
// 走 import "lib/*.uc" 的普通函数调用, 不在此产生式.
builtin_function_call
                   ::= 'alloc' '(' type ')'                  // [§11.0.1] alloc(T), return T*
                    | 'free' '(' expression ')'              // [§11.0.1] free(p), return void (runtime-arch §3.2 提升)
                    | 'move' '(' expression ')'              // [§11.0.1] move(p), return T*
                    | 'clone' '(' expression ')'             // [§11.0.1] clone(s), return T*
                    | 'mod' '(' expression ')'               // [§11.0.1] mod(ref), return void (runtime-arch §3.2 提升)
                    | 'unmod' '(' expression ')'             // [§11.0.1] unmod(ref), return void (runtime-arch §3.2 提升)

// [0.3.3 NEW + runtime-architecture 集成] Compiler intrinsics (不进 BUILTIN_SIGS[], codegen 特殊处理, per runtime-arch §5.2)
intrinsic_call     ::= 'null' '(' ')'                        // 返回 i8* null 常量
                    | 'is_null' '(' expression ')'           // emit icmp eq i8* %p, null
                    | 'sizeof' '(' type ')'                  // 编译期常量
                    | 'alignof' '(' type ')'                 // 编译期常量

// [0.3.3 NEW + runtime-architecture 集成] Preprocessor directives (§10.2)
preprocessor_directive
                   ::= '@ifdef' '(' identifier ')' directive_body_list '@end'
                    | '@ifndef' '(' identifier ')' directive_body_list '@end'
                    | '@if' preprocessor_expr directive_body_list '@end'
                    | '@elif' preprocessor_expr               // [@else 分支中的 elif]
                    | '@else' directive_body_list '@end'
                    | '@define' '(' identifier ',' preprocessor_expr ')'    // 简单赋值宏
                    | '@error' '(' string_literal ')'        // 编译期错误
                    | '@warning' '(' string_literal ')'      // 编译期警告

directive_body_list
                   ::= /* empty */
                    | directive_body_list (preprocessor_directive | statement | declaration)

preprocessor_expr  ::= /* C 风格条件表达式 */
                      // defined(MACRO), MACRO, MACRO == VALUE, !defined(MACRO),
                      // MACRO && MACRO2, MACRO || MACRO2, etc.

// [0.3.3 NEW + runtime-architecture 集成] Inline asm block (§10.3, GCC 约束风格)
asm_block          ::= 'asm' '{' asm_template asm_clobber_list? '}'
                    | 'asm' 'volatile' '{' asm_template asm_clobber_list? '}'

asm_template       ::= string_literal                         // 指令模板, e.g. "syscall"
                      /* 4 段格式由 GCC 约束语法定义:
                       * : output_operands ("=a"(result))
                       * : input_operands ("0"(num), "D"(arg1))
                       * : clobber_list ("rcx", "r11", "cc", "memory")
                       */

// [0.3.3 NEW + runtime-architecture 集成] sys:: namespace 调用 (§10.4)
// 这是普通函数调用 (解析为 import "lib/sys/sys.uc" 中的 std::read / sys::open / ...),
// 不走 builtin_function_call, 不进 BUILTIN_SIGS[]
// (用户代码形如 sys::read(fd, buf, n); parser 识别 `sys::` 命名空间语法)
qualified_call     ::= identifier '::' identifier '(' argument_list? ')'

// [0.3.3] Unary expression — operators ONLY (mod/unmod/move/clone moved to builtin_function_call)
unary_expression   ::= postfix_expression
                    | ('+' | '-' | '!' | '~' | '*' | '&') unary_expression  // [0.3.3] 2.5 level, right-to-left

postfix_expression ::= primary_expression
                    | postfix_expression '[' expression ']'
                    | postfix_expression '(' argument_list? ')'
                    | postfix_expression '.' identifier
                    | postfix_expression '->' identifier
                    | postfix_expression '++'
                    | postfix_expression '--'

primary_expression ::= identifier
                    | literal
                    | '(' expression ')'
                    | builtin_function_call                                       // [0.3.3]
                    | intrinsic_call                                             // [0.3.3 + runtime-arch §5.2]
                    | asm_block                                                  // [0.3.3 + runtime-arch §8]
```

> **[0.3.3 + runtime-architecture 重要变化]**: `builtin_function_call` 仅含 6 项语言原语 (`alloc` / `free` / `move` / `clone` / `mod` / `unmod`); 原 14 项 stdlib (`print` / `strlen` / `memcpy` 等) **完全移出** builtin_function_call, 用户通过 `import "lib/print.uc"` 等使用。
>
> `intrinsic_call` 是新的 4 项特殊产生式 (`null` / `is_null` / `sizeof` / `alignof`), 走 intrinsic 路径不进 BUILTIN_SIGS[] 表。
>
> `preprocessor_directive` / `asm_block` 是 §10.2 / §10.3 新引入的语法。`preprocessor_directive` 在 lexer / parser 阶段立即求值 (不进入 AST); `asm_block` 走特殊 LLVM IR codegen 路径, 由 codegen 通过 LLVM inline asm intrinsic 翻译。

### 6.3 同步: §12.3 附录优先级表

见 §4.6 — 与 §4.1 主表同步修订。

### 6.4 Rationale

- EBNF 当前**混** `move` / `mod` 在 `unary_expression` 中,与 `*` / `&` 运算符同级,语义模糊
- 0.3.3 加 `builtin_function_call` 规则,显式区分 builtin 函数调用与运算符
- `mod` / `unmod` / `move` / `clone` 从 `unary_expression` 移到 `builtin_function_call`
- **0.3.3 + runtime-architecture 进一步修订**:
  - `builtin_function_call` 仅含 6 项语言原语 (`alloc` / `free` / `move` / `clone` / `mod` / `unmod`); stdlib 14 项 (`print` / `strlen` / `memcpy` / ...) **完全移出**, 走 `import "lib/*.uc"` 普通函数调用
  - 加 `intrinsic_call` 产生式处理 4 项编译器 intrinsic (`null` / `is_null` / `sizeof` / `alignof`, per runtime-arch §5.2), 不进 BUILTIN_SIGS[]
  - 加 `preprocessor_directive` 产生式 (§10.2, C 风格 `@ifdef` / `@if defined()`)
  - 加 `asm_block` 产生式 (§10.3, GCC 约束风格)
  - 加 `qualified_call` 产生式处理 `sys::read(...)` 等 namespace 调用 (§10.4)

---

## 7. Before / After Diff 完整对比

### 7.1 §4.1 运算符优先级表 (修订)

| 维度 | 0.3.2 | 0.3.3 |
|------|-------|-------|
| 2 级 | `() [] . -> ++ --` (混后缀与前缀) | 明确"后缀,左到右" |
| 2.5 级 | ❌ 不存在 | ✅ 加 (`*` `&` `+` `-` `!` `~` `++` `--` 一元前缀, **右到左**) |
| 15 级 `move clone` | ✅ 存在(错) | ✅ 删除 (移至 §11.0.1) |
| 注脚 | "`mod` / `unmod` 是一元运算符" (错) | "`mod` `unmod` 是 builtin 函数" |

### 7.2 §7.0 概念清晰化 (新增)

| 维度 | 0.3.2 | 0.3.3 |
|------|-------|-------|
| 章节存在 | ❌ 不存在 | ✅ §7.0 全章 (5 子节) |
| 运算符 vs builtin 分类 | ❌ | ✅ §7.0.1 vs §7.0.2 |
| mod/unmod/move/clone 性质 | 矛盾(运算符 vs 函数) | ✅ 明确 builtin 函数 (§7.0.3) |
| 历史回顾 | ❌ | ✅ §7.0.4 (0.2.0 → 0.3.3) |
| 交叉引用 | ❌ | ✅ §7.0.5 (11 章节) |

### 7.3 §4.13.5 deref 语义 (新增)

| 维度 | 0.3.2 | 0.3.3 |
|------|-------|-------|
| 章节存在 | ❌ 不存在 | ✅ §4.13.5 全章 (6 子节) |
| `*p` 基本 deref | 散落 | ✅ §4.13.5.1 完整 |
| `&*p` / `*&x` 恒等 | 隐式 | ✅ §4.13.5.2 显式 |
| `**pp` 多级 deref | ❌ | ✅ §4.13.5.2.3 |
| `p->field` ≡ `(*p).field` | ❌ | ✅ §4.13.5.2.4 |
| 与 `*` 乘法同名异义 | ❌ | ✅ §4.13.5.4 |

### 7.4 §7.8 引用 (修订)

| 维度 | 0.3.2 | 0.3.3 |
|------|-------|-------|
| `&` 多重身份说明 | ❌ | ✅ 加 (运算符 2.5 + builtin 接受者) |
| deref 边界引用 | ❌ | ✅ 加 §4.13.5 引用 |
| mod/unmod 函数化引用 | ❌ | ✅ 加 §7.0 + §11.0.1 引用 |

### 7.5 §4.9 / §4.10 mod / unmod (修订)

| 维度 | 0.3.2 | 0.3.3 |
|------|-------|-------|
| mod/unmod 性质 | "一元运算符" (错) | "builtin 函数" (对) |
| §7.0 引用 | ❌ | ✅ |
| §11.0.1 引用 | ❌ | ✅ |

### 7.6 §11 builtin 双分类 (修订, runtime-architecture 集成)

| 维度 | 0.3.2 | 0.3.3 初稿 | **0.3.3 + runtime-architecture (本文档当前版)** |
|------|-------|-----------|------------------------------------------------|
| §11.0 单分类 | ✅ 18 项 | ❌ 删除 (双分类替代) | ❌ 删除 (双分类替代) |
| §11.0.1 builtin 语言原语 | ❌ | ✅ 7 项 (alloc/free/move/clone/null/mod/unmod) | ✅ **6 项** (alloc/free/move/clone/mod/unmod; `null` 退到 intrinsic, per runtime-arch §5.2) |
| §11.0.2 stdlib / UltraCPP stdlib 路径 | ❌ | ✅ 14 项 stdlib 约定 (I/O/字符串/内存/工具/abs_int), 走 `extern "C"` | ✅ **UltraCPP stdlib 路径 `lib/*.uc`**: print/string/memory/math/alloc/sys.stdlib; **不接 libc** (per runtime-arch §3.1 + §5.3) |
| `free` 分类 | FFI / extern "C" | FFI / extern "C" | **builtin** (提升从 FFI, per runtime-arch §3.2) |
| `mod` / `unmod` 分类 | intrinsic | intrinsic (编译期决策, 无 emit) | **builtin** (提升从 intrinsic, emit IR marker, per runtime-arch §3.2) |
| codegen `BUILTIN_SIGS[]` | 18 项 (含 stdlib, 含缺漏 body) | 7 项语言原语 (无 stdlib) | **6 项**语言原语 (alloc/free/move/clone/mod/unmod) — `null` 不进, stdlib 不进; 全部有 body |
| `lib/uc_runtime.c` bootstrap | C 实现 (临时) | C 实现 (临时, plan 阶段不补) | C 实现仍是临时 bootstrap (per runtime-arch §9.3); `lib/*.uc` 是 0.4.0+ 目标 |
| §11 标题注 | 单分类 | 双分类 | 双分类 + runtime-arch 集成 |

### 7.7 §12.1 EBNF (修订, runtime-architecture 集成)

| 维度 | 0.3.2 | 0.3.3 初稿 | **0.3.3 + runtime-architecture (本文档当前版)** |
|------|-------|-----------|------------------------------------------------|
| `builtin_function_call` 产生式 | ❌ 无 | ✅ 新增 (19 个 builtin, 含 stdlib) | ✅ 新增 (**6 项语言原语** + 0 stdlib); 加 `intrinsic_call` (4 项) / `preprocessor_directive` (§10.2) / `asm_block` (§10.3) / `qualified_call` (§10.4) |
| `unary_expression` 含 `move` | ✅ (错) | ❌ 删除 | ❌ 删除 (保持) |
| `unary_expression` 含 `mod`/`unmod` | ❌ (0.3.1 已删, 0.3.0 spec 残留) | ❌ 确认删除 | ❌ 确认删除 (保持) |
| `intrinsic_call` 产生式 | ❌ | ❌ | ✅ 新增 (4 项: null / is_null / sizeof / alignof) |
| `preprocessor_directive` 产生式 | ❌ | ❌ | ✅ 新增 (§10.2 C 风格 @ifdef) |
| `asm_block` 产生式 | ❌ | ❌ | ✅ 新增 (§10.3 GCC 约束风格) |
| `qualified_call` 产生式 (`sys::read` 等) | ❌ | ❌ | ✅ 新增 (§10.4 sys:: namespace) |
| 2.5 级标注 | ❌ | ✅ (`*` `&` `+` `-` `!` `~` `++` `--` 一元前缀) | ✅ (保持) |

### 7.8 §12.3 附录优先级表 (修订)

| 维度 | 0.3.2 | 0.3.3 |
|------|-------|-------|
| 2 级 (后缀与前缀混) | ✅ (混) | ✅ 明确"后缀" |
| 2.5 级 | ❌ | ✅ 加 (显式一元前缀,右到左) |
| 15 级 `move` `clone` `move_to_thread` | ✅ | ✅ 删除 |
| 一元表 `mod` `unmod` | ✅ (错) | ✅ 删除 |

---

## 8. 0.3.2 → 0.3.3 升级影响分析

### 8.1 测试影响

- **现有 PASS 测试**: 27 baseline PASS 测试预期全保留 (0.3.3 是 spec 清晰化,不改 codegen 行为,只把现有 4 个 builtin (`move/clone/free/mod/unmod` 整合到 §11.0.1, per runtime-architecture §5.1)
- **现有 FAIL 测试**:
  - **m0_36** `move(p) / free(p)` — 0.3.3 实施后预期翻 PASS (move + free builtin 签名完整收录 §11.0.1, codegen emit `emit_move_call` + `emit_free` ≈ 50 LOC)
  - 其他 20 FAIL 保持 (与 0.3.3 无关)
- **单元测试**: 505/505 预期保留 (codegen 改动局部)
- **e2e sample**: 7 sample 预期保留

**预期 baseline**: 27 → **28/48 PASS** (m0_36 翻)。

### 8.2 API 兼容

- ✅ **无变化** (0.3.3 是 spec 概念重新分类, 不暴露公共 API)

### 8.3 ABI 兼容

- ✅ **无变化** (0.3.3 生成的 LLVM IR 与 0.3.2 生成的 IR 在功能上等价;
  0.3.3 把 6 个 builtin (`alloc/free/move/clone/mod/unmod`) 整合到 §11.0.1, codegen emit 路径不变)
- ✅ **无 ABI 变化** for current users — stdlib 走 `extern "C"` 是 0.3.2 以来既有机制; 0.3.3 + runtime-arch 集成仅是 spec 修订, 实际生成的 LLVM IR 不变 (stdlib 的 `print` / `strlen` 等仍经 C runtime 提供)
- 注: 0.4.0+ 首次自举后 (per runtime-arch §9.3), ABI 会随着 `lib/*.uc` 逐文件替换 `lib/uc_runtime.c` 而逐步变化

### 8.4 运行时行为

- ✅ **无 breaking change** (0.3.3 + runtime-architecture 是 spec 清晰化)
- ✅ 现有合法 UltraCPP 程序预期**全部**保持编译/运行行为不变
- ✅ `mod(ref)` / `unmod(ref)` / `move(p)` / `clone(s)` 调用语法不变; 但 mod/unmod 现在有 IR marker emit (per runtime-arch §3.2)

### 8.5 编译器实现 *(runtime-architecture 集成修订)*

- **codegen.c**: ~100-115 LOC (per runtime-architecture §5.1 + §10.3)
  - `BUILTIN_SIGS[]` 简化为 **6 项** (alloc/free/move/clone/mod/unmod); 移除 stdlib 14 项; 移除 `null`
  - `emit_move_call()` (~25 LOC) — IR 标记所有权 + 加载 + 返回新 owner
  - `emit_clone_call()` (~25 LOC) — alloc + memcpy + 返回新副本
  - `emit_free()` (~10 LOC, NEW per runtime-arch §3.2) — emit `call @free` + 标记 freed
  - `emit_mod_marker()` + `emit_unmod_marker()` (~5 LOC each, NEW per runtime-arch §3.2) — emit `@uc_borrow_mod_enter` / `@uc_borrow_mod_exit` IR marker
  - `intrinsic_call` codegen 处理 (~5 LOC each × 4 = ~20 LOC, NEW per runtime-arch §5.2) — `UC_EXPR_IS_NULL` → `icmp eq i8* %p, null`; `UC_EXPR_SIZEOF` → 编译期常量; `UC_EXPR_ALIGNOF` → 编译期常量; `UC_EXPR_NULL` → `i8* null`
  - `asm_block` codegen (~15 LOC, NEW per §10.3) — 翻译 GCC 约束风格 `asm { "syscall" : "=a"(r) : ... }` 为 LLVM `inline asm` intrinsic
  - `preprocessor_directive` 处理 (~10 LOC, NEW per §10.2) — 在 lexer / parser 立即求值, 不进入 AST
- **parser.c**: ~30-50 LOC (per runtime-architecture §3.4 + §3.5)
  - 新语法: `preprocessor_directive` 处理 (~20 LOC for 7 directives per §10.2)
  - 新语法: `asm_block` 解析 (~15 LOC for 4-segment GCC 约束语法 per §10.3)
  - 新语法: `qualified_call` 解析 (`sys::read(...)` ~10 LOC for §10.4)
  - 既有 builtin_function_call 从 19 项简化到 6 项 (~-15 LOC; net unchanged or slightly more)
- **ast.c / ast.h**: ~15-25 LOC
  - `UCExprCall` 加 `int is_builtin;` 字段 (per impl plan §5 Phase 2)
  - 加 `UC_EXPR_IS_NULL` / `UC_EXPR_SIZEOF` / `UC_EXPR_ALIGNOF` / `UC_EXPR_NULL` 4 个 intrinsic AST 节点 (~5 LOC each, per runtime-arch §5.2)
  - 加 `UC_EXPR_ASM` (asm block) AST 节点 (~5 LOC, per §10.3)
- **测试基础设施**: 无变化
- **总预估**: ~150-200 LOC (per runtime-architecture integration plan); 相比 0.3.3 初稿 (~50-65 LOC) 增加 ~85-135 LOC, 主要来自 stdlib 不再借 FFI / 表达化 preprocessor / asm / sys:: namespace

### 8.6 spec 本身 *(runtime-architecture 集成修订)*

- **新章节**: 5 个 (§7.0 + §4.13.5 + §11.0 重分组 + §10.2 预编译宏 + §10.3 内联汇编 + §10.4 sys:: namespace = 实际 6 个)
- **修订章节**: 7 个 (§4.1 + §7.8 + §4.9/§4.10 + §11 标题 + §12.1 + §12.3 + §10.1 extern "C" = 实际 7 个)
- **总改动**: ~480 行 spec 文本新增/修订 (runtime-architecture 集成新增 ~130 行覆盖 §10.2-§10.4)

---

## 9. Spec 0.3.3 Release Notes (正式版) *(runtime-architecture 集成)*

> 目标文件: `docs/UltraCPP-v0.3.3-release-notes.md` (0.3.3 发布时新建)

```markdown
# UltraCPP 0.3.3 Release Notes

**Release date**: 2026-08-XX (待定)
**Source spec**: `docs/UltraCPP-v0.3.3-spec-zh-CN.md`
**Base spec**: `docs/UltraCPP-v0.3.2-spec-zh-CN.md`

## Spec 修订

### 0.3.3 性质: Spec 清晰化 (clarification), 非 bug fix

0.3.3 是 **minor spec 修订**, 目的是:
1. 解决 §4.1 优先级表与 §4.9/§4.10 + §7.4/§7.5 之间的**内部矛盾** (mod/unmod/move/clone 性质)
2. 引入**运算符 vs builtin 函数**清晰分类 (§7.0)
3. 完整化 **deref 语义边界** (§4.13.5 新增)
4. **重新分组** builtin 签名表 (§11.0.1 + §11.0.2 双分类; 6 项语言原语 builtin + UltraCPP stdlib 路径 `lib/*.uc`, per `.dev/drafts/0.3.0-runtime-architecture.md` §5)
5. 引入**跨平台预编译宏** (§10.2 新增, `@ifdef` C 风格, per runtime-arch §7)
6. 引入**内联汇编机制** (§10.3 新增, `asm { ... }` GCC 风格, per runtime-arch §8)
7. 引入 **`sys::` 系统调用 namespace** (§10.4 新增, per runtime-arch §6)
8. 同步 §12.1 EBNF 与 §12.3 附录表

**无 bug fix**, **对现有合法程序无破坏性**, **生成的 LLVM IR 与 0.3.2 等价** (仅 spec 修订).

### 新增章节

- **§7.0 概念清晰化:运算符 vs builtin 函数** (5 子节)
  - §7.0.1 运算符 (§4.1 优先级表)
  - §7.0.2 builtin 函数 (§11.0.1 签名总表 + intrinsic + UltraCPP stdlib 路径 `lib/*.uc`)
  - §7.0.3 关键澄清 (mod/unmod/move/clone 重新分类 + mod/unmod 是 builtin IR marker, **非**「编译期决策、无 runtime」, per runtime-arch §3.2)
  - §7.0.4 历史回顾 (0.2.0 → 0.3.3, 加 runtime-architecture 集成行)
  - §7.0.5 交叉引用
- **§4.13.5 deref 语义** (6 子节)
  - §4.13.5.1 基本 deref `*p`
  - §4.13.5.2 复合 deref (`&*p` / `*&x` / `**pp` / `p->field`)
  - §4.13.5.3 与 §3.5 指针类型关系
  - §4.13.5.4 优先级 (2.5 级, 与 `*` 乘法同名异义)
  - §4.13.5.5 错误情况 (`InvalidDereferenceError`)
  - §4.13.5.6 交叉引用
- **§11.0.1 builtin 语言原语** (新分类, **6 项**, per runtime-arch §5.1: `alloc` / `free` / `move` / `clone` / `mod` / `unmod`; `null` 改为 intrinsic)
- **§11.0.2 UltraCPP stdlib 路径** (新分类, per runtime-arch §3.1 + §5.3: 替换 0.3.2 原 14 项 stdlib 约定, 全部由 UltraCPP 自己写在 `lib/*.uc`)
- **§10.2 预编译宏机制** (新章节, C 风格 `@ifdef` / `@if defined()` / `@else` / `@end`, per runtime-arch §7)
- **§10.3 内联汇编机制** (新章节, GCC 约束风格 `asm { "syscall" : ... }`, per runtime-arch §8)
- **§10.4 `sys::` 系统调用 namespace** (新章节, 跨平台系统调用抽象, per runtime-arch §6)

### 修改章节

- §4.1 运算符优先级表 — 加 2.5 级 + 删 15 级 + 改注脚 (mod/unmod 是 builtin 函数)
- §4.9 `mod()` 表达式 — 加 §7.0 + §11.0.1 引用; **注: mod 是 builtin, emit IR marker** (per runtime-arch §3.2, 不再是"编译期决策无 runtime")
- §4.10 `unmod()` 表达式 — 加 §7.0 + §11.0.1 引用; **注: unmod 是 builtin, emit IR marker** (同上)
- §7.3 `free(p)` — 加 §11.0.1 引用; **注: free 是 builtin, 不再走 `extern "C"` FFI** (per runtime-arch §3.2)
- §7.8 引用 `&T` — 加 §4.13.5 + §7.0 引用 (`&` 多重身份)
- §10.1 `extern "C"` 块 — 加强「不用于 stdlib」原则 (per runtime-arch §3.1)
- §11 标题 — 加 §11.0.1 / §11.0.2 双分类引用 (新计数: 6 项语言原语 + UltraCPP stdlib 路径)
- §12.1 完整 EBNF — 加 `builtin_function_call` 产生式 (**6 项**语言原语) + `intrinsic_call` (4 项) + `preprocessor_directive` + `asm_block` + `qualified_call` (5 个新产生式, per runtime-arch + §10.2-§10.4)
- §12.3 附录优先级表 — 与 §4.1 同步修订 (加 2.5 级 + 删 15 级)

### 跨章节引用更新

~20 处 (见本文档 §5 表): §3.7 struct / §4.1 / §4.9 / §4.10 / §7 标题 / §7.2-§7.8 / §10.1-§10.5 / §11 标题 / §11.0.1 + §11.0.2 / §12.1 / §12.3

### 关联决策文档

- `.dev/drafts/0.3.0-runtime-architecture.md` — runtime 架构与 BUILTIN_SIGS[] 划分决策 (本文档的 §11.0 + §10.2-§10.4 修订依据)

### 不变

- 所有 27 个 P0/P1 baseline PASS 测试预期保留
- 单元测试 505/505 预期保留
- m0_42 / m0_41 (0.3.2 修复) 仍 PASS
- m0_36 `move(p)` + `free(p)` 预期从 runtime_fail → PASS (0.3.3 §11.0.1 move + free 完整 emit)
- API 兼容、生成的 LLVM IR 与 0.3.2 等价
- 现有 27 PASS 程序预期**全部**保持编译/运行行为不变
```

---

## 10. Spec 维护 Checklist (供 spec maintainer 用)

> 以下是 spec 维护者按本草案应用 spec 修订时的 checklist。**plan 阶段不执行**, 留给 spec maintainer。

- [ ] **Step 1**: 复制 0.3.2 spec 文件到 0.3.3 working copy
  - `cp docs/UltraCPP-v0.3.2-spec-zh-CN.md docs/UltraCPP-v0.3.3-spec-zh-CN.md`
- [ ] **Step 2**: 在 §7 标题后、§7.1 前插入 §7.0 完整内容 (见本文档 §3.1.2)
  - [ ] §7.0.1 运算符
  - [ ] §7.0.2 builtin 函数
  - [ ] §7.0.3 关键澄清 (mod/unmod/move/clone 函数化)
  - [ ] §7.0.4 历史回顾
  - [ ] §7.0.5 交叉引用
- [ ] **Step 3**: 在 §4.13.4 后插入 §4.13.5 完整内容 (见本文档 §3.2.2)
  - [ ] §4.13.5.1 基本 deref
  - [ ] §4.13.5.2 复合 deref (4 小节)
  - [ ] §4.13.5.3 与 §3.5 关系
  - [ ] §4.13.5.4 优先级
  - [ ] §4.13.5.5 错误情况
  - [ ] §4.13.5.6 交叉引用
- [ ] **Step 4**: 修改 §4.1 优先级表 (见本文档 §4.1.2) — 加 2.5 级 + 删 15 级 + 改注脚
- [ ] **Step 5**: 修改 §4.9 mod (见本文档 §4.3.2) — 加 §7.0 + §11.0.1 引用; **注 mod 现在是 builtin IR marker, per runtime-arch §3.2**
- [ ] **Step 6**: 修改 §4.10 unmod — 加 §7.0 + §11.0.1 引用; **注 unmod 现在是 builtin IR marker, per runtime-arch §3.2**
- [ ] **Step 7**: 修改 §7.8 引用 (见本文档 §4.2.2) — 加 §4.13.5 + §7.0 引用
- [ ] **Step 8**: 修改 §10.1 `extern "C"` 块 (per runtime-arch §3.1) — 强化「不用于 stdlib」原则
- [ ] **Step 9 (新)**: 修改 §10.1 之后插入 §10.2 (新) 预编译宏机制 (见本文档 §3.4.1) — C 风格 `@ifdef` 语法 + 预定义宏表
- [ ] **Step 10 (新)**: 修改 §10.1 之后插入 §10.3 (新) 内联汇编机制 (见本文档 §3.4.1) — GCC 约束风格 `asm { "..." : ... }` + 约束字符表
- [ ] **Step 11 (新)**: 修改 §10.1 之后插入 §10.4 (新) `sys::` 系统调用 namespace (见本文档 §3.4.1) — 跨平台 API 表 + 目录组织
- [ ] **Step 12**: 修改 §11 标题 (见本文档 §4.4.2) — 加 §11.0.1 (6 项语言原语) + §11.0.2 (UltraCPP stdlib 路径 `lib/*.uc`) 双分类引用
- [ ] **Step 13**: 修改 §11.0 builtin 签名表 (见本文档 §3.3.2, runtime-architecture 集成) — 双分类 §11.0.1 + §11.0.2
  - [ ] §11.0.1 builtin 语言原语 (**6 项**: alloc / free / move / clone / mod / unmod; `null` 退到 intrinsic)
  - [ ] §11.0.2 UltraCPP stdlib 路径 (`lib/*.uc`: print/string/memory/math/alloc/sys/...) — 替代 0.3.2 原 14 项 stdlib 约定表
  - [ ] §11.0.3 codegen 集成 (**BUILTIN_SIGS[] 6 项**, 无 `is_type_parametric` 字段; stdlib 不进)
  - [ ] §11.0.4 添加新 builtin 流程 (注意新增 intrinsic 路径: `null` / `is_null` / `sizeof` / `alignof` 走 AST intrinsic 节点而非 BUILTIN_SIGS[])
  - [ ] §11.0.5 交叉引用 (加 §10.2 / §10.3 / §10.4 引用)
- [ ] **Step 14**: 修改 §11.1 - §11.6 (各加 §11.0.2 双向引用; 或者改为指明 `lib/*.uc` 路径)
- [ ] **Step 15**: 修改 §12.1 EBNF (见本文档 §4.5.2) — 加 `builtin_function_call` (**6 项**只) + `intrinsic_call` (4 项) + `preprocessor_directive` + `asm_block` + `qualified_call` (5 个新产生式)
- [ ] **Step 16**: 修改 §12.3 附录优先级表 (见本文档 §4.6.2) — 加 2.5 级 + 删 15 级 + 删 `mod/unmod` 一元表行
- [ ] **Step 17**: 跨章节引用更新 (见本文档 §5) — §3.7 / §4.1 / §4.9 / §4.10 / §7 / §7.2-§7.8 / §10.1-§10.5 / §11 / §11.x / §12.1 / §12.3 加新引用
- [ ] **Step 18**: §0 / §版本历史 加 0.3.3 changelog 条目 (引用 `.dev/drafts/0.3.0-runtime-architecture.md` 作为依据)
- [ ] **Step 19**: 全文 search "运算符" / "builtin" / "mod/unmod" / "move/clone" 核对引用完整
- [ ] **Step 20**: 全文 search "2.5 级" / "15 级" 核对 §4.1 / §12.3 同步
- [ ] **Step 21**: 全文 search "7 项" / "14 项" / "18 项" 核对 (剩余出现的应只在历史回顾与原因表中, 不出现在新建文档状态表)
- [ ] **Step 22**: 全文 search "@ifdef" / "asm {" / "sys::" 核对 §10.2-§10.4 子节已加
- [ ] **Step 23**: markdownlint / 拼写检查
- [ ] **Step 24**: 章节编号核对 (§7.0 / §4.13.5 / §10.2-§10.4 / §11.0.1 / §11.0.2 不与现有编号冲突)
- [ ] **Step 25**: 内部 review (spec 维护者 + implementer 互审)
- [ ] **Step 26**: commit 到 spec 0.3.3 release

---

## 11. Spec 0.3.2 → 0.3.3 兼容矩阵 *(runtime-architecture 集成)*

| spec 0.3.2 写法 | 0.3.3 + runtime-arch 行为 | 兼容性 | 备注 |
|----------------|---------------------------|--------|------|
| `mod(ref)` | 同 0.3.2 (合法 builtin 函数调用); **现在 emit IR marker** | ✅ | §11.0.1 (per runtime-arch §3.2 提升), 行为不变 |
| `unmod(ref)` | 同 0.3.2; **现在 emit IR marker** | ✅ | §11.0.1 (per runtime-arch §3.2 提升) |
| `move(p)` | 同 0.3.2 | ✅ | §11.0.1 (0.3.3 完整 emit ~25 LOC) |
| `clone(s)` | 同 0.3.2 | ✅ | §11.0.1 (0.3.3 完整 emit ~25 LOC) |
| `null` | 同 0.3.2 (codegen emit `i8* null` 常量) | ✅ | **intrinsic** (per runtime-arch §5.2, 不进 §11.0.1) |
| `is_null(p)` | 同 0.3.2 (codegen emit `icmp eq`) | ✅ | **intrinsic** (per runtime-arch §5.2) |
| `sizeof(T)` | 同 0.3.2 (编译期常量) | ✅ | **intrinsic** (per runtime-arch §5.2) |
| `alignof(T)` | 同 0.3.2 (编译期常量) | ✅ | **intrinsic** (per runtime-arch §5.2) |
| `alloc(T)` | 同 0.3.2 | ✅ | §11.0.1 (A 类 builtin) |
| `free(p)` | 同 0.3.2 (现为 builtin, 不再走 extern "C") | ✅ | §11.0.1 (per runtime-arch §3.2 提升) |
| `*p` (deref) | 同 0.3.2 | ✅ | §4.13.5 (0.3.3 显式语义) |
| `**pp` (多级 deref) | 同 0.3.2 | ✅ | §4.13.5.2.3 |
| `&*p` / `*&x` | 同 0.3.2 (编译器优化) | ✅ | §4.13.5.2 |
| `p->field` | 同 0.3.2 | ✅ | §4.13.5.2.4 |
| `*a` 解引用 vs `a * b` 乘法 | 同 0.3.2 (语法区分) | ✅ | §4.1 (2.5 级 vs 3 级) |
| `&x` addr-of vs `a & b` 按位与 | 同 0.3.2 (语法区分) | ✅ | §4.1 (2.5 级 vs 8 级) |
| `int x = factorial(5);` | 同 0.3.2 | ✅ | §6.2.1 + §11.0 |
| `*view = payload;` | 同 0.3.2 (S2 + S4 修复) | ✅ | m0_42 仍 PASS |
| `abs_int(-5)` | 同 0.3.2 (0.3.2 S5 修复) | ✅ | m0_41 仍 PASS; 但 0.3.3 + runtime-arch 推荐改用 `lib/math.uc` `uc_abs` (UltraCPP 自带) |
| `@ifdef(...)` 预编译宏 | ❌ 0.3.2 无, 0.3.3 新增 | ✅ 新 | §10.2 (per runtime-arch §7) |
| `asm { ... }` 内联汇编 | ❌ 0.3.2 无, 0.3.3 新增 | ✅ 新 | §10.3 (per runtime-arch §8) |
| `sys::read(...)` 系统调用 namespace | ❌ 0.3.2 无, 0.3.3 新增 | ✅ 新 | §10.4 (per runtime-arch §6) — 用户需 `import "lib/sys/sys.uc"` |
| EBNF `unary_expression` | 不变 (0.3.3 仅加 `builtin_function_call` + 4 其他产生式) | ✅ | §12.1 |
| §4.1 表查 `move` 优先级 | ❌ 0.3.2 有 15 级, 0.3.3 删 15 级 | ✅ 新 | `move` 是函数, 不查表 |
| §11.0 单分类 18 项 | ❌ 0.3.2 单表, 0.3.3 双分类 (§11.0.1 6 + §11.0.2 stdlib 路径) | ✅ 新 | 双分类, 但 stdlib 走 lib/*.uc 不进 BUILTIN_SIGS[] |
| BUILTIN_SIGS[] 含 stdlib | ❌ 0.3.2 含 14 项 stdlib, 0.3.3 移到 `lib/*.uc` | ✅ 新 | per runtime-arch §3.1 + §5.3 (UltraCPP 不接 libc) |
| BUILTIN_SIGS[] 含 `null` | ❌ 0.3.2/0.3.3 初稿含, 0.3.3 + runtime-arch 移除 | ✅ 新 | `null` 是 intrinsic, 不进 BUILTIN_SIGS[] (per runtime-arch §5.2) |
| codegen emit `UC_EXPR_MOVE` | ❌ 0.3.2 缺, 0.3.3 实施 | ✅ 新 | ~25 LOC |
| codegen emit `UC_EXPR_CLONE` | ❌ 0.3.2 缺, 0.3.3 实施 | ✅ 新 | ~25 LOC |
| codegen emit `UC_EXPR_FREE` | ❌ 0.3.2 走 FFI, 0.3.3 + runtime-arch 提升为 builtin | ✅ 新 | ~10 LOC (per runtime-arch §3.2) |
| codegen emit `UC_EXPR_MOD` / `UC_EXPR_UNMOD` | ❌ 0.3.2 缺 / 0.3.3 初稿标"无 emit", 0.3.3 + runtime-arch 实施 IR marker | ✅ 新 | ~5 LOC each (per runtime-arch §3.2) |
| codegen `UC_EXPR_IS_NULL` / `UC_EXPR_SIZEOF` / `UC_EXPR_ALIGNOF` | ❌ 0.3.2 缺, 0.3.3 + runtime-arch 实施 (intrinsic 路径) | ✅ 新 | ~5 LOC each (per runtime-arch §5.2) |
| codegen `UC_EXPR_ASM` (asm block) | ❌ 0.3.2 缺, 0.3.3 新增 | ✅ 新 | ~15 LOC (per §10.3) |
| parser 处理 `@ifdef(...)` | ❌ 0.3.2 无, 0.3.3 新增 | ✅ 新 | ~20 LOC (per §10.2) |
| parser 处理 `sys::` namespace | ❌ 0.3.2 无, 0.3.3 新增 | ✅ 新 | ~10 LOC (per §10.4) |

**总兼容**: ✅ **完全向后兼容**。0.3.3 + runtime-arch 集成**不引入新语法层面的破坏性改动**; 但**新增语法** (`@ifdef` / `asm { ... }` / `sys::` namespace 调用) 是**新增能力**, 不与现有写法冲突。
m0_36 `move(p) / free(p)` 由 0.3.3 实施后翻 PASS (运行时 builtin 完整)。

---

## 12. 与 0.3.2 / 0.3.1 / S1 / S3 / S6-S9 关系

### 12.1 与 0.3.2 (S4 + S5 + bug fixes) 关系 *(runtime-architecture 集成)*

| 维度 | 0.3.2 (前序) | 0.3.3 (本文档, runtime-arch 集成) |
|------|-------------|-----------------------------------|
| **范围** | Bug fix + 概念补全 | Spec clarification + runtime-arch 集成 |
| **新增章节** | §4.8.1 + §6.2.1 + §11.0 (3 章节) | §7.0 + §4.13.5 + §11.0.1+§11.0.2 (3 章节重分组) **+ §10.2 宏 + §10.3 asm + §10.4 sys:: 命名空间** (3 新增, runtime-arch) |
| **修订章节** | §4.8 + §6.1 + §6.2 + §11 + §11.1-11.6 + §12.1 + §12.3 (10 个) | §4.1 + §4.9 + §4.10 + §7.3 + §7.8 + §10.1 + §11 + §11.x + §12.1 + §12.3 (10 个) |
| **触发** | m0_42 + m0_41 bug | §4.1 vs §4.9/§4.10 spec 内部矛盾 + runtime-arch 决策 (UltraCPP 不接 libc, stdlib 自带) |
| **代码改动** | codegen.c ~130 LOC + parser.c ~30 LOC + ast.c ~10 LOC | codegen.c ~100-115 LOC (BUILTIN_SIGS 6 项 + emit_move/clone/free/move_enter/exit + 4 intrinsic + asm) + parser.c ~30-50 LOC (宏/asm/sys::) + ast.c ~15-25 LOC (UC_EXPR_IS_NULL/SIZEOF/ALIGNOF/NULL/ASM) |
| **测试影响** | 27 → 29/48 PASS (m0_42 + m0_41 翻) | 27 → 28/48 PASS (m0_36 翻, m0_42/m0_41 保留); runtime-arch 不直接影响测试, 仅 spec 修订 |
| **新语法** | `(T)expr` C-style cast | `@ifdef(...)` (C 风格宏) + `asm { ... }` (内联汇编) + `sys::` namespace 调用 (3 个新增语法 per runtime-arch §6-§8) |
| **stdlib 路径** | 走 `extern "C"` libc FFI | 全部由 UltraCPP 自带 (`lib/*.uc`), **不接 libc** (per runtime-arch §3.1 + §9.2) |

### 12.2 与 0.3.1 (S2) 关系

| 维度 | 0.3.1 (S2 lvalue/rvalue) | 0.3.3 (本文档) |
|------|------------------------|---------------|
| **范围** | lvalue/rvalue 概念明确化 | deref 语义边界 + 概念分类 |
| **§4.13.x** | §4.13.1-§4.13.4 (lvalue/rvalue) | 加 §4.13.5 (deref 语义) — 在 §4.13.4 之后 |
| **协同** | S2 引入 lvalue 概念, 0.3.3 §4.13.5 在此基础上定义 deref 总是 lvalue | — |

### 12.3 与 S1 / S3 / S6 / S7 / S8 / S9 关系

| Gap | 标题 | 0.3.3 解决? | 推到版本 | 备注 |
|-----|------|-----------|---------|------|
| S1 | 一元 op 优先级完整化 | ✅ **0.3.3 解决** | — | §4.1 加 2.5 级 + 删 15 级 |
| S2 | lvalue / rvalue 概念明确化 | ✅ (0.3.1) | — | — |
| S3 | 一元 op 语义完整化 | ✅ **0.3.3 解决** | — | §4.13.5 deref 语义 |
| S4 | cast 显式化 | ✅ (0.3.2) | — | — |
| S5 | 函数返回类型推断 | ✅ (0.3.2) | — | — |
| S6 | FFI body 来源策略 | ❌ | 0.3.3+ / 0.4.0 | 重大议题, 涉及 `extern "C" { }` body 来源 |
| S7 | CJK 真支持 (标识符) | ❌ | 0.4.0 | 需 spec 重大修订 (lexer + parser) |
| S8 | 隐式 mod 规则全面化 | 部分 | 0.3.3+ | S2 解决 "隐式 mod 目标是 lvalue"; 0.3.3 §4.13.5 加 deref lvalue 路径; 0.3.3+ 剩余 |
| S9 | spec 风格修订 (markdownlint / 一致性) | ❌ | 0.3.3+ | 风格统一, 不影响语义 |

### 12.4 0.3.1 → 0.3.2 → 0.3.3 升级路径

```
0.3.0 spec
   ↓ (0.3.1 = S2 lvalue/rvalue)
0.3.1 spec (新增 §4.13.1-§4.13.4, 修订 §4.1/§4.6/§7.8/§12.1/§12.3)
   ↓ (0.3.2 = S4 cast + S5 返回类型 + bug fixes)
0.3.2 spec (新增 §4.8.1/§6.2.1/§11.0, 修订 §4.8/§6.1/§6.2/§11/§11.x/§12.1/§12.3)
   ↓ (0.3.3 = S1 优先级表 + S3 deref + mod/unmod/move/clone 函数化)
0.3.3 spec (新增 §7.0/§4.13.5/§11.0.1+§11.0.2, 修订 §4.1/§4.9/§4.10/§7.8/§11/§12.1/§12.3)
```

### 12.5 0.3.1 / 0.3.2 / 0.3.3 是独立版本, 不是修订关系

- **0.3.1** 仍是**完整** spec 版本, 自身**已发布**
- **0.3.2** 在 0.3.1 基础上**独立发布**新版本, **不修订** 0.3.1
- **0.3.3** 在 0.3.2 基础上**独立发布**新版本, **不修订** 0.3.2
- 0.3.3 spec 文件名 `UltraCPP-v0.3.3-spec-zh-CN.md`, 与 0.3.1 / 0.3.2 平级
- 0.3.1 / 0.3.2 spec 文件保持**不可变**

---

## 13. 实施依赖顺序 (spec → code → test)

```
1. spec 修订     ← 本文档第 3-6 章 (spec 维护者执行)
2. code 修订     ← 0.3.3 implementation plan 第 5 章 (implementer 执行)
3. 验证          ← 0.3.3 implementation plan 第 7 章 (verifier 执行)
4. release       ← 本文档第 9 章 (0.3.3 tag + 发布)
```

| 阶段 | 工作 | 执行者 | 输出 |
|------|------|--------|------|
| 1. spec 修订 | 加 §7.0 + §4.13.5 + §11.0.1+§11.0.2 + 改 §4.1/§4.9/§4.10/§7.8/§11/§12.1/§12.3 | spec 维护者 | `docs/UltraCPP-v0.3.3-spec-zh-CN.md` |
| 2. code 修订 | codegen.c 加 4 个 builtin (`move` / `clone` / `mod` / `unmod`) emit 80-100 LOC | implementer | `src-c/src/codegen.c` + `src-c/src/ast.c` 改动 |
| 3. 验证 | 27 baseline + m0_36 + 505 单元 + 7 e2e | verifier | 全部 PASS (28/48 baseline) |
| 4. release | 0.3.3 tag + 发布 notes | releaser | git tag 0.3.3 + release notes |

**顺序要求**: spec 必须先于 code (spec 是 code 的依据)。

---

## 14. 与 S6 / S7 / S9 后续 spec gap 关系

0.3.3 落地 S1 + S3 + mod/unmod/move/clone 函数化。剩余 3 个 spec gap:

| Gap | 标题 | 推到版本 | 备注 |
|-----|------|---------|------|
| S6 | FFI body 来源策略 | 0.3.3+ / 0.4.0 | 重大议题, 涉及 `extern "C" { }` body 来源 (libc? user-provided? builtin?) |
| S7 | CJK 真支持 (标识符) | 0.4.0 | 需 spec 重大修订 (lexer + parser) — UTF-8 标识符 |
| S8 | 隐式 mod 规则全面化 | 0.3.3+ | S2 解决 "隐式 mod 目标是 lvalue"; 0.3.3 §4.13.5 加 deref lvalue 路径; 剩余 (e.g. `r = v` 中 v 类型转换与 mod 交互) |
| S9 | spec 风格修订 (markdownlint / 一致性) | 0.3.3+ | 风格统一, 不影响语义 (e.g. 术语一致性、章节编号、引用格式) |

**总判断**: 0.3.3 落地后, **27 → 28/48 baseline PASS** (m0_36 翻)。剩余 20 FAIL 由其他 gap 处理, **与 0.3.3 无关**。

(详细讨论排队中, 等本次 0.3.3 落地后, 在 `.dev/drafts/0.3.0-m0-priority.md` §9.x 继续跟踪。)

---

## 15. Spec 文档清单 (用户用)

> 0.3.3 发布后, 用户需更新/新增以下文档:

| 文档 | 操作 | 路径 |
|------|------|------|
| `UltraCPP-v0.3.3-spec-zh-CN.md` | **新建** (基于 0.3.2 spec 应用本文档 §3-6 修订) | `docs/UltraCPP-v0.3.3-spec-zh-CN.md` |
| `UltraCPP-v0.3.3-release-notes.md` | **新建** (基于本文档 §9) | `docs/UltraCPP-v0.3.3-release-notes.md` |
| `UltraCPP-v0.3.2-spec-zh-CN.md` | **不变** (0.3.2 已发布, 保持不可变) | `docs/UltraCPP-v0.3.2-spec-zh-CN.md` |
| `UltraCPP-v0.3.1-spec-zh-CN.md` | **不变** | `docs/UltraCPP-v0.3.1-spec-zh-CN.md` |
| `UltraCPP-v0.3.0-spec-zh-CN.md` | **不变** | `docs/UltraCPP-v0.3.0-spec-zh-CN.md` |
| `README.md` | **更新** (版本号 0.3.2 → 0.3.3, changelog 加 §7.0/§4.13.5/§11.0.1+§11.0.2/§10.2/§10.3/§10.4) | `README.md` |
| `CHANGELOG.md` | **更新** (0.3.3 条目 + 引用 `.dev/drafts/0.3.0-runtime-architecture.md` 作为依据) | `CHANGELOG.md` (若存在) |

---

## 附录 A: 与 0.3.3 Implementation Plan 文档的章节对照表 *(runtime-architecture 集成)*

> 方便 spec 维护者与 implementer 互相对照

| 0.3.3 spec changes (本文档) | 0.3.3 implementation plan | 内容 |
|-----------------------------|--------------------------|------|
| §1 元信息 | §1 元信息 | 元数据 |
| §2 概述 | §2 Executive Summary | 0.3.3 范围/影响 |
| **§3.1 (§7.0 概念清晰化, 新)** | §4 现有 spec 章节交互分析 + §5 Phase 1 | spec 文本新增 (概念清晰化) |
| **§3.2 (§4.13.5 deref 语义, 新)** | §5 Phase 1 | spec 文本新增 (deref 语义) |
| **§3.3 (§11.0.1 + §11.0.2 双分类, runtime-arch 集成)** | §5 Phase 1 + §5 Phase 3 | **BUILTIN_SIGS[] 6 项语言原语 + §11.0.2 lib/*.uc stdlib** (per runtime-arch §5) |
| **§3.4 (§10.2 + §10.3 + §10.4 新子节, runtime-arch 新增)** | §5 Phase 3b + Phase 3c + Phase 3d | **C 风格预编译宏 + GCC 约束 asm + sys:: namespace** (per runtime-arch §6-§8) |
| §4.1 修改 §4.1 优先级表 | §5 Phase 1 | 加 2.5 级 + 删 15 级 + 改注脚 |
| §4.2 修改 §7.8 引用 | §5 Phase 1 | 加 §4.13.5 + §7.0 引用 |
| §4.3 修改 §4.9/§4.10 mod/unmod | §5 Phase 1 | 加 §7.0 + §11.0.1 引用 (**mod/unmod 现在是 builtin IR marker, per runtime-arch §3.2**) |
| §4.4 修改 §11 标题 | §5 Phase 1 | 双分类 §11.0.1 + §11.0.2 |
| §4.5 修改 §12.1 EBNF | §5 Phase 2 | **5 个新产生式**: `builtin_function_call` (6 项) + `intrinsic_call` (4 项) + `preprocessor_directive` + `asm_block` + `qualified_call` |
| §4.6 修改 §12.3 附录表 | §5 Phase 1 | 与 §4.1 同步修订 |
| §4.7 修改 §7.4/§7.5 move/clone | §5 Phase 1 + §5 Phase 4-5 | 加 codegen emit 说明 (`emit_move_call` / `emit_clone_call`) |
| **§4.8 (新增) 修改 §10.1 extern "C" + 加 §10.2/§10.3/§10.4** | **§5 Phase 3b + 3c + 3d** | **runtime-architecture 集成** |
| §5 跨章节引用更新 | (impl plan 独有 — code 视角) | spec 视角 |
| §6 EBNF 更新 | §5 Phase 2 | 5 个新产生式 (builtin_function_call / intrinsic_call / preprocessor_directive / asm_block / qualified_call) |
| §7 Before/After Diff | (spec changes 独有) | 8 维度对比, 含 runtime-arch 集成前/中/后 |
| §8 升级影响 | §7 测试重新评估 | baseline / API / ABI / runtime (**§8.5 codegen LOC ~100-115 + parser ~30-50 + ast ~15-25**) |
| §9 Release Notes | §9 0.3.3 Release Notes 草稿 | 正式 release notes 草稿 (含 §10.2/§10.3/§10.4 + §11.0.2 lib/*.uc) |
| §10 Spec 维护 Checklist | §11 实施 Checklist (impl 视角) | 各自 checklist (扩展到 26 步) |
| §11 兼容矩阵 | §7 测试重新评估 (子集) | 兼容写法表 (扩展, 含 `@ifdef` / `asm` / `sys::` 新语法行) |
| §12 与 0.3.2/0.3.1 关系 | §12 参考 + 文档互补关系 | 版本关系 (加 runtime-arch 一行) |
| §13 实施依赖顺序 | §8 实施时间线 | spec → code → test → release |
| §14 与 S6/S7/S9 关系 | §13 与其他 spec gap 关系 | 其他 gap 推到版本 |
| §15 Spec 文档清单 | (spec changes 独有) | 用户用文档清单 |
| (impl plan 独有) | §3 问题描述 | §4.1 vs §4.9/§4.10 矛盾 + deref 语义模糊 + §11.0 单分类 + codegen 缺 emit + §7.0 概念缺失 |
| (impl plan 独有) | §5 实施 Checklist (按 phase) | **10 phase 拆分** (runtime-arch 集成后: Phase 1-3 已有 + Phase 3b lib/*.uc + Phase 3c intrinsic + Phase 3d §10 新子节 + Phase 4-7 既有 + Phase 8-9 可选) |
| (impl plan 独有) | §6 Code 修改映射 | spec → src-c (~150-200 LOC 总计) |
| (impl plan 独有) | §8 实施时间线 | ~1-2 天 |
| (impl plan 独有) | **§10 风险评估** | **14 风险 + 缓解** (含 runtime-arch 集成相关新风险) |

---

## 附录 B: 文档引用清单

**本文档引用的所有文件路径**:

| 类型 | 绝对路径 |
|------|----------|
| 基础 spec (0.3.0) | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.0-spec-zh-CN.md` |
| 基础 spec (0.3.2, 待创建) | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.2-spec-zh-CN.md` |
| 基础 spec (0.3.3, 待创建) | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.3-spec-zh-CN.md` |
| 0.3.2 spec modifications | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.2-spec-modifications.md` |
| 0.3.2 implementation plan | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.2-implementation-plan.md` |
| 0.3.3 implementation plan | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.3-implementation-plan.md` (本文档配套) |
| 优先级文档 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.0-m0-priority.md` |
| m0_36 测试源 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/test/programs/baseline/m0_36_move_free.uc` |
| codegen 源 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/src-c/src/codegen.c` |
| ast 头 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/src-c/include/uc_ast.h` |
| 0.3.2 关联 commit | (待 0.3.2 milestone commit) |
| 0.3.3 关联 commit | (待 0.3.3 milestone commit) |
| Output spec 文件 (待创建) | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.3-spec-zh-CN.md` |
| Output release notes (待创建) | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/docs/UltraCPP-v0.3.3-release-notes.md` |

---

**草案结束。** 本文档**不修改任何 spec / 源码文件**, 仅作为 `orch-plan-writer` 阶段产出的 spec 文本变更记录。后续 `orch-implementer` 按 0.3.3 implementation plan §11 checklist 推进 code 修订, spec 维护者按本文档 §10 checklist 推进 spec 修订。
