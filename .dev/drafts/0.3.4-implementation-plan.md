# S6/S8/S9 spec gaps + lib/*.uc stdlib bootstrap Implementation Plan for UltraCPP 0.3.4

**Date**: 2026-08-21
**Status**: draft
**Owner**: orch-plan-writer
**关联 spec**: 0.3.3 → 0.3.4 修订（spec 文档新写：`docs/UltraCPP-v0.3.4-spec-zh-CN.md` + `docs/UltraCPP-v0.3.4-spec-en.md`）
**关联 spec 修订**: `.dev/drafts/0.3.4-spec-text-changes.md`（实施阶段新写，process doc 引用）
**关联 process doc**: `.dev/drafts/0.3.4-implementation-process.md`（实施阶段新写，镜像 0.3.3 process 模板）
**关联 upstream**:
- `.dev/drafts/0.3.3-implementation-process.md`（completed，14 commits）— 过程风格模板
- `.dev/drafts/0.3.3-implementation-plan.md`（superseded）— 仅作 commit 节奏参照（不复用结构）
- `.dev/drafts/0.3.0-runtime-architecture.md` — §11.0.1 双分类 / §9 自举路径 / §6 sys:: 设计 / §7 preprocessor / §8 inline asm
- `.dev/drafts/0.3.0-m0-priority.md` — S1-S9 spec gaps（0.3.4 仅取 S6/S8/S9）
- `.dev/plans/0.3.0-borrow-check-milestones.md` — M0-M5 路线图
- `.dev/drafts/0.3.3-spec-text-changes.md` — 0.3.3 spec 改动基线

**HEAD**: `7f44eab`（0.3.3 完整链路完成 + docs refresh）
**基线状态**: M0 **32/48 PASS / 16 FAIL**（per 0.3.3 实施末态，5 个 0.3.3 翻 PASS：m0_30 / m0_37 / m0_41 / m0_42 / m0_44）；C unit tests **505/505 PASS**
**Target baseline**: **36-40/48 PASS**（+4-8：m0_02/m0_03/m0_05/m0_06/m0_08/m0_16/m0_18/m0_20 + 部分 m0_25/m0_40 期望翻 PASS）
**Estimated effort**: M-L（10 commits，~580-950 LOC，预计 5-8 工作日跨多 agent session）

---

## §0. 元数据

| 字段 | 值 |
|---|---|
| 文档标题 | S6/S8/S9 spec gaps + lib/*.uc stdlib bootstrap Implementation Plan for UltraCPP 0.3.4 |
| 文档路径 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification/.dev/drafts/0.3.4-implementation-plan.md` |
| 文档性质 | **原始计划（draft）**：列出 10 commits 的工作清单 + 6 项用户决策 + 风险登记 |
| 上游 | `.dev/drafts/0.3.3-implementation-process.md`（completed）+ `.dev/drafts/0.3.0-runtime-architecture.md` + `.dev/drafts/0.3.0-m0-priority.md` §9.x |
| 工作目录 | `/home/zjtti/Coding/UltraCpp/.worktrees/borrow-check-verification` |
| 分支 | `feature/borrow-check-verification` |
| 当前 HEAD | `7f44eab` |
| 基线状态 | 32/48 PASS（66.7%）· 505/505 C 单元测试 |
| 目标基线 | 36-40/48 PASS（+4-8）· ~555/555 C 单元测试（+50） |
| 总 LOC | ~580-950（含 lib/*.uc UltraCPP 源码 + src-c/ codegen 修订 + spec 修订 + docs sync） |
| Commit 总数 | **10 commits**（10a-10e lib/*.uc 5 个并行 + 11a-11c codegen 串行 + 12 cleanup + 13 docs） |
| Agent 分组 | 6 个并行组（per Q6 决策） |

---

## §1. 用户决策一览（6 项硬约束）

0.3.4 启动前，orchestrator 与用户对齐 6 项决策（全部已"按推荐执行"批准）：

### D1 — UltraCPP 直接 bootstrap lib/*.uc（**不创建 `lib/uc_runtime.c`**）
**决定**：不创建 `lib/uc_runtime.c`（C bootstrap 14 项 builtin 临时文件）。
**理由**：runtime-architecture §9 Stage 1 直接用 `uc_compiler` 编译 `lib/*.uc`（UltraCPP 自实现 stdlib），不走 C 临时 bootstrap。当前 src-c/ 已采用 inline LLVM IR + libc 链接混合方案（per 0.3.3 process §2.1），临时 C bootstrap 已被证明不必要的假设（per 0.3.3 process §2.5）。
**影响**：`lib/uc_runtime.c` 在 git history 中**永不出现**；src-c/Makefile 不编译 lib/*.uc；lib/*.uc 用 `uc_compiler --build` 编译为 `libuc.a`（远期 0.4.0+ Stage 1 启动）。

### D2 — `abs_int` 完全从 BUILTIN_SIGS[] + `get_builtins_defs` 移除
**决定**：`abs_int` 从 `BUILTIN_SIGS[]`（`src-c/src/codegen.c:38` 当前第 1 项）完全删除；同时从 `get_builtins_defs()` 中删除 `@builtin_abs_int` inline IR 实现（21 行，per 0.3.3 process §2.1）。
**理由**：abs_int 行为简单（`abs(n) = n < 0 ? -n : n`），完全可以由 UltraCPP 自身实现（`lib/math.uc::uc_abs`）；保留 inline IR 等于把 stdlib 函数"植入"编译器，违背 runtime-architecture §3.1 "UltraCPP 不使用 libc 作为 runtime" 原则。
**影响**：codegen 新增 `emit_uc_abs_call` 路由函数（约 30 LOC）；`UCExprCall` 主流水中**新增分支**（`fn_name == "uc_abs"` → `emit_uc_abs_call`）；m0_41 测试链改为 `abs_int(-7)` → ... → `@uc_abs`（lib/math.uc 中定义）。
**回归保证**：lib/math.uc::uc_abs 行为必须与原 inline IR **完全一致**（per R2 风险缓解）。

### D3 — P3 spec gaps 范围 = S6 + S8 + S9（不推 S1/S2）
**决定**：0.3.4 spec 修订仅解决 `.dev/drafts/0.3.0-m0-priority.md` §9.x 中的 **S6**（FFI extern body 来源）+ **S8**（deref-assign 类型安全）+ **S9**（main 返回 N 实际行为 / exit code 8-bit 截断）。
**理由**：S1（`*` `&` `mod` `unmod` 一元优先级）已在 0.3.1 解决；S2（lvalue/rvalue 概念缺失）已在 0.3.3 解决；S4（C-style cast）已在 0.3.2 解决。S6/S8/S9 是 M0 baseline 中**最后 3 个未明 spec 缺口**，必须随 m0_41/m0_42/m0_30/m0_34/m0_36/m0_48 修复一并解决才能翻 PASS。
**影响**：spec 修订规模从 S1-S9 全集（250-350 LOC）缩小到 S6/S8/S9 三个缺口（80-150 LOC）；codegen 修订范围对应缩减（30-50 LOC）。

### D4 — `lib/sync.uc`（mutex/atomic）推到 0.3.5 / M5
**决定**：0.3.4 不写 `lib/sync.uc`（mutex/atomic 包装）。
**理由**：M5（线程模型）是独立里程碑（per `.dev/plans/0.3.0-borrow-check-milestones.md` §3.5），启动条件是 `shared` / `__thread` / `move_to_thread` 关键字全部 lexer/parser/codegen 完成（M1 范畴），0.3.4 阶段这些关键字尚未就绪。
**影响**：lib/ 目录在 0.3.4 阶段只增 5 个文件（print.uc + string.uc + memory.uc + math.uc + alloc.uc + sys/sys.uc + sys/raw.uc = 7 个），不增 `lib/sync.uc`；m5_t5/m5_t6 测试（M5 mutex/atomic 范畴）**不在 0.3.4 目标 baseline 范围**。

### D5 — P4（P3-5 pointer runtime）纳入 0.3.4 commit 11c
**决定**：`m0_41` / `m0_42` / `m0_30` / `m0_34` / `m0_36` 修复（deref-assign LHS 类型 + abs_int linker + call signature + pointer runtime）作为 commit **11c**，纳入 0.3.4。
**理由**：当前 baseline 32/48 中仍有 5 个测试卡在 pointer runtime 范畴；不解决这 5 个，0.3.4 baseline 提升受限（最多到 35-37，远低于 36-40 目标）。S8（deref-assign 类型安全）spec 缺口是这 5 个测试的根因，spec 修订（commit 11b）+ codegen 修订（commit 11c）必须**同 commit 链推进**。
**影响**：commit 11c 工作量 +80-120 LOC（deref-assign LHS 类型修复 + store 指令 emit 修正 + abs_int 调用签名修正）；与 commit 11b（spec 修订 S8）严格顺序（11b 先定 spec，11c 后改 codegen）；与 commit 11a（abs_int 路由到 uc_abs）独立可并行。

### D6 — 多 agent 分组并行（lib/*.uc 5 文件并行 + codegen 串行 + docs 1 agent）
**决定**：0.3.4 阶段用 3 类 agent 并行推进：
- **Group L（lib/*.uc，5 个并行 agent）**：10a / 10b / 10c / 10d / 10e 独立 commit，可同 session 内并行 dispatch
- **Group C（codegen，串行 3 agent）**：11a → 11b → 11c 顺序执行（前 commit 是后 commit 的依赖）
- **Group D（cleanup + docs，1 agent）**：12 → 13 合并到 1 个 agent 串行
**理由**：lib/*.uc 文件之间无依赖（每个文件都是独立 UltraCPP 源码模块）；codegen 修改之间有依赖（11a 依赖 10d 完成 lib/math.uc 存在，11c 依赖 11b spec 定型）；cleanup + docs 是收尾工作，不影响中间 commits。
**影响**：orchestrator 在 lib/*.uc 全部完成后停下验证 baseline + 决定是否进入 codegen 阶段（关键 checkpoint）；codegen 11a 后单独验证 m0_41 仍 PASS（abs_int 走新路径）；11c 后单独验证 m0_30/34/36/42 翻 PASS。

---

## §2. 架构发现（与 0.3.3 的衔接）

### §2.1 当前 src-c/ 状态（HEAD `7f44eab` 后）

```
src-c/
├── include/
│   ├── uc_ast.h             — AST 节点定义（含 UC_TYPE_MUTABLE_POINTER 死代码，per §2.6）
│   ├── uc_codegen.h
│   ├── uc_lexer.h
│   ├── uc_parser.h
│   └── uc_token.h           — keyword token 定义（含 UC_TOK_KW_MOD/UNMOD/SIZE/ALIGNOF 等 6 个新增）
├── src/
│   ├── ast.c
│   ├── codegen.c            — 主战场：codegen 修订都在这里
│   ├── lexer.c
│   ├── main.c               — CLI + link llc + gcc
│   ├── parser.c
│   └── token.c
├── tests/                   — C 单元测试（505/505 PASS）
└── Makefile                 — 仅编 src/*.c，不编 lib/*.uc
```

### §2.2 当前 BUILTIN_SIGS[]（src-c/src/codegen.c:38-40）

```c
static const builtin_sig_t builtin_sigs[] = {
    {"abs_int", "i32"},                                              /* [0] 0.3.4 移除 (D2) */
    {"alloc", "i8*"}, {"free", "void"}, {"move", "i8*"},             /* [1-3] A 类 builtin */
    {"clone", "i8*"}, {"mod", "void"}, {"unmod", "void"},            /* [4-6] A 类 builtin */
    {NULL, NULL}
};
```

**当前 7 项**（per 0.3.3 process §2.3）：
- `abs_int`：inline IR 完整实现（21 LOC，get_builtins_defs() 内部）
- 6 个 primitives：alloc/free/move/clone/mod/unmod（per runtime-architecture §5.1 + §3.2）

**0.3.4 之后 BUILTIN_SIGS[] 变 6 项**（移除 abs_int）。

### §2.3 当前 lib/ 实际状态

```
lib/
├── io.uc   (231 bytes, 9 行)  — UltraCPP 源码，sys$strlen + sys$write 包装（getValue() + print_str()）
└── math.uc (51 bytes, 3 行)   — UltraCPP 源码，只有 add 函数，无 abs_int
```

**两个文件均不被 src-c/Makefile 编译**（per 0.3.3 process §2.2 确认）。
**0.3.4 增 5 个文件**（10a-10e），均不编入 src-c/Makefile（per D1，UltraCPP 直接 bootstrap）。

### §2.4 codegen 当前 emit 函数清单（src-c/src/codegen.c）

| 函数 | 行号（估计）| 作用 | 0.3.4 处理 |
|---|---|---|---|
| `emit_*_intrinsic` × 4 | codegen.c:600-750（per 0.3.3 process §3.1 commit 3）| emit null/is_null/sizeof/alignof 4 个 intrinsic | **不动**（per 0.3.3 已就绪）|
| `emit_{move,clone,alloc,free}_call` × 4 | codegen.c:800-950（per 0.3.3 process §3.1 commit 6）| emit 4 个 primitive 函数调用 | **不动**（0.3.3 已就绪）|
| `emit_mod_enter` / `emit_mod_exit` × 2 | codegen.c:1000-1100（per 0.3.3 process §3.1 commit 5）| emit `@uc_borrow_mod_enter` / `@uc_borrow_mod_exit` IR marker | **不动**（0.3.3 已就绪）|
| `emit_uc_abs_call` | — | **新增**（commit 11a）| emit `call i32 @uc_abs(i32 %x)`（或加载 lib/math.uc 中符号）|
| `gen_syscall_call` | codegen.c:979-1020（per 0.3.3 process §2.1）| 处理 sys$* 前缀 → 直接映射 libc | **不动**（lib/io.uc 仍用 sys$strlen/sys$write）|
| `UCExprCall` 主流水 | codegen.c:1313-1345（per 0.3.3 process §2.1）| 4 级优先级链：sys$* → lookup_builtin_ret → lookup_extern_func → default i32 | **新增分支**：`fn_name == "uc_abs"` → `emit_uc_abs_call`（commit 11a）|

### §2.5 runtime-arch §9 bootstrap 路径（关键）

runtime-architecture §9 Stage 1：
```
src-c/ (C 实现的 uc_compiler)
    ↓ uc_compiler --build lib/print.uc → lib/print.o
    ↓ uc_compiler --build lib/string.uc → lib/string.o
    ↓ ... (5 files)
    ↓ ar rcs libuc.a lib/*.o
    → libuc.a (UltraCPP runtime 静态库)
```

**0.3.4 范围**：仅写 `lib/*.uc` 源码（10a-10e）；**不**用 uc_compiler 实际编译它们（编译链路是 0.4.0+ Stage 1 工作）。
**验证方式**：commit 10a-10e 单独验证「lib/*.uc 用 uc_compiler 解析通过」（PARSE-ONLY），不要求产出 libuc.a；src-c/Makefile 不变（baseline 32/48 不动）。

### §2.6 UC_TYPE_MUTABLE_POINTER 死代码位置

```
src-c/include/uc_ast.h:99-100  — 死类型定义（per .dev/drafts/0.3.0-mutable-pointer-todo.md）
src-c/src/ast.c:NNN            — 死节点构造（per grep）
src-c/src/codegen.c:NNN        — 死 emit 分支（per grep）
```

**commit 12 清理**：删除 UC_TYPE_MUTABLE_POINTER 节点构造 + emit 分支 + AST 类型定义（预计 -10-20 LOC 总计）。**不**影响 baseline（无 m0 测试触碰此类型）。

---

## §3. 修订后的 Commit 计划（10 commits）

### §3.0 总体框架

```
┌────────────────────────────────────────────────────────┐
│ Group L: lib/*.uc bootstrap (5 files, 5 commits)       │
│   10a lib/print.uc UltraCPP 版        [+50-80, 中]    │
│   10b lib/string.uc UltraCPP 版       [+40-60, 低]    │
│   10c lib/memory.uc UltraCPP 版       [+50-70, 中]    │
│   10d lib/math.uc::uc_abs + lib/alloc.uc [+15-20 + ~10, 低]│
│   10e lib/sys/sys.uc + lib/sys/raw.uc [+130-200, 高]  │
│   ⤷ 全部可并行（per D6）                            │
└────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────┐
│ Group C: codegen 修订 (3 commits, 串行)              │
│   11a codegen 移除 abs_int inline IR, emit_uc_abs_call│
│       [-21 + ~30, 中]                                 │
│   11b spec 修订 S6/S8/S9 + codegen S8 路径           │
│       [spec 80-150 + codegen 30-50, 中]               │
│   11c codegen deref-assign LHS 类型修复 (P3-5)        │
│       [+80-120, 中]                                   │
│   ⤷ 11a → 11b → 11c 顺序（11a 依赖 10d）            │
└────────────────────────────────────────────────────────┘
                          ↓
┌────────────────────────────────────────────────────────┐
│ Group D: cleanup + docs (2 commits, 1 agent)          │
│   12 cleanup UC_TYPE_MUTABLE_POINTER 死代码           │
│       [-10-20, 低]                                    │
│   13 docs sync (HANDOFF + AGENTS + README + spec en)  │
│       [200-400, 低]                                   │
└────────────────────────────────────────────────────────┘

总计：~580-950 LOC · ~555/555 C 单元测试（+50）· baseline 32→36-40/48（+4-8）
```

### §3.1 详细 commit 清单

| # | Commit | Files | Specific Changes | LOC | Verification | Depends on | Risk |
|---|---|---|---|---|---|---|---|
| **10a** | `lib(0.3.4): add lib/print.uc UltraCPP stdlib` | `lib/print.uc` (NEW) | 实现 `uc_print(s)`, `uc_print_num(n)`, `uc_print_float(f)` 三个函数；用 sys$write 包装（不直接 inline libc）| +50-80 | `uc_compiler lib/print.uc` 解析通过；m0_25 sys$write 仍 PASS（per 0.3.3 §3.5）| D1（无依赖）| **中**（首次 UltraCPP 自举，需校验 sys$write 包装正确性）|
| **10b** | `lib(0.3.4): add lib/string.uc UltraCPP stdlib` | `lib/string.uc` (NEW) | 实现 `uc_strlen(s)`, `uc_strcpy(dst, src)`, `uc_strcmp(a, b)`；不接 libc，全部 UltraCPP 字面量循环实现 | +40-60 | `uc_compiler lib/string.uc` 解析通过；m0_23 / m0_24 仍 PASS（字符串字面量）| D1 | **低**（纯字面量循环）|
| **10c** | `lib(0.3.4): add lib/memory.uc UltraCPP stdlib` | `lib/memory.uc` (NEW) | 实现 `uc_memcpy(dst, src, n)`, `uc_memmove(dst, src, n)`, `uc_memset(dst, val, n)`；用 alloc + 字节循环 | +50-70 | `uc_compiler lib/memory.uc` 解析通过 | D1 | **中**（需 alloc + 字节操作正确）|
| **10d** | `lib(0.3.4): uc_abs in lib/math.uc + lib/alloc.uc thin wrapper` | `lib/math.uc` (REWRITE) + `lib/alloc.uc` (NEW) | math.uc 重写：移除 add（保 m0 兼容）；新增 `uc_abs(n: int) -> int { return n < 0 ? -n : n; }`；alloc.uc 新增 `uc_alloc(size: int) -> i8*` 包装（薄层，主要是命名一致性）| +15-20 + ~10 | `uc_compiler lib/math.uc` 解析通过 + uc_abs 行为与原 inline IR 字节一致（per R2）| D1 + D2（abs_int 路径）| **低**（uc_abs 简单 + alloc 薄层）|
| **10e** | `lib(0.3.4): sys:: namespace + raw syscall (x86_64 only)` | `lib/sys/sys.uc` (NEW) + `lib/sys/raw.uc` (NEW) | sys.uc：实现 `sys::read/write/open/close/lseek/exit` 等 6-8 个包装；raw.uc：实现 `sys::raw_syscall(num, a1, a2, ...)` 用 inline asm x86_64 only；arm64 / riscv64 暂未实现（per R5）| +130-200 | `uc_compiler lib/sys/raw.uc` 解析通过（注意 asm { } 0.3.3 已就绪）| D1 + 0.3.3 commit 8（preprocessor + asm）| **高**（首次 syscall 自举 + asm 调试）|
| **11a** | `codegen(0.3.4): remove abs_int inline IR, emit_uc_abs_call` | `src-c/src/codegen.c` | (1) BUILTIN_SIGS[] 移除 `abs_int` entry；(2) get_builtins_defs() 移除 `@builtin_abs_int` 实现（21 行）；(3) 新增 `emit_uc_abs_call(...)` 函数（emit `call i32 @uc_abs(i32 %x)`）；(4) UCExprCall 主流水新增 `fn_name == "uc_abs"` 分支 | -21 + ~30 | m0_41_extern_c 仍 PASS（abs_int 走新 emit 路径 → 链接 libuc.a 时找到 @uc_abs）| 10d | **中**（关键回归点；per R2）|
| **11b** | `spec(0.3.4): close S6/S8/S9 spec gaps + codegen S8 path` | `docs/UltraCPP-v0.3.4-spec-zh-CN.md` + `docs/UltraCPP-v0.3.4-spec-en.md` + `src-c/src/codegen.c` | (1) spec §6.2 + §10 新增 S6 extern body 来源规则；(2) spec §4.13.5 + §4.6 修订 S8 deref-assign 类型安全（明确 `*p = X` 中 p 必须 owning ptr / X 必须 lvalue-compatible）；(3) spec §3.x main 退出修订 S9 exit code 8-bit 截断说明；(4) codegen `UC_EXPR_DEREF_ASSIGN` 节点（S8 路径占位） | spec 80-150 + codegen 30-50 | baseline 32/48 仍 PASS（spec 修订不改任何 m0 测试源代码）| 独立 | **中**（spec 修订风险主要在跨章节一致性）|
| **11c** | `codegen(0.3.4): deref-assign LHS type fix (P3-5 pointer runtime)` | `src-c/src/codegen.c` | (1) `UC_EXPR_DEREF` 节点 emit store 指令时类型 cast fix（i32 vs ptr）；(2) `UC_STMT_ASSIGN` LHS 为 `*p` 时类型检查 + emit；(3) `UC_EXPR_CALL` 主流水中 `abs_int` → `uc_abs` 链接修正（libuc.a 符号路径）；(4) `m0_30/34/36/42` deref-assign 测试链修复 | +80-120 | m0_30 / m0_34 / m0_36 / m0_42 翻 PASS（+4）| 11b（spec S8 定型）| **中**（per R3，类型路径复杂）|
| **12** | `cleanup(0.3.4): remove UC_TYPE_MUTABLE_POINTER dead code` | `src-c/include/uc_ast.h` + `src-c/src/ast.c` + `src-c/src/codegen.c`（引用清理） | 删除 UC_TYPE_MUTABLE_POINTER 节点构造 + emit 分支 + AST 类型定义；grep 确认 0 引用 | -10-20 | `make -C src-c test` 505/505 + baseline 36-40/48 不回归 | 独立 | **低**（纯 dead code 清理）|
| **13** | `docs(0.3.4): sync HANDOFF + AGENTS + README + spec en translation` | `HANDOFF.md` + `AGENTS.md` + `README.md` + `docs/UltraCPP-v0.3.4-spec-en.md` | (1) HANDOFF §1 更新 baseline 36-40/48；(2) AGENTS.md spec 指针 0.3.3 → 0.3.4；(3) README.md 当前规范版本号；(4) spec en 从 zh-CN 翻译（per AGENTS.md 翻译规则）| docs 200-400 | 仅文档，不需 verification | 所有前面 commits | **低**（纯文档）|

### §3.2 关键产物（每 commit）

#### Commit 10a 关键产物

- **lib/print.uc** 内容（UltraCPP 源码）：
  ```
  // print.uc - UltraCPP stdlib print functions
  // 用 sys$write (libc 包装，per 0.3.3 gen_syscall_call) 而非直接 libc

  fn uc_print(s: i8*) -> int {
      int len = uc_strlen(s);
      return sys$write(1, s, len);
  }

  fn uc_print_num(n: int) -> int {
      // 数字 → 字符串（简化：先实现 0-9 + 负数）
      // TODO(0.4.0): 完整 itoa 实现
      return 0;
  }

  fn uc_print_float(f: float) -> int {
      // TODO(0.3.5): float 打印
      return 0;
  }
  ```
- **commit message 草案**：
  ```
  lib(0.3.4): add lib/print.uc UltraCPP stdlib

  - 实现 uc_print(s) 包装 sys$write
  - uc_print_num(n) stub（0.4.0 完整化）
  - uc_print_float(f) stub（0.3.5 完整化）
  - 不接 libc（per runtime-architecture §3.1）

  Refs: .dev/drafts/0.3.4-implementation-plan.md §3.1 (10a)
  ```
- **验证命令**：`bash -c 'cd src-c && make clean && make && ./build/uc_lexer --build ../lib/print.uc 2>&1 | head -20'`

#### Commit 10b 关键产物

- **lib/string.uc** 内容（UltraCPP 源码）：
  ```
  fn uc_strlen(s: i8*) -> int {
      int n = 0;
      while (s[n] != 0) {
          n = n + 1;
      }
      return n;
  }

  fn uc_strcpy(dst: i8*, src: i8*) -> i8* {
      int i = 0;
      while (src[i] != 0) {
          dst[i] = src[i];
          i = i + 1;
      }
      dst[i] = 0;
      return dst;
  }

  fn uc_strcmp(a: i8*, b: i8*) -> int {
      int i = 0;
      while (a[i] != 0 && b[i] != 0 && a[i] == b[i]) {
          i = i + 1;
      }
      return a[i] - b[i];
  }
  ```
- **commit message 草案**：`lib(0.3.4): add lib/string.uc UltraCPP stdlib (uc_strlen/strcpy/strcmp, no libc)` 

#### Commit 10c 关键产物

- **lib/memory.uc** 内容（UltraCPP 源码）：
  ```
  fn uc_memcpy(dst: i8*, src: i8*, n: int) -> i8* {
      int i = 0;
      while (i < n) {
          dst[i] = src[i];
          i = i + 1;
      }
      return dst;
  }

  fn uc_memmove(dst: i8*, src: i8*, n: int) -> i8* {
      // TODO: overlap 处理
      return uc_memcpy(dst, src, n);
  }

  fn uc_memset(dst: i8*, val: int, n: int) -> i8* {
      int i = 0;
      while (i < n) {
          dst[i] = val;
          i = i + 1;
      }
      return dst;
  }
  ```

#### Commit 10d 关键产物

- **lib/math.uc** 重写：
  ```
  // math.uc - UltraCPP stdlib math functions
  // 取代 0.3.3 inline IR @builtin_abs_int

  fn uc_abs(n: int) -> int {
      return n < 0 ? -n : n;
  }
  ```
- **lib/alloc.uc** 新建（薄层）：
  ```
  // alloc.uc - UltraCPP stdlib alloc thin wrapper
  // alloc 是 builtin（per spec §11.0.1）；此文件仅命名一致性

  fn uc_alloc(size: int) -> i8* {
      return alloc(size);  // delegate to builtin
  }
  ```
- **关键验证**：`uc_abs(-7)` 必须返回 7（与原 inline IR 行为字节一致）

#### Commit 10e 关键产物

- **lib/sys/sys.uc**（高 level wrapper）：
  ```
  // sys.uc - UltraCPP sys:: namespace (high-level)
  @ifdef(TARGET_OS_LINUX)
  fn sys::read(fd: int, buf: i8*, len: int) -> int {
      return sys::raw_syscall(0, fd, buf, len);  // SYS_read = 0
  }
  fn sys::write(fd: int, buf: i8*, len: int) -> int {
      return sys::raw_syscall(1, fd, buf, len);  // SYS_write = 1
  }
  // ... (6-8 函数)
  @end
  ```
- **lib/sys/raw.uc**（low level + inline asm）：
  ```
  // raw.uc - UltraCPP raw syscall (low-level)
  @ifdef(TARGET_ARCH_X86_64)
  fn sys::raw_syscall(num: int, a1: int, a2: int, a3: int, a4: int, a5: int, a6: int) -> int {
      int result;
      asm {
          "syscall"
          : "=a"(result)
          : "a"(num), "D"(a1), "S"(a2), "d"(a3), "r"(a4), "r"(a5), "r"(a6)
          : "rcx", "r11", "cc", "memory"
      }
      return result;
  }
  @end
  @if !defined(TARGET_ARCH_X86_64)
  @error("lib/sys/raw.uc 仅支持 x86_64，arm64/riscv64 推到 0.3.5")
  @end
  ```

#### Commit 11a 关键产物

- **src-c/src/codegen.c** 三处改动：
  ```c
  // (1) BUILTIN_SIGS[]: 移除 abs_int entry
  static const builtin_sig_t builtin_sigs[] = {
      /* [0.3.4 commit 11a] Removed abs_int per D2: migrate to lib/math.uc::uc_abs */
      {"alloc", "i8*"}, {"free", "void"}, {"move", "i8*"},
      {"clone", "i8*"}, {"mod", "void"}, {"unmod", "void"},
      {NULL, NULL}
  };

  // (2) get_builtins_defs(): 移除 @builtin_abs_int 21 行实现

  // (3) 新增 emit_uc_abs_call
  static void emit_uc_abs_call(CodeGen* g, UCExprCall* call) {
      // emit `call i32 @uc_abs(i32 %x)` —— 符号由 libuc.a 提供
      LLVMValueRef arg = gen_expr(g, call->args[0]);
      LLVMValueRef fn = LLVMGetNamedFunction(g->module, "uc_abs");
      // ... LLVMBuildCall2 ...
  }

  // (4) UCExprCall 主流水新增分支
  if (strcmp(fn_name, "uc_abs") == 0) {
      emit_uc_abs_call(g, call);
      return;
  }
  ```

#### Commit 11b 关键产物

- **spec 修订**（zh-CN + en 双语）：
  - §6.2 新增"extern body 来源规则"段落（**S6 关闭**）
  - §4.13.5 + §4.6 修订 deref-assign 类型安全（**S8 关闭**）
  - §3.x main 返回修订 exit code 8-bit 截断（**S9 关闭**）
- **codegen 修订**：
  - 新增 `UC_EXPR_DEREF_ASSIGN` 节点（S8 路径占位）
  - parser 解析 `*p = X` 时构造此节点（替代之前的 `UC_EXPR_DEREF` + `UC_STMT_ASSIGN` 二元组合）

#### Commit 11c 关键产物

- **codegen 三处修复**：
  ```c
  // (1) UC_EXPR_DEREF emit store 指令时类型 cast
  static void emit_deref_store(CodeGen* g, UCExprDeref* deref, LLVMValueRef rhs) {
      LLVMValueRef ptr = gen_expr(g, deref->operand);
      // 旧：LLVMBuildStore(g->builder, rhs, ptr)  // i32 vs ptr mismatch
      // 新：先 cast rhs 到指针基类型，再 store
      LLVMTypeRef base_type = LLVMGetElementType(LLVMTypeOf(ptr));
      if (base_type == LLVMInt32Type()) {
          LLVMValueRef rhs_i32 = LLVMBuildBitCast(g->builder, rhs, LLVMInt32Type(), "to_i32");
          LLVMBuildStore(g->builder, rhs_i32, ptr);
      }
      // ... 其他类型
  }

  // (2) UC_STMT_ASSIGN LHS 为 *p 时调用 emit_deref_store
  // (3) abs_int → uc_abs 链接修正：libuc.a 符号路径
  ```
- **预期回归**：m0_30 / m0_34 / m0_36 / m0_42 翻 PASS（+4）

#### Commit 12 关键产物

- **死代码清理**：
  ```
  src-c/include/uc_ast.h:99-100  — 删除 UC_TYPE_MUTABLE_POINTER 枚举
  src-c/src/ast.c:NNN           — 删除对应节点构造
  src-c/src/codegen.c:NNN       — 删除 emit 分支
  ```
- **grep 验证**：`grep -r "UC_TYPE_MUTABLE_POINTER" src-c/` 应仅返回 0 命中

#### Commit 13 关键产物

- **文档同步**：
  - `HANDOFF.md` §1 顶部"当前任务"更新：0.3.4 完成；下一步 M1 / 0.3.5
  - `HANDOFF.md` §1.1 进度表追加 10 行（commit 10a-13）
  - `AGENTS.md` spec 指针：`docs/UltraCPP-v0.3.3-spec-zh-CN.md` → `docs/UltraCPP-v0.3.4-spec-zh-CN.md`
  - `README.md` "Current Spec" 段版本号同步
  - `docs/UltraCPP-v0.3.4-spec-en.md` 从 zh-CN 全量翻译（per AGENTS.md 翻译规则）

---

## §4. 执行顺序

### §4.1 总体顺序（10 commits）

```
Group L (并行):
  10a → 10b → 10c → 10d → 10e

Group C (串行):
  11a (依赖 10d) → 11b (独立) → 11c (依赖 11b)
                  ↑                        ↑
                  └── (11b 实际可与 11a 并行) ┘

Group D (串行):
  12 → 13 (依赖所有前面)
```

### §4.2 详细时间线（推荐）

| 阶段 | Day | Commits | 并行度 | 备注 |
|---|---|---|---|---|
| **阶段 1：lib/*.uc 引导** | Day 1-2 | 10a / 10b / 10c / 10d / 10e | **5 个并行 agent** | 全部 PARSE-ONLY 验证；baseline 不变（32/48）|
| **Checkpoint 1** | Day 3 上午 | — | — | orchestrator 验证 5 文件 + baseline 32/48 未动；决定进入 codegen |
| **阶段 2a：codegen abs_int 路由** | Day 3 下午 | 11a | 1 agent | 关键回归点（m0_41）；baseline 32/48 应仍全 PASS |
| **阶段 2b：spec S6/S8/S9** | Day 4-5 | 11b | 1 agent | spec 修订 + UC_EXPR_DEREF_ASSIGN 占位；baseline 32/48 不变 |
| **阶段 2c：deref-assign 修复** | Day 5-7 | 11c | 1 agent | P3-5 pointer runtime；预期 baseline → 36-40/48（+4-8）|
| **Checkpoint 2** | Day 7 下午 | — | — | orchestrator 验证 baseline 36-40/48 + 决定进入 cleanup |
| **阶段 3：cleanup + docs** | Day 8 | 12 → 13 | 1 agent | 收尾；baseline 不变 |

### §4.3 依赖关系图

```
10a ─┐
10b ─┤
10c ─┤
10d ─┼─→ 11a ──┐
10e ─┘         │
               ├─→ 11c ─┐
       11b ────┘        │
                        ├─→ 12 ─→ 13
                        └─────→ 13
```

### §4.4 关键顺序约束

| edge | 原因 |
|---|---|
| **10d (lib/math.uc::uc_abs) → 11a (emit_uc_abs_call)** | lib/math.uc 必须先存在（即使只是 PARSE-ONLY 验证），11a 才能 emit `call @uc_abs` |
| **11b (spec S8) → 11c (deref-assign 修复)** | S8 spec 明确 `*p = X` 类型规则后，11c 才能 emit 正确 IR |
| **11a / 11b → 11c**（建议串行，但理论上可并行）| 11c 主要改 deref-assign，独立于 11a（abs_int 路由）；但 spec S6 修订可能影响 11c 中 libuc.a 符号路径，建议串行降低风险 |
| **11c → 12** | cleanup 必须最后做（避免 commit 12 时还有未解决的 codegen 引用）|
| **所有 → 13** | docs 是收尾，必须最后 commit |

### §4.5 不要的顺序（错误示例）

- ❌ 11a → 10d：11a emit `@uc_abs` 但 lib/math.uc 尚未定义 → m0_41 FAIL → baseline 32→31
- ❌ 11c → 11b：codegen 改了 deref-assign 但 spec 未明 → 与 0.3.4 原则冲突（spec-led codegen）
- ❌ 12 → 11c：cleanup 移除 UC_TYPE_MUTABLE_POINTER 后，11c 还可能在某个分支误引用

---

## §5. 测试策略

### §5.1 当前 baseline（HEAD `7f44eab`）

- **M0 测试套件**: 48 个 .uc 测试 → **32 PASS / 16 FAIL**
- **C 单元测试**: **505/505 PASS**
- **0.3.3 翻 PASS 测试**: m0_30 / m0_37 / m0_41 / m0_42 / m0_44（5 个）
- **0.3.3 实施后仍 FAIL**: m0_02 / m0_03 / m0_05 / m0_06 / m0_08 / m0_16 / m0_18 / m0_20 / m0_23 / m0_24 / m0_25 / m0_32 / m0_33 / m0_40 / m0_49（15 个）

### §5.2 预期 baseline 变化（0.3.4 完整实施后）

- baseline **预期 36-40/48 PASS**（+4-8）
- **预期翻 PASS 测试**：
  - m0_30（pointer deref）— **11c 修复**
  - m0_34（alloc/free）— **11c 修复**（deref-assign LHS）
  - m0_36（move keyword）— **11c 修复**（deref-assign LHS）
  - m0_42（unsafe block）— **11c 修复**（deref-assign LHS）
  - m0_02 / m0_03（int arithmetic）— **lib/print.uc 提供 print_num 后期望翻 PASS**
  - m0_25（string io）— **lib/print.uc + lib/string.uc 后期望翻 PASS**
- **不预期翻 PASS**：m0_05 / m0_06 / m0_08 / m0_16 / m0_18 / m0_20（位运算 + 函数 ABI 范畴，0.3.4 不解决）；m0_23 / m0_24（字符串字面量，已 PASS，0.3.4 不改）；m0_32 / m0_33（struct，P2 范畴，0.3.4 不解决）；m0_40（module include，P2 范畴）；m0_49（mixed comments，已 PASS）
- **不预期回归**：每 commit 后跑 baseline 验证；m0_01 必须保持 PASS

### §5.3 关键关注点

| 测试 | commit 影响 | 必须保持 |
|---|---|---|
| **m0_41 (extern "C" / abs_int)** | commit 11a 移除 `abs_int` inline IR → 改为 emit `call @uc_abs`；要求 lib/math.uc 存在 | **PASS**（关键回归点；per R2）|
| **m0_30 (pointer deref)** | commit 11c 修复 deref-assign store 类型 | **翻 PASS** |
| **m0_34 / m0_36 (alloc/free/move)** | commit 11c 修复 deref-assign LHS；commit 6 emit_*_call 路径 | **翻 PASS** |
| **m0_42 (unsafe block)** | commit 11c 修复 deref-assign LHS | **翻 PASS** |
| **m0_25 (string io)** | commit 10a lib/print.uc 提供 `uc_print` 包装 sys$write | **期望翻 PASS** |
| **m0_02 / m0_03 (int arithmetic)** | commit 10a lib/print.uc 提供 `uc_print_num` stub | **期望翻 PASS** |
| **m0_23 / m0_24 (string literal)** | commit 10b lib/string.uc 提供 `uc_strlen`（m0_23 用） | **保持 PASS** |
| **m0_50 (CJK identifier)** | 与 0.3.4 无关 | **PASS**（runner marker 机制）|
| **505/505 C unit tests** | 每 commit `make -C src-c test` | **保持 PASS**，0.3.4 末态 ~555/555（+50）|

### §5.4 新增 C unit test 用例（按 §3 commit 分组）

| commit | test file | 用例 | 数量 |
|---|---|---|---|
| 10a | 无新增（lib/print.uc 是 .uc 源码，非 C 单元测试）| — | 0 |
| 10b | 无新增 | — | 0 |
| 10c | 无新增 | — | 0 |
| 10d | `test_codegen.c` | `uc_abs` 行为测试（输入 -7 期望输出 7）+ BUILTIN_SIGS[] 无 abs_int entry | +2 |
| 10e | `test_codegen.c` | `sys::` namespace 函数 emit 路径（mock）+ raw_syscall asm 解析（mock）| +3 |
| 11a | `test_codegen.c` | `emit_uc_abs_call` 调用 `@uc_abs` 验证 + BUILTIN_SIGS[] 不再含 abs_int | +4 |
| 11b | `test_parser.c` + `test_codegen.c` | S6 extern body 路径 + S8 `UC_EXPR_DEREF_ASSIGN` 节点构造 + S9 main return 解析 | +5 |
| 11c | `test_codegen.c` | deref-assign store 类型 cast（i32 vs ptr）+ abs_int → uc_abs 链接符号修正 | +8 |
| 12 | `test_ast.c` | UC_TYPE_MUTABLE_POINTER 节点 0 引用验证（用 grep 替代）| +1 |
| 13 | 无新增（纯 docs）| — | 0 |

**总计 +23 用例**（C unit tests 505 → 528）。

**预期增加测试**：
- 10a-10e 阶段增加 ~27 个 lib/*.uc 的 PARSE-ONLY 测试（独立 .uc 文件，每个 lib/X.uc 一个测试程序），C unit tests 不计入
- **总 C unit tests: 505 → 528**（保守估计）；如果 10e 实现更复杂（多平台），可达 555

### §5.5 0.3.4 阶段不新增的测试

- **不新增 .uc 测试到 baseline 目录**（48 个 m0_* 套件不变）
- **新增 lib/*.uc 的 PARSE-ONLY 测试**（独立目录 `test/programs/lib_parse/`）：每个 lib/X.uc 一个测试程序，验证能被 uc_compiler 解析通过
- **不新增 integration 测试**（libuc.a 完整链接是 0.4.0+ Stage 1 工作）

---

## §6. 风险登记

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| **R1** | 首次 UltraCPP 自举（lib/*.uc 用 uc_compiler 编译）| 高 | commit 10a-10e 单独 PARSE-ONLY 验证；src-c/Makefile 不编 lib/*.uc → 不影响 baseline（32/48 不动）；每 commit 后跑 `make -C src-c test` 保持 505/505 |
| **R2** | `lib/math.uc::uc_abs` 中 `abs(n) = n < 0 ? -n : n` 与原 inline IR 行为不一致 | **高** | commit 11a 后立即跑 m0_41 验证；用例 `-7 / 0 / 7 / INT_MIN` 都要字节一致；原 inline IR 已在 0.3.3 测试中通过（m0_41 PASS）；**实施 agent 必跑：m0_41_extern_c.uc 通过 + `uc_abs(-7) == 7` 等 4 个 corner case** |
| **R3** | deref-assign 类型路径复杂（i32 vs ptr + owning ptr vs borrowed ptr）| 中 | commit 11c 拆为 2 sub-commit：(11c-i) `UC_EXPR_DEREF` 节点类型 cast fix；(11c-ii) `UC_STMT_ASSIGN` LHS 路径集成；每个 sub-commit 后跑 m0_30/34/36/42 验证 |
| **R4** | spec gap S8（隐式 mod）可能影响 codegen 既有路径 | 中 | commit 11b 后跑完整 baseline 回归（32/48 不动）+ 单元测试 505/505；若 m0_41/m0_42 regress，回退 spec 修订局部段落 |
| **R5** | lib/sys/raw.uc 用 inline asm syscall（跨平台 @ifdef）| 中 | commit 10e 先 x86_64 only（per D6）；arm64 / riscv64 推到 0.3.5；raw.uc 顶部加 `@error("仅支持 x86_64")` 当非 x86_64 时编译失败（明确告知用户）|
| **R6** | agent turn limit（10 commits 跨越多 session）| 中 | 每 commit 独立 verification + 跨 session handoff via HANDOFF.md；orchestrator 在每个 checkpoint（lib/*.uc 完成 / 11a 完成 / 11c 完成）停下确认；关键 commit（11a / 11c）单独 dispatch agent 不与其他 commit 混 |
| **R7** | 0.3.4 期间新发现的 spec gap（S30+）| 低 | 留 §8 open questions 区记录；不阻塞当前 commits；推迟到 0.3.5 spec 修订 |
| **R8** | 0.3.3 已有 baseline 32/48 回归 | 高 | 每 commit 后跑 `bash test/e2e/run_baseline.sh` 验证 baseline 不降；commit 11a 关键回归点（m0_41）必须 PASS；commit 11c 关键翻 PASS 点（m0_30/34/36/42）必须新增 PASS |
| **R9** | lib/*.uc 跨文件依赖（lib/string.uc 用 uc_strlen，lib/print.uc 用 uc_strlen）| 低 | lib/print.uc 直接 inline strlen 实现（不依赖 lib/string.uc）；lib/string.uc 自包含；lib/math.uc 自包含；lib/alloc.uc 依赖 builtin alloc；lib/sys/sys.uc 依赖 lib/sys/raw.uc |
| **R10** | lib/sys/raw.uc 中 `syscall` 指令 clobber list 不全（导致寄存器破坏）| 中 | 显式列 `rcx / r11 / cc / memory`（per runtime-architecture §8.1 + §8.2 GCC 约束）；commit 10e 实施 agent 必跑 lldb / gdb 验证寄存器状态 |
| **R11** | 0.3.4 期间 `uc_compiler` 自身 crash 在解析 lib/*.uc | 中 | lib/*.uc 写成简单 + 单文件 + 短函数（每文件 <100 行）；优先用 PARSE-ONLY 验证（不要求完整 codegen）；失败的 lib/*.uc 立即修复 |
| **R12** | 11b spec 修订与 en 翻译不同步 | 低 | commit 11b 同步 zh-CN + en 双语（per AGENTS.md 翻译规则）；实施 agent 用 spec translation 模板 |

---

## §7. 范围外（明确不做）

以下项**明确推迟**，0.3.4 实施不涉及：

- **完整 borrow check（M1-M5）** — DEFERRED 到 0.3.5+（per `.dev/plans/0.3.0-borrow-check-milestones.md`）
- **`lib/sync.uc`（mutex/atomic）** — DEFERRED 到 0.3.5（M5 启动时再写）
- **CJK 标识符真支持** — DEFERRED 到 0.4.0（per `.dev/drafts/0.3.0-m0-priority.md` §4.1 P1-5 deferred；当前 spec §2.4 不要求 CJK 支持）
- **自举 `uc_compiler` 自身** — DEFERRED 到 0.4.0+（Stage 2，per runtime-architecture §9.1）
- **`lib/t1*` 测试套件扩展** — DEFERRED 到 0.4.0（保留旧测试，不动）
- **Rust 端口 `src/`** — DEFERRED forever（已冻结，仅作历史参照）
- **m0_05 / m0_06 / m0_08 / m0_16 / m0_18 / m0_20 / m0_32 / m0_33 / m0_40 / m0_49 翻 PASS** — DEFERRED 到 0.3.5+（不在 0.3.4 目标 baseline 范围）
- **`lib/print.uc::uc_print_num` 完整 itoa** — DEFERRED 到 0.4.0（0.3.4 仅 stub）
- **`lib/print.uc::uc_print_float` 完整实现** — DEFERRED 到 0.3.5（0.3.4 仅 stub）
- **`lib/sys/raw.uc` arm64 / riscv64 支持** — DEFERRED 到 0.3.5（0.3.4 x86_64 only）
- **`lib/sync.uc` futex 实现** — DEFERRED 到 M5
- **spec §10 / §11 之外的章节修订** — DEFERRED 到下一个 spec 修订周期
- **C 单元测试完整覆盖所有 lib/*.uc 函数** — DEFERRED 到 0.4.0（libuc.a 阶段）
- **新错误码（UseAfterMove 等）— DEFERRED 到 M2**
- **`UC_TYPE_MUTABLE_POINTER` 之外的其他死代码** — DEFERRED 到 0.3.5 cleanup 阶段

---

## §8. 关联文档

### 上游（已存在）

- `.dev/drafts/0.3.3-implementation-process.md`（**completed**，14 commits）— 过程风格模板
- `.dev/drafts/0.3.3-implementation-plan.md`（**superseded**）— 仅作 commit 节奏参照，不复用结构
- `.dev/drafts/0.3.0-runtime-architecture.md`（**authoritative**）— §11.0.1 双分类 / §9 自举路径 / §6 sys:: / §7 preprocessor / §8 inline asm
- `.dev/drafts/0.3.0-m0-priority.md`（**authoritative for S6/S8/S9**）— §9.x spec gaps
- `.dev/drafts/0.3.0-mutable-pointer-todo.md`（**authoritative**）— UC_TYPE_MUTABLE_POINTER 死代码位置
- `.dev/plans/0.3.0-borrow-check-milestones.md`（**authoritative**）— M0-M5 路线图
- `.dev/drafts/0.3.3-spec-text-changes.md`（**completed**）— 0.3.3 spec 改动基线

### 当前状态文档

- `HANDOFF.md` — 顶块当前任务 = 0.3.4 规划
- `AGENTS.md` — 代理规则 + Git 权限 + 翻译规则 + 版本命名约定
- `README.md` / `README-zh-CN.md` — 项目自述
- `docs/UltraCPP-v0.3.3-spec-zh-CN.md` — 当前中文规范（基线）
- `docs/UltraCPP-v0.3.3-spec-en.md` — 当前英文规范（基线）

### src-c/ 架构参考（实施 agent 必读）

- `src-c/src/codegen.c:38-40` — BUILTIN_SIGS[] 7 项（0.3.4 末态 6 项）
- `src-c/src/codegen.c:295-330` — `get_builtins_decls()` + `get_builtins_defs()`（11a 删除 abs_int 实现）
- `src-c/src/codegen.c:979-1020` — `gen_syscall_call`（sys$ 处理；lib/io.uc 仍用）
- `src-c/src/codegen.c:1313-1345` — UCExprCall codegen 主流程（11a 新增 uc_abs 分支）
- `src-c/include/uc_ast.h:99-100` — UC_TYPE_MUTABLE_POINTER 死代码（commit 12 清理）
- `src-c/Makefile` — Phase 1 lexer-only build（不编 lib/*.uc）

### 测试参考

- `test/programs/baseline/m0_41_extern_c.uc` — abs_int 回归测试（关键关注，commit 11a 必保）
- `test/programs/baseline/m0_30_pointer_deref.uc` — pointer deref（11c 翻 PASS）
- `test/programs/baseline/m0_34_alloc_free.uc` — alloc/free（11c 翻 PASS）
- `test/programs/baseline/m0_36_move_keyword.uc` — move（11c 翻 PASS）
- `test/programs/baseline/m0_42_unsafe_block.uc` — unsafe block（11c 翻 PASS）
- `test/programs/baseline/m0_25_string_io.uc` — string + sys$write（10a 期望翻 PASS）
- `test/e2e/run_baseline.sh` — baseline 验证脚本（每 commit 后跑）

### 下游（本计划完成后需新写）

- `.dev/drafts/0.3.4-implementation-process.md`（**实施过程 ledger**，与 0.3.3 process 镜像模板；commit 推进时持续追加）
- `.dev/drafts/0.3.4-spec-text-changes.md`（spec 修订 diff，对应 11b 改动）
- `docs/UltraCPP-v0.3.4-spec-zh-CN.md`（中文规范，commit 11b 产出）
- `docs/UltraCPP-v0.3.4-spec-en.md`（英文规范，commit 11b 同步产出 per AGENTS.md 翻译规则）
- `docs/UltraCPP-v0.3.4-release-notes.md`（release notes，commit 13 产出）

---

## §9. Open Questions（实施 agent 必读）

> 0.3.4 启动前已对齐 6 项决策（per §1），下列问题**仅**影响 0.3.5+ 工作，不阻塞 0.3.4 当前 commits。

| # | 问题 | 影响 | 建议 |
|---|---|---|---|
| **OQ1** | 0.3.4 期间新发现的 spec gap（**S30+**）如何处理？| 不影响 0.3.4 baseline | 留 S30+ catalog 在 `.dev/drafts/0.3.4-process.md` §9.x；不阻塞当前 commits；0.3.5 spec 修订时一并解决 |
| **OQ2** | lib/*.uc 完整 PARSE-ONLY 测试要加多少？| 测试工作量 | 0.3.4 阶段建议至少 7 个（每 lib/X.uc 一个）；不强制完整覆盖 |
| **OQ3** | 0.3.4 spec en 翻译是先 zh-CN 定稿再 en，还是 commit 11b 双语同步？| 工作量分布 | per AGENTS.md 翻译规则：**双语同步**（commit 11b 同时产出 zh-CN + en）|
| **OQ4** | lib/sync.uc 推到 0.3.5 / M5；具体哪个？| 0.3.5 范围 | M5 启动条件 = 线程相关关键字 lexer/parser/codegen 全部完成（M1 范畴）；0.3.4 不预设 |
| **OQ5** | commit 11c deref-assign 修复是只在 spec 缺口（S8）层面修复，还是同时支持 `&*p` / `*&x` 恒等性？| 11c 工作量 | **仅修复 S8 主路径**（`*p = X` 类型正确）；`&*p` / `*&x` 恒等性是 P3-5 子任务，0.3.5+ 处理 |
| **OQ6** | 0.3.4 baseline 提升目标是 36/48 还是 40/48？| 验收标准 | 当前规划 **36-40/48**（区间）；下限 36 由 m0_30/34/36/42 翻 PASS 保底；上限 40 取决于 m0_02/03/25 翻 PASS（依赖 lib/print.uc + lib/string.uc 完整度）|

---

## §10. 修订历史

| 日期 | 版本 | 改动 | 作者 |
|---|---|---|---|
| 2026-08-21 | v1 | 初稿：基于 0.3.3 完成（HEAD `7f44eab`，baseline 32/48）+ 用户批准的 6 项决策（D1-D6）+ 10 commit 计划（10a-13）+ 风险登记 + 测试策略 + 范围外清单 + 关联文档 | orch-plan-writer |

> 本文件为**原始计划**（draft），0.3.4 实施推进时由 `.dev/drafts/0.3.4-implementation-process.md`（镜像 0.3.3 process 模板）持续追加修订历史 + 新发现。
> 修订模板：
>
> | 日期 | 版本 | 改动 | 作者 |
> |---|---|---|---|
> | YYYY-MM-DD | vN | commit X 完成 / 新发现 Y / 风险 Z 变化 / baseline 变化 | orch-XXX |

---

*最后更新：2026-08-21 — 初稿*
