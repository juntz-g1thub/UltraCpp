# UltraCPP 文档更新与整理计划

## TL;DR

> **任务**：整理项目文档到 `docs/` 目录，并更新规格文档以反映最新设计决定
>
> **更新内容**：
> 1. 创建 `docs/` 目录
> 2. 复制并更新规格文档
> 3. 修复 Rust 语法问题（C++ 风格统一）
>
> **估计工作量**：小

---

## Context

### 当前状态
- 规格文档位于：`.sisyphus/plans/UltraCPP-v0.1.0-spec-zh-CN.md`
- `docs/` 目录不存在
- 发现多处 Rust 风格语法需要修正为 C++ 风格

### 最新设计决定（2026-04-17）
1. **unique 默认化**：所有指针默认不可复制，赋值即转移所有权
2. **嵌入汇编语法**：确认使用 `unsafe {}` + `asm {}` 块语法
3. **clone() 函数**：添加显式克隆功能

---

## Work Objectives

### 必须完成
- [ ] 创建 `docs/` 目录
- [ ] 将更新后的规格文档写入 `docs/UltraCPP-v0.1.0-spec-zh-CN.md`
- [ ] 修复所有 Rust 语法问题

### 设计更新
- [ ] 更新指针所有权语义（第 3 章）
- [ ] 更新内存管理章节（第 7 章）
- [ ] 更新内联汇编语法（第 10 章）
- [ ] 添加 `clone` 关键字
- [ ] 修复 FFI 函数签名（C++ 风格）

---

## Rust 语法问题清单

| 位置 | 问题 | 修复 |
|------|------|------|
| 1.3 符号约定 | `fn(T) -> U` | 改为 `T(*)(U)` |
| 2.3 关键字 | 缺少 `clone` | 添加 `clone` |
| 3.2 指针类型 | 未说明所有权默认语义 | 添加说明 |
| 7.x 内存管理 | 未说明赋值即转移 | 重大更新 |
| 10.3 FFI 块 | `fn strlen(...)` | 改为 `int strlen(...)` |
| 10.4 内联汇编 | 简单示例 | 更新为完整语法 |
| 11.4 工具函数 | 缺少 `clone_impl` | 添加 |

---

## Execution Steps

### Step 1: 创建 docs 目录
```bash
mkdir -p docs
```

### Step 2: 写入更新后的规格文档
更新内容：
1. **第 1.3 节** — 函数类型符号改为 C++ 风格 `T(*)(U)`
2. **第 2.3 节** — 关键字列表添加 `clone`
3. **第 3.2 节** — 指针类型添加所有权默认语义说明
4. **第 3.7 节** — 类型修饰符说明 `unique` 为可选（与默认一致）
5. **第 7 章** — 重大更新：
   - 7.1 新增"所有权语义"节，说明赋值即转移
   - 7.5 新增 `clone()` 函数说明
6. **第 10.3 节** — FFI 函数签名全部改为 C++ 风格
7. **第 10.4 节** — 内联汇编更新为完整语法
8. **第 11.4 节** — 添加 `clone_impl` 函数
9. **第 12.x EBNF** — 添加 `clone` 到 `unary_expression`

### Step 3: 验证文档
- 检查所有代码示例为 C++ 风格
- 确认无 Rust 语法残留

---

## Success Criteria

- [ ] `docs/` 目录存在
- [ ] `docs/UltraCPP-v0.1.0-spec-zh-CN.md` 文件存在
- [ ] 所有函数签名使用 C++ 风格（`int func(type param)` 而非 `fn func(param: type) -> ret`）
- [ ] 指针所有权语义已明确说明
- [ ] 内联汇编语法已更新
- [ ] 文档历史已更新

---

## 文件位置

更新后的文档：`docs/UltraCPP-v0.1.0-spec-zh-CN.md`
